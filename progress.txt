GameNight Bingo — Ralph Progress
Branch: ralph/private-realtime-bingo
Source: tasks/prd-private-realtime-bingo.md
Completed: 0/63
Next story: US-001

## Codebase Patterns
- Stories are dependency ordered; document planned commands explicitly when their workspace/tooling lands later, and do not invent unconfirmed configuration defaults.
- Dependency installation, dependency changes, configuration changes, and database migrations require confirmation before execution.
- Local Compose data services bind to `127.0.0.1`; PostgreSQL is the only default service and future-only services must use explicit profiles.
- Project-local skills live under `.opencode/.opencode/skills`; they reference root `AGENTS.md`, use Bun scripts with Vitest, and report unavailable later-story tooling instead of creating it early.
- Workspace packages use the `@gamenight-bingo/*` namespace, extend `tsconfig.base.json`, and expose only compileable source entry points until their dependency-ordered implementation stories land; root workspaces are restricted to exactly `apps/*` and `packages/*`, and new package directories under them must be added to the validated workspace contract.
- Run `bun run test:workspace-boundaries` after workspace-manifest or domain-import changes; the TypeScript AST guard checks static, type-only, dynamic, direct CommonJS (including TypeScript-transparent and comma loader expressions), JSDoc, and triple-slash references with normalized path separators without matching ordinary comments or strings.
- Boundary parsing must use the source file's actual script kind and fail closed on syntax errors; workspace checks also require real `tsc` scripts without strictness downgrades, prohibit shared/domain module aliases, `rootDirs`, and `moduleSuffixes`, constrain relative source and root/nested package-resolution targets to `packages/domain/src`, and reject source/reference symlinks across TypeScript declaration and JavaScript-extension resolution candidates plus escaping absolute, file-URL, relative, type-reference, file-reference, and AMD dependency paths.
- Workspace typechecks must cover every discovered `src` file without inherited or implicit exclusions, verified against TypeScript's parsed effective file set, and must reject all parsed configuration diagnostics while retaining effective `noEmit` with `outDir`/`declarationDir` outside `src`; domain source also rejects JavaScript and `@ts-nocheck`. Shared/domain TypeScript configuration cannot inherit or synthesize implicit module imports, and domain bare imports plus package-valued type/AMD directives must be Node built-ins, self-references, or declared dependencies without traversal segments and validated against forbidden runtime or type-package aliases.
- Keep intentional ESLint, Prettier, TypeScript, Vitest, React Testing Library, and Playwright failures under `tests/fixtures/quality`; normal checks exclude them, while `bun run test:quality-tooling` requires each tool to fail for its expected diagnostic rather than accepting any nonzero exit.
- Vitest discovery is limited to repository `apps`, `packages`, and `tests` paths, explicitly excludes nested `node_modules` reached through Bun workspace links, runs TS tests in Node and TSX tests in jsdom, and leaves `tests/e2e` to the separate `bun run test:e2e` command.
- Runtime entry points pass `process.env` to `parseRuntimeConfig` from `packages/contracts`; downstream and domain code consume the immutable parsed type rather than reading environment state directly, and startup subprocess tests isolate environment/module state.
- After workspace dependency manifests change, run `bun install` to refresh `bun.lock`, then verify reproducibility with `bun install --frozen-lockfile`.
- Versioned Zod wire contracts live under `packages/contracts/src/v1`; every nested object is strict, top-level messages carry `schemaVersion`, identifiers and active-lobby sequences are branded, client commands contain intent rather than authoritative state, and authorized snapshots structurally omit credentials, foreign cards, future draws, prior-round history, and active event history. Lobby-wide sequences belong only on messages delivered to every authorized lobby consumer; private-only events omit them, acknowledgements declare their scope, and snapshot refinements enforce cross-object roster/state consistency.
- `docs/bingo-pattern-catalog.md` inventories source diagrams as `p1/dNN` in reading order with exact, flexible-example, or alias modes and tested 5x5 source masks; its generated core section is parity-tested from `packages/patterns/src/catalog.ts` rather than maintaining a second runtime mask source.
- Domain randomization accepts injected `CryptographicRandomBytes`; runtime adapters own the secure byte source, deterministic tests inject seeded bytes, and bounded integer selection uses rejection sampling rather than biased modulo reduction.
- Runtime pattern definitions store every accepted required-cell arrangement as a 5x5 mask: One Line has 12 line masks, Two Lines has all 66 distinct line-pair unions, exact patterns retain one untransformed source mask, and matching always satisfies index 12 as the free center.
- Exact generated Markdown parity blocks use `prettier-ignore` markers around the generated boundaries so formatting cannot silently rewrite canonical generator output.
- Source review records map every PDF diagram to its runtime ID and disposition (`exact-mask-match`, `flexible-rule-example`, or `source-alias`), record all 25 cells reviewed, and parity-test exact records against canonical runtime masks without duplicating masks in the review table.
- Pattern preview galleries derive a normalized model from `patternCatalog`; flexible-rule source examples live with the canonical pattern source, committed JSON goldens cover every runtime selection, and standalone generated HTML is parity-tested and isolated from Prettier with an exact-node ignore marker.
- Category preview pages reuse the source-filtered canonical preview model and shared renderer while exposing category-specific creation/generation functions, committed goldens, and standalone HTML paths.
- Complete pattern audits join all PDF inventory/review rows to runtime IDs, aliases, exact or flexible mask parity, approved identity/mask constraints, and the combined category preview goldens; mutation tests must prove each audit failure category remains fail-closed.
- Domain transitions return discriminated stable errors for expected state rejection and throw `RangeError` only for malformed internal values such as invalid counts, timestamps, or durations.
- Prisma 7 is confined to `packages/database`; `bun run db:generate` creates the ignored client during install, while repository interfaces expose durable aggregate state without exporting Prisma types.
- PostgreSQL-only partial indexes, checks, and composite foreign keys live in append-only migration SQL; the durable schema keeps one current round per lobby so round cards, marks, private draw order, calls, winners, events, and command results cascade without prior-round history.
- Database integration tests require an explicitly approved, migrated PostgreSQL 16 `TEST_DATABASE_URL`; the generic Vitest run skips them when the variable is absent.
- Serializable command transactions lock and scope work to one lobby, check persisted command results before mutation, and use bounded observable retries; callbacks may rerun only after rollback and must have no external effects, while only fresh active-lobby commits return a broadcastable event.
- PostgreSQL concurrency tests must coordinate actual transaction overlap before asserting Serializable retries; observing the second command waiting on the lobby fence avoids flaky assumptions about `Promise.all` scheduling. Lock-coordination harnesses must roll back and release checked-out clients in `finally` blocks and await blocked operations during cleanup so synchronization failures cannot hang the suite.
- Retry classification scans nested Prisma/adapter `cause`, `meta`, and `driverAdapterError` metadata before falling back to generic conflict codes; PostgreSQL `40P01` deadlocks take precedence over outer Prisma `P2034` wrappers so telemetry stays accurate.
- Active lobby creation goes through `LobbyStateRepository.createActive`: generate each candidate outside retryable transactions, serialize PostgreSQL admission before counting waiting/active lobbies, and retry only active-code collisions so capacity cannot be oversubscribed.
- Normalize usernames at the trusted reservation boundary by rejecting Unicode control/format characters, trimming and collapsing Unicode whitespace, applying NFC, enforcing the 128-character display/key limit, then lowercasing for lobby-scoped uniqueness; serialize reservations on the lobby row before checking names and configured capacity.
- Participant session credentials use 32 injected cryptographic bytes encoded as unpadded base64url; hash the exact encoded token with SHA-256 before persistence, return it only through a Secure/HttpOnly/SameSite=Strict lobby-path cookie, and resolve identity through a narrow active-lobby plus hash lookup rather than a full aggregate or client-supplied participant ID.
- Same-device session lifecycle transitions share the active-lobby transaction fence and sample their repository clock after acquiring it on every retry: disconnect stores one durable deadline, rejoin-status sweeps expiry before cookie validation, `now >= rejoinUntil` atomically expires due credentials and departs a slot only when no valid sibling remains, timely rejoin clears lifecycle timestamps while invalidating siblings, and persistence derives waiting eligibility for new active-round joins.
- Next.js consumes the workspace's raw TypeScript packages through Webpack with a `.js` extension alias because their Node ESM source specifiers intentionally name emitted `.js` files; keep `.next` output ignored by Git and ESLint.
- Lobby entry adapters validate strict v1 intent contracts and rate-limit mutations plus lobby-fenced reads before body or database work using an authenticated proxy marker, hashed requester identities, and bounded per-operation storage. Replayed entry commands decode the complete original allowlisted projection without minting credentials, rejoin command persistence and activation share one lock-time lobby transaction, and snapshots authenticate/project in one fenced actor-scoped query that excludes unrelated departed history while preserving the 25-player round plus one waiting replacement only when that waiting participant is the requester.
- Next App Router API modules forward supported methods, unsupported methods, and unknown private catch-all paths to the shared versioned dispatcher so stable JSON errors and `no-store` headers survive the framework routing boundary.
- Authoritative snapshot projections construct stage-specific fields only for stages that require them; an open co-winner window may contain confirmed winners before a settled result exists.
- Framework-routing guarantees require live Next request coverage under an approved migrated `TEST_DATABASE_URL`; dispatcher unit tests and route-source assertions do not prove unsupported methods or catch-all resources retain stable errors and private cache headers.
- Realtime tickets use 32 random bytes encoded as canonical unpadded base64url, persist only SHA-256 hashes bound to lobby/participant/session scope, and issue under the lobby fence from an active cookie session. Consumption burns the presented row whether valid or not and returns only authoritative identity for a valid unexpired active scope; issuance purges expired abandoned tickets, and session deactivation invalidates outstanding tickets so rejoin cannot revive them.
- Round-control commands are rate-limited before lobby-fence acquisition, then resolve the active session, actor role, current round, and own card inside one Serializable transaction; command results persist canonical intent so changed-payload command-ID reuse is rejected, active-lobby replays retain their original sequence, and private mark acknowledgements allocate no lobby sequence. Before replacing a round, expire due participant sessions before roster selection and detach prior active-event and command-result round scopes so replay tombstones survive without retaining old gameplay state.

Append-only iteration log
-------------------------

## 2026-07-16 01:31:51 MST - US-001
- What was implemented
  - Verified the user-authorized baseline retains the deleted outdated Next.js scaffold, added PDFs under `docs/`, AI skills under `.opencode/`, and this PRD.
  - Confirmed the baseline was already committed before implementation as `chore(project): establish clean project baseline` and the worktree remained clean.
- Files changed: `progress.txt`, `prd.json`
- Commit message: `feat: US-001 - Commit the authorized project baseline`
- Checks:
  - `git status` (pass)
  - `git diff` (pass)
  - `git log --oneline -10` (pass)
  - Secret-pattern scan of tracked text files (pass)
  - Typecheck (not run; application code and configuration were intentionally removed)
  - Lint (not run; application code and configuration were intentionally removed)
  - Tests (not run; US-001 explicitly requires no tests)
- Implementation agents:
  - Recommended: none
  - Used: skipped because this was a trivial baseline verification story with no implementation work
- Specialist review:
  - Agents used: Ralph self-review, as required for trivial changes in standard mode
  - Findings: pending staged-diff review
- Decisions (why):
  - Reused the existing exact baseline commit instead of duplicating it because `ae87476` already satisfies the story's required baseline commit and no implementation followed it.
  - Preserved every authorized deletion and addition because the baseline commit exactly matches the story scope.
- **Learnings for future iterations:**
  - The intentional baseline contains no application workspace or quality-tool configuration; subsequent stories establish those assets in dependency order.
  - Binary PDF whitespace reported by `git diff --check` is embedded PDF data, not editable source whitespace.
- Known issues / follow-ups:
  - None for this story.
- Next iteration start:
  - Implement US-002, beginning with the README requirements and current baseline contents.
---

Review outcome for US-001:
- Staged self-review completed with no actionable findings.
- Residual risk is limited to the intentionally untested, code-free baseline; no application checks exist yet.

## 2026-07-16 11:33:39 MST - US-002
- What was implemented
  - Added a complete root README covering the MVP, modular architecture, prerequisites, setup, confirmed runtime defaults, Docker and Bun command contracts, workspace boundaries, patterns/themes, authoritative reconnect behavior, privacy/security, checks, contribution rules, and required troubleshooting.
  - Clearly marked the workspace, Compose stack, and package scripts as planned later stories so contributors are not told that unavailable commands currently work.
- Files changed: `README.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-002 - Create the project README`
- Checks:
  - `ruby -e '<local Markdown link validation>'` (pass)
  - External prerequisite documentation links fetched successfully (pass)
  - `ruby -e '<required documented-command validation>'` (pass)
  - `git diff --check` (pass)
  - `test ! -e package.json && test ! -e tsconfig.json && test -z "$(git ls-files '*.ts' '*.tsx')"` (pass; confirms typecheck is not applicable before the dependency-ordered workspace/tooling stories)
  - Lint (not run; no root package, lint configuration, or application source exists yet)
  - Tests (documentation validation commands passed; no project test runner exists yet)
- Implementation agents:
  - Recommended: `documentation-engineer`
  - Used: skipped because this was a straightforward documentation-only story and standard mode budgets zero implementation agents for trivial work
- Specialist review:
  - Agents used: `code-reviewer`, `documentation-engineer`; targeted re-review by `documentation-engineer`
  - Findings: fixed the unconfirmed PostgreSQL Compose service name before commit; discarded metadata findings that conflicted with Ralph's required commit message and post-review PRD staging sequence
- Decisions (why):
  - Documented only the seven PRD-confirmed application defaults and deferred infrastructure-specific names/defaults because inventing them would constrain later Compose and runtime-configuration stories.
  - Included future root commands as an explicit project contract with a bootstrap warning because US-002 requires command documentation before US-003, US-006, and US-007 create their implementations.
  - Did not add documentation tooling, package configuration, or tests because doing so would cross later dependency-ordered stories and require configuration/dependency confirmation.
  - Kept Ralph's exact required story commit message despite review suggestions to add a conventional scope because the iteration instructions take precedence.
- **Learnings for future iterations:**
  - The README is the contributor-facing source for the target command contract; later infrastructure, workspace, and quality-tooling stories must keep it synchronized with executable scripts and actual environment names/defaults.
  - Root application checks remain unavailable until the workspace and quality-tooling stories; documentation-only iterations can still validate links, required command references, and whitespace without adding dependencies.
- Known issues / follow-ups:
  - Later stories must replace bootstrap caveats with concrete environment templates and verified commands as those assets are added.
- Next iteration start:
  - Implement US-003 only after obtaining the required Docker configuration confirmation, then update the README with the actual Compose environment names/defaults.
---

## 2026-07-16 12:42:32 MST - US-003
- What was implemented
  - Added `compose.yaml` with PostgreSQL 16, configurable local database settings, a persistent named volume, and a readiness healthcheck.
  - Added Redis behind the explicit `redis` profile without making it a default or MVP dependency, while keeping web and game-server processes outside Docker.
  - Updated the README with the executable Compose lifecycle, environment defaults, loopback-only bindings, optional Redis command, persistence behavior, and PostgreSQL troubleshooting.
- Files changed: `compose.yaml`, `README.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-003 - Add local Docker infrastructure`
- Auto approval: `enabled`
- Checks:
  - `docker compose config --quiet` (pass)
  - `test "$(docker compose config --services)" = "postgres"` (pass)
  - `test "$(docker compose --profile redis config --services | paste -sd, -)" = "postgres,redis"` (pass)
  - `POSTGRES_HOST_PORT=55432 ... docker compose -p gamenight-bingo-us003-check up -d --wait postgres` (pass; PostgreSQL became healthy)
  - `docker compose -p gamenight-bingo-us003-check exec -T postgres pg_isready ...` (pass)
  - `docker compose -p gamenight-bingo-us003-check exec -T postgres psql ... -c "SELECT 1;"` (pass)
  - `docker compose -p gamenight-bingo-us003-check down` (pass; test container and network removed, named data volume retained by design)
  - `git diff --check` (pass)
  - `test ! -e package.json && test ! -e tsconfig.json && test -z "$(git ls-files '*.ts' '*.tsx')"` (pass; confirms typecheck is not applicable before the dependency-ordered workspace/tooling stories)
  - Lint (not run; no root package, lint configuration, or application source exists yet)
  - Tests (Compose model, profile behavior, PostgreSQL health, and live query validations passed; no project test runner exists yet)
- Implementation agents:
  - Recommended: `devops-engineer`, `database-administrator`
  - Used: `devops-engineer`; skipped `database-administrator` because standard mode budgeted one useful adviser and the PostgreSQL configuration was directly verified with readiness and query checks
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `security-engineer`
  - Findings: no blocking findings; corrected the copy-safe profile assertion, retained the planned post-review `prd.json` manifest entry, and declined non-blocking digest pinning for the local development images
- Decisions (why):
  - Used explicit local-only defaults because this story establishes the Compose contract and the README clearly prohibits reusing the development password in hosted environments.
  - Bound published ports to loopback because these services are for local development and do not need LAN exposure.
  - Kept Redis defined but profile-gated because the PRD requires it to be available only for future optional work without affecting the MVP stack.
  - Used PostgreSQL 16 and Redis 7 Alpine version tags rather than immutable digests because local development should receive compatible image security updates without manual digest maintenance.
  - Skipped source-text unit tests because this is declarative infrastructure; Compose validation and a live PostgreSQL readiness/query check provide meaningful coverage instead.
- **Learnings for future iterations:**
  - `docker compose config --services` excludes the profile-gated Redis service by default and includes it only with `--profile redis`.
  - PostgreSQL initialization variables apply when the named volume is first initialized; changing them later does not rewrite an existing database cluster.
  - Root application typecheck, lint, and Vitest commands remain unavailable until the workspace and quality-tooling stories.
- Known issues / follow-ups:
  - The isolated health-check project intentionally retained its named volume, matching the documented non-destructive `docker compose down` behavior.
- Next iteration start:
  - Implement US-004 by creating root contributor/agent guidance aligned with the README and PRD conventions.
---

## 2026-07-16 12:48:46 MST - US-004
- What was implemented
  - Added root `AGENTS.md` with repository-wide guidance for architecture, commands, domain boundaries, HTTP/realtime behavior, TDD, UI, accessibility, security/privacy, database migrations, local operations, confirmations, and Git workflow.
  - Documented command idempotency, persist-before-broadcast transactions, monotonic active-lobby event sequences, authoritative reconnect snapshots, and conventional commits.
  - Updated the README contributor guidance to link to the now-available root agent guide.
- Files changed: `AGENTS.md`, `README.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-004 - Add project implementation guidance`
- Auto approval: `enabled`
- Checks:
  - `ruby -e '<required AGENTS.md section and invariant validation>'` (pass)
  - `ruby -e '<local Markdown link validation>'` (pass)
  - `git diff --check` (pass)
  - `ruby -e '<root TypeScript applicability validation>'` (pass; confirms typecheck is not applicable before the dependency-ordered workspace/tooling stories)
  - Lint (not run; no root package or lint configuration exists yet)
  - Tests (documentation validations passed; no project test runner exists yet)
- Implementation agents:
  - Recommended: `documentation-engineer`
  - Used: skipped because standard mode budgets zero implementation agents for a straightforward documentation-only story
- Specialist review:
  - Agents used: `code-reviewer`, `documentation-engineer`
  - Findings: none; both specialists returned empty actionable finding lists
- Decisions (why):
  - Documented both currently executable Compose commands and future Bun command contracts so the guide is useful now without claiming later-story tooling already exists.
  - Kept application rules at repository scope because the planned package directories do not exist yet and all future modules need the same authority, privacy, and persistence invariants.
  - Added an exact-message exception to the conventional commit rule because authorized automation workflows can require a specific commit message.
- **Learnings for future iterations:**
  - Root `AGENTS.md` is the repository-wide implementation authority; add narrower guidance only when a future module has genuinely directory-specific conventions.
  - Validation must remain proportional while the workspace is absent: check documentation structure, links, and whitespace rather than creating later-story tooling early.
- Known issues / follow-ups:
  - Root typecheck, lint, formatting, and Vitest commands become available only after the workspace and quality-tooling stories.
- Next iteration start:
  - Implement US-005 by reviewing the project-local skills and aligning them with root `AGENTS.md`, Bun, Vitest, and Bingo-specific examples.
---

## 2026-07-16 12:55:07 MST - US-005
- What was implemented
  - Rewrote all five project-local skills around GameNight Bingo architecture, server authority, privacy, accessibility, TDD, and focused Git workflows.
  - Replaced npm/npx and calculator guidance with Bun-driven Vitest commands and explicit deferral when dependency-ordered tooling is not yet available.
  - Pointed every skill to root `AGENTS.md` and retained the existing skill names and locations.
- Files changed: `.opencode/.opencode/skills/code-formatter/SKILL.md`, `.opencode/.opencode/skills/feature-implementer/SKILL.md`, `.opencode/.opencode/skills/git-manager/SKILL.md`, `.opencode/.opencode/skills/refactor-specialist/SKILL.md`, `.opencode/.opencode/skills/test-runner/SKILL.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-005 - Rewrite project-local skills`
- Auto approval: `enabled`
- Checks:
  - `ruby -e '<skill frontmatter, reference, Bun/Vitest, and forbidden-example validation>'` (pass)
  - `ruby -e '<documented skill command and project-rule validation>'` (pass)
  - `git diff --check` (pass)
  - `ruby -e '<root TypeScript applicability validation>'` (pass; confirms typecheck is not applicable before US-006 establishes the workspace)
  - Lint (not run; no root package or lint configuration exists yet)
  - Tests (skill reference and documented-command validations passed; no project test runner exists until US-007)
- Implementation agents:
  - Recommended: none
  - Used: skipped because this was a straightforward documentation/configuration rewrite and standard mode budgets zero implementation agents for trivial work
- Specialist review:
  - Agents used: `code-reviewer`, `documentation-engineer`; targeted re-review by both specialists
  - Findings: fixed implicit package execution, the incomplete refactor TDD rule, and unsupported coverage guidance; targeted re-review returned no actionable findings; discarded the pre-review PRD-status finding because Ralph requires that update only after review passes
- Decisions (why):
  - Rewrote the existing five skills in place rather than adding or renaming skills because the story asks to make the authorized skill set project-specific.
  - Documented planned root commands with availability guards because US-006 and US-007 establish the executable workspace and quality tooling later.
  - Omitted direct formatter and coverage invocations until repository scripts and providers exist because project skills must not download or invent tooling.
  - Kept reusable guidance in root `AGENTS.md` rather than adding a nested guide because the skill rules directly derive from existing repository-wide conventions.
- **Learnings for future iterations:**
  - Skill validation can remain dependency-free before the workspace exists by checking frontmatter, root-guide references, forbidden legacy examples, and documented command contracts.
  - `bun run test` must invoke Vitest; `bun test` is intentionally excluded because it selects Bun's built-in runner.
- Known issues / follow-ups:
  - Root typecheck, lint, formatting, and Vitest scripts remain unavailable until their dependency-ordered workspace and tooling stories.
- Next iteration start:
  - Implement US-006 by establishing the approved Bun workspace and strict TypeScript package boundaries.
---

## 2026-07-16 13:12:07 MST - US-006
- What was implemented
  - Established the private Bun workspace with `apps/web`, `apps/game-server`, and the seven required responsibility-focused packages under the `@gamenight-bingo/*` namespace.
  - Added strict shared TypeScript configuration and per-workspace typecheck scripts with compile-only entry points, without creating premature application or package behavior.
  - Added a TypeScript AST-based boundary checker and executable matcher tests that reject React, Next.js, Prisma, and Socket.IO references from domain source and dependencies.
  - Updated repository documentation and guidance for the now-available install, boundary, and typecheck commands while retaining later-story runtime and quality-tooling caveats.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: checker module missing)
  - `bun install --frozen-lockfile` (pass)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and pinned TypeScript resolved)
  - `git diff --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: `build-engineer`, `typescript-pro` for workspace shape, strict compiler options, boundary strategy, and validation guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`; targeted re-review by `code-reviewer` and `qa-expert`
  - Findings: initial type-reference/JSDoc import-type and relative dynamic/CommonJS bypasses were fixed, but targeted re-review found remaining medium bypasses for JSDoc `@import`, relative `types=` directives, and backslash-relative specifiers; commit blocked by the one-re-review limit
- Decisions (why):
  - Used compile-only `export {}` entry points rather than framework/server/package placeholders because later dependency-ordered stories own actual behavior.
  - Used Bun workspace filtering for no-emit per-package typechecks instead of TypeScript project references because the current packages have no dependency graph and project references would require generated build artifacts.
  - Pinned TypeScript 5.9.3 instead of the initially resolved 7.0.2 because TypeScript 7 exposes its AST API through explicitly unstable entry points, while the stable compiler API is appropriate for the boundary guard.
  - Kept the boundary suite as directly executable assertions rather than introducing Vitest early because US-007 owns the test framework configuration.
- **Learnings for future iterations:**
  - Bun's `--filter '@gamenight-bingo/*'` runs the shared `typecheck` script across all workspace packages from the root.
  - TypeScript compiler options and workspace package exports do not enforce architectural import restrictions by themselves; the AST guard is the mechanical domain boundary.
  - Framework-specific types and dependencies should be added only to the workspace that needs them rather than broadening the shared base configuration.
- Known issues / follow-ups:
  - Resolve and regression-test JSDoc `@import` module references, relative triple-slash `types=` directives, and backslash-relative static/dynamic/CommonJS specifiers before re-running the staged review loop.
  - US-006 remains `passes: false` and no commit was created because medium findings remained after the allowed targeted re-review.
  - Root lint, formatting, generic Vitest, and Git-hook commands remain unavailable until US-007.
  - Web and game-server development scripts remain unavailable until their runtime implementation stories; no no-op scripts were added.
- Next iteration start:
  - Resume US-006 by fixing the three targeted re-review bypasses, rerunning boundary/type checks, and restarting the staged review loop.
---

## 2026-07-16 13:24:21 MST - US-006
- What was implemented
  - Resumed the blocked workspace story and added regression coverage for JSDoc `@import`, escaping triple-slash `types=` directives, and backslash-relative module specifiers.
  - Extended the TypeScript AST boundary guard to inspect JSDoc import tags, constrain relative type references to `packages/domain`, and normalize path separators before containment checks.
  - Preserved the existing workspace, strict TypeScript configuration, package boundaries, and dependency-ordered runtime/tooling scope.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail: JSDoc `@import` was not detected)
  - Targeted boundary probes (expected fail: escaping `types=` and backslash-relative imports returned no violations)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun install --frozen-lockfile` (pass; no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: no additional agents; both recommended agents were already used for this story, and the resumed fixes followed their guidance plus concrete prior review findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `security-engineer` planned for the complete staged workspace/configuration diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Normalized backslashes only for containment analysis while retaining the original specifier in diagnostics so the guard is platform-independent without rewriting source.
  - Added the missing constructs to the existing AST traversal instead of introducing text matching because semantic references should be rejected without false positives from comments or ordinary strings.
  - Restarted the review loop because the prior iteration correctly stopped after its one permitted targeted re-review still found blocking issues.
- **Learnings for future iterations:**
  - TypeScript exposes JSDoc `@import` as `JSDocImportTag.moduleSpecifier`; ordinary import-type handling does not cover it.
  - Triple-slash `types=` directives are stored separately from `referencedFiles`, so relative containment checks must cover both directive collections.
  - POSIX path resolution treats backslashes as ordinary characters; normalize module separators before testing whether a relative reference escapes a package.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 13:30:43 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- Merged blocking findings identified two root causes: triple-slash `path=` references did not normalize backslashes, and computed CommonJS require forms bypassed loader inspection.
- Fixed both root causes with failing regression tests first, then also canonicalized forbidden package-name casing/separators and covered the direct `module.require.call` equivalent raised by security review.
- Added the QA review's non-blocking dependency-section coverage because it was specific, cheap, and directly exercised existing boundary behavior.
- Broader arbitrary loader-alias dataflow analysis was not added because the local syntax guard is scoped to semantic module references and direct loader forms; implementing a whole-program resolver would be disproportionate to this workspace story.
- Post-fix checks: `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review of the revised complete staged diff.

Targeted re-review outcome for US-006 at 2026-07-16 13:33:07 MST:
- `code-reviewer` and `qa-expert` returned no actionable findings.
- `security-engineer` found one remaining medium direct-loader bypass: parenthesized `require`, `module.require`, and computed `module["require"]` expressions are not unwrapped before loader matching.
- Targeted probes confirmed all reported parenthesized literal and nonliteral forms currently return no violations.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and the PRD was not updated.
- Next iteration start: add failing regressions for parenthesized direct loaders, unwrap parenthesized expressions before CommonJS matching, rerun all US-006 checks, and restart the staged review loop.

## 2026-07-16 13:35:21 MST - US-006
- What was implemented
  - Resumed the workspace story by adding regression coverage for parenthesized `require`, `module.require`, computed `module["require"]`, and direct `.call(...)` forms with literal and non-literal arguments.
  - Unwrapped parenthesized expressions before direct CommonJS loader matching, closing the final bypass reported by the prior targeted security review.
  - Preserved the established modular workspace, strict TypeScript configuration, and focused direct-reference boundary scope.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: `(require)("react")` returned no violation)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun install --frozen-lockfile` (pass; no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents already advised this story and the remaining work was a concrete, narrowly scoped prior-review finding
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `security-engineer` planned for the complete staged workspace/configuration diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Unwrapped only parenthesized expressions in the existing direct-loader matcher because that closes the demonstrated bypass without expanding into disproportionate whole-program alias analysis.
  - Covered both forbidden literals and non-literal arguments because the checker must reject framework references and indeterminate direct loaders through the same parenthesized syntax.
  - Did not add nested `AGENTS.md` guidance because the existing root guide already defines the reusable domain-boundary and validation contracts.
- **Learnings for future iterations:**
  - Parentheses change the TypeScript AST shape of a call target; direct loader matchers must unwrap `ParenthesizedExpression` nodes before checking identifiers or property access.
  - Regression tests should cover both literal forbidden imports and non-literal loader arguments when adding a recognized loader syntax.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 13:44:49 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- Merged blocking findings identified six root causes: incomplete parenthesis normalization across direct CommonJS target components, incorrect all-TSX parsing without fail-closed diagnostics, absolute/file-URL and source-symlink containment gaps, omitted AMD dependency directives, and missing enforcement of strict base/typecheck workspace configuration.
- Added failing regressions for each reported syntax, parser, filesystem, directive, configuration, and symlink behavior before implementing the fixes.
- Centralized parenthesis unwrapping for the full direct CommonJS target, receiver, computed key, and `.call` path; inferred script kind by extension and converted parse diagnostics into violations.
- Rejected absolute paths/file URLs and domain-source symlinks, applied containment checks across direct/type/file/AMD references, inspected forbidden AMD dependencies, and validated strict base configuration plus each workspace's base extension and typecheck script.
- Updated root `AGENTS.md` and Codebase Patterns with the reusable fail-closed parsing, containment, and configuration-validation contracts.
- Post-fix checks: `bun scripts/check-workspace-boundaries.test.mjs` (pass), `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), `bun pm ls --all` (pass), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review of the revised complete staged diff because the fixes touched each original specialist area.

Targeted re-review outcome for US-006 at 2026-07-16 13:49:43 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff.
- Merged blocking findings identified three remaining medium root causes: computed `require["call"]`/`module.require["call"]` targets bypass direct CommonJS detection; symlink rejection does not cover the `packages/domain/src` root or referenced paths elsewhere under `packages/domain`; and workspace validation accepts no-op typecheck scripts plus local `strict: false` overrides.
- The findings are in scope because they directly weaken the claimed direct-loader, filesystem-containment, and strict-TypeScript acceptance criteria.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and the PRD was not updated.
- Next iteration start: add failing regressions for computed `call` access, root/relative-target symlinks, no-op typecheck scripts, and strictness overrides; implement the narrow fixes, rerun all US-006 checks, and restart the staged review loop.

## 2026-07-16 13:53:52 MST - US-006
- What was implemented
  - Resumed the workspace story and closed the recorded computed `require["call"]`, source-root/reference symlink, no-op typecheck, and local strictness-downgrade gaps.
  - Unified static property and element access recognition for direct CommonJS loaders, including literal and non-literal computed `.call` forms.
  - Required the established real `tsc` command in every workspace, rejected local strict-family options set to false, and checked relative domain targets plus the source root for symlink traversal.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: `require["call"]` returned no violation)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun install --frozen-lockfile` (pass; no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-006 and the remaining changes directly implement concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: initial bare-package traversal, framework type-package, unexpected-workspace, and remaining direct CommonJS loader findings were fixed before targeted re-review
- Decisions (why):
  - Reused one static-member parser for dotted and computed CommonJS access because both forms represent the same direct loader boundary and should not drift.
  - Enforced the workspace's established `tsc --project tsconfig.json --pretty false` command exactly because accepting arbitrary nonempty scripts allowed checks that never invoked TypeScript or masked failures.
  - Inspected symlinks only along source and referenced domain paths rather than rejecting unrelated repository symlinks because the boundary risk is filesystem traversal from domain code.
- **Learnings for future iterations:**
  - TypeScript element access with a static string key must receive the same boundary treatment as dotted property access, including nested `require` and `call` members.
  - Lexical path containment is insufficient when a contained path segment can be a symlink; inspect the source root and each referenced path segment without following symlinked source trees.
  - Strict inheritance can be weakened by workspace compiler overrides, so boundary validation must reject false strict-family options as well as a false top-level `strict` value.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 16:01:55 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- The merged blocking findings identified four medium root causes: traversal segments in declared/self package imports, incomplete forbidden framework `@types` families, package directories matched by workspace globs but omitted from validation, and remaining direct CommonJS loader-value forms.
- Added failing regressions before implementation for slash/backslash package traversal, runtime/type-package variants, recursively bound loaders, constant-computed `module.require`, direct loader values and standard invocation helpers, and unexpected workspace paths.
- Rejected traversal before declared-package lookup, expanded forbidden type-package families, rejected unexpected package workspaces, recursively reduced bound bind functions, statically evaluated constant member names, and failed closed when a direct CommonJS loader value is not part of a modeled invocation.
- Post-fix checks: `bun scripts/check-workspace-boundaries.test.mjs` (pass), `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), `bun pm ls --all` (pass), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because the fixes affected every original specialist area.

Targeted re-review outcome for US-006 at 2026-07-16 16:05:58 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` confirmed the initial traversal, type-package, expected-workspace-directory, and direct-loader-form fixes but identified two remaining medium root causes.
- Direct loader-value suppression currently accepts any loader node nested below a recognized loader-call ancestor, including unrelated extra-argument subtrees such as `require("node:path", Reflect.apply(require, null, ["react"]))`; the exemption must be limited to nodes structurally consumed by the modeled callee/receiver path.
- The root configuration requires the two approved workspace globs but permits additional globs such as `tools/*`, while discovery validates only `apps/*` and `packages/*`; the configured workspace-glob set must be restricted to exactly the supported pair or every configured glob must be resolved and validated.
- These findings remain in scope because both allow code selected by the configured workspace or a direct loader path to bypass the claimed mechanical boundary.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add failing regressions for loader values in unrelated direct/`call`/`apply` argument subtrees and extra root workspace globs; constrain loader exemptions to the modeled operand path and require the approved workspace-glob set exactly; rerun all checks and restart the staged review loop.

Review remediation for US-006 at 2026-07-16 14:02:06 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- Merged blocking findings identified four medium root causes: TypeScript-transparent expressions bypassed direct loader matching; direct CommonJS `.apply` calls were not inspected; extensionless/directory references did not inspect resolved symlink candidates; and ESM URL normalization could turn encoded contained paths into escapes.
- Added failing regressions before implementation for transparent wrappers, `.apply` literal/non-literal arguments, extensionless and directory symlink candidates, encoded dot-segment/separator paths, query/fragment ambiguity, and separate CommonJS path semantics.
- Unwrapped transparent TypeScript expression nodes, recognized static dotted/computed `.apply` calls, canonicalized ESM references through file URLs, and checked supported extension/index candidates for symlink traversal.
- Post-fix checks: `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), `bun pm ls --all` (pass), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because each original specialist area was affected by the substantive fixes.

Targeted re-review outcome for US-006 at 2026-07-16 14:05:48 MST:
- `qa-expert` returned no actionable findings; `code-reviewer` and `security-engineer` identified two deduplicated medium root causes that remain in scope.
- TypeScript instantiation expressions such as `(require<string>)("react")` remain transparent at runtime but are represented as `ExpressionWithTypeArguments`, which the direct-loader unwrapping does not yet cover.
- Symlink candidate generation omits declaration files and TypeScript extension substitution, so extensionless imports resolving to `.d.ts` and `.js` specifiers resolving to `.ts` can avoid referenced-path symlink inspection.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add failing literal/non-literal regressions for instantiation-wrapped direct/call/apply loaders and filesystem regressions for `.d.ts` plus `.js`-to-TypeScript resolution candidates, implement the narrow fixes, rerun all checks, and restart the staged review loop.

## 2026-07-16 14:09:01 MST - US-006
- What was implemented
  - Resumed the modular-workspace story and added regression coverage for instantiation-wrapped direct, `.call`, and `.apply` CommonJS loaders with literal and non-literal arguments.
  - Treated TypeScript `ExpressionWithTypeArguments` nodes as transparent loader expressions and expanded symlink checks to declaration files plus TypeScript's `.js`, `.jsx`, `.mjs`, and `.cjs` extension substitutions.
  - Preserved the established strict workspace, package boundaries, and dependency-ordered runtime/tooling scope.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: `(require<string>)("react")` returned no violation)
  - Targeted declaration/extension-substitution probe (expected fail before implementation: `.d.ts` and `.js`-to-`.ts` symlink candidates returned no violations)
  - `bun install --frozen-lockfile` (pass; no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-006 and the remaining work directly addressed concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer`; targeted re-review by `code-reviewer` and `security-engineer` planned after substantive fixes
  - Findings: initial review findings for unscanned reachable source, comma-wrapped loaders, and module aliases were fixed; targeted re-review found remaining `rootDirs` and package-resolution redirect gaps
- Decisions (why):
  - Reused the existing transparent-expression normalization because instantiation expressions alter only TypeScript typing and do not change the runtime loader target.
  - Modeled TypeScript's extension substitutions explicitly instead of checking only the written path because a contained import can resolve through a symlinked `.d.ts`, `.ts`, `.tsx`, `.mts`, or `.cts` candidate.
  - Did not add application behavior, framework dependencies, linting, formatting, or Vitest because those belong to later dependency-ordered stories.
- **Learnings for future iterations:**
  - TypeScript represents instantiation expressions such as `require<string>` as `ExpressionWithTypeArguments`; direct-loader analysis must unwrap that node before matching the runtime expression.
  - Symlink containment checks must inspect declaration files and TypeScript extension substitutions, not only the literal import path and simple extensionless implementation files.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 14:16:48 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- `qa-expert` returned no actionable findings; the merged blocking findings identified three medium root causes: relative references could reach unscanned source outside `packages/domain/src`, comma expressions could wrap direct CommonJS loaders, and TypeScript/package aliases could hide forbidden or escaping targets.
- Added failing regressions first for import and triple-slash escapes from the source root, literal/non-literal comma-wrapped direct/call/apply loaders, shared/domain TypeScript aliases, and domain package import aliases.
- Constrained source references to `packages/domain/src`, unwrapped comma expressions through their runtime right operand, and rejected aliases inherited by or declared for the domain workspace while preserving workspace-local aliases elsewhere.
- Post-fix checks: `bun scripts/check-workspace-boundaries.test.mjs` (pass), `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), `bun pm ls --all` (pass), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer` and `security-engineer` review because the fixes addressed their findings; `qa-expert` is not repeated because its initial findings list was empty.

Targeted re-review outcome for US-006 at 2026-07-16 14:22:27 MST:
- `code-reviewer` and `security-engineer` reviewed the revised complete staged diff; both confirmed the initial three findings were addressed.
- The merged blocking findings identified two remaining medium root causes: shared/domain `rootDirs` can redirect an apparently contained import to unscanned source, and package resolution metadata can redirect relative package-directory or domain self-references to unscanned targets outside `packages/domain/src`.
- These findings remain in scope because they materially bypass the mechanical domain-import boundary required by US-006.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add failing regressions for shared/domain `rootDirs`, nested relative package `types`/`main` redirects, and domain self-reference `exports` redirects; either reject the redirecting configuration or resolve and constrain every static target to `packages/domain/src`, then rerun all checks and restart the staged review loop.

## 2026-07-16 14:25:33 MST - US-006
- What was implemented
  - Resumed the modular-workspace story and added regressions for inherited/local `rootDirs`, escaping domain self-reference exports, and nested package `main`/`types` redirects.
  - Rejected shared or domain `rootDirs`, recursively inspected root and nested domain package manifests, and constrained package resolution targets and their symlink candidates to `packages/domain/src`.
  - Preserved the existing safe `./src/index.ts` domain export and the established strict workspace structure.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `enabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: domain `rootDirs` returned no configuration error)
  - `bun install --frozen-lockfile` (pass; no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents previously advised US-006 and the remaining work directly addressed concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: initial findings were fixed; targeted re-review found remaining composed-loader, directive-import, source-checking, and TypeScript redirect/exclusion gaps, so the commit remains blocked
- Decisions (why):
  - Rejected `rootDirs` rather than resolving its virtual merge behavior because the domain workspace does not require it and prohibiting redirects keeps the boundary fail-closed.
  - Allowed package metadata only when every static resolution target remains within domain source, preserving the existing package export without permitting root or nested manifest escapes.
  - Recursively inspected nested `package.json` files because relative directory imports can consult package metadata below the domain root.
- **Learnings for future iterations:**
  - Source-only AST inspection is insufficient when TypeScript or package metadata can redirect an apparently safe import to unscanned files.
  - Root and nested package `main`, `module`, `types`, `typings`, `exports`, and `typesVersions` targets must remain inside `packages/domain/src` and receive the same symlink-candidate checks as source imports.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 14:25:33 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- The merged blocking findings identified five root causes: inherited or implicit TypeScript module resolution, `noCheck` or incomplete source inclusion, bound CommonJS loaders, browser/package redirects, and undeclared bare imports resolving through hoisted aliases.
- Added failing regressions before implementation for root `extends`, implicit `types`/JSX imports, `noCheck`, all-source include coverage, bound `require`, browser redirects, and declared-package enforcement.
- Rejected root configuration inheritance and implicit domain module options, required `src/**/*` without restrictive `files`/`exclude`, prohibited `noCheck`, rejected bound CommonJS loaders, validated browser targets, and restricted bare domain imports to built-ins, self-references, or declared dependencies.
- The low-severity request for end-to-end filesystem symlink fixtures was not implemented because candidate-level symlink tests already exercise the lstat integration points and the finding is non-blocking under the local review standard.
- Post-fix checks: `bun scripts/check-workspace-boundaries.test.mjs` (pass), `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because the fixes affected every original specialist area.

Targeted re-review outcome for US-006 at 2026-07-16 14:25:33 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff.
- The merged blocking findings identified four remaining medium root causes: composed `.bind`/`.call`/`.apply` CommonJS loader chains; undeclared package aliases in triple-slash type and AMD directives; source files escaping semantic checks through `@ts-nocheck` or unchecked JavaScript; and `moduleSuffixes` or inherited shared exclusions redirecting/omitting domain source.
- These findings are in scope because they weaken the mechanical domain-import boundary and the requirement that real strict TypeScript checks cover workspace source.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add failing regressions for composed direct-loader chains, declared directive packages, `@ts-nocheck`/JavaScript source, `moduleSuffixes`, and shared source exclusions; implement the narrow fail-closed checks, rerun all US-006 checks, and restart the staged review loop.

## 2026-07-16 15:37:09 MST - US-006
- What was implemented
  - Resumed the modular-workspace story and closed the recorded composed CommonJS loader, directive-package, unchecked-source, `moduleSuffixes`, and inherited source-filter gaps.
  - Replaced one-level direct-loader matching with recursive invocation modeling for composed `bind`, `call`, and `apply` chains while preserving literal and non-literal rejection behavior.
  - Required package-valued type/AMD directives to use declared dependencies, rejected `@ts-nocheck` and JavaScript domain source, and prohibited shared/domain module suffixes plus inherited root source restrictions.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `disabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: composed `require.bind(...).call(...)` did not report the forbidden module)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped because both recommended agents previously advised US-006 and this resumed iteration implemented concrete, narrowly scoped prior-review findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer` planned for the complete staged workspace/configuration diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Modeled loader invocation arguments instead of matching more syntax strings because one recursive representation covers direct and composed `bind`/`call`/`apply` forms without whole-program alias analysis.
  - Rejected JavaScript in domain source rather than changing shared TypeScript configuration because the framework-independent domain package is intentionally strict TypeScript and unchecked JavaScript would evade the real `tsc` contract.
  - Rejected any explicit `moduleSuffixes` and inherited root source filters because neither is needed by the workspace and both can redirect or omit domain source outside the guard's checked model.
  - Did not add nested `AGENTS.md` guidance because the root guide already owns reusable workspace and domain-boundary conventions.
- **Learnings for future iterations:**
  - CommonJS loader analysis must model the function produced by `bind` and the argument transforms performed by `call` and `apply`; inspecting only the immediate receiver misses composed chains.
  - Type and AMD directives participate in package resolution and need the same declared-dependency checks as imports.
  - Wildcard source inclusion does not make unchecked JavaScript or `@ts-nocheck` strict; boundary validation must reject both when the package promises strict TypeScript coverage.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

Review remediation for US-006 at 2026-07-16 15:37:09 MST:
- Initial specialist review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- `code-reviewer` returned no actionable findings; the merged blocking findings identified two medium root causes: constructor and `bind.call`/`bind.apply` CommonJS loads bypassed direct-loader inspection, and config-shape checks did not prove TypeScript's effective file set covered hidden, package-named, or output-directory source.
- Added failing literal/non-literal regressions for constructor loads and `bind` invoked through `call`/`apply`, plus source-coverage regressions for an output directory under `src` and discovered files absent from TypeScript's checked set.
- Extended loader invocation modeling to constructor expressions and indirect `bind` invocation, rejected output directories under `src`, and compared recursively discovered workspace source with TypeScript's parsed effective file list.
- The low-severity real-filesystem symlink-fixture request remains non-blocking and was not implemented because existing injected candidate and path-segment tests exercise the relevant `lstat` behavior without adding test-managed filesystem mutation before US-007.
- Post-fix checks: `bun scripts/check-workspace-boundaries.test.mjs` (pass), `bun run test:workspace-boundaries` (pass), `bun run typecheck` (pass for all nine workspaces), and `git diff --check && git diff --cached --check` (pass).
- Targeted re-review: pending `qa-expert` and `security-engineer` review because the substantive fixes address their findings; `code-reviewer` is not repeated because its initial findings list was empty.

Targeted re-review outcome for US-006 at 2026-07-16 15:49:36 MST:
- `qa-expert` and `security-engineer` confirmed the constructor, immediate `bind.call`/`bind.apply`, and parsed-file-set coverage fixes but identified two remaining medium root causes.
- Nested indirect bind chains such as `require.bind.call.call(require.bind, require, null, "react")()` are not recursively reduced and still bypass direct-loader inspection, including the equivalent non-literal form.
- The source-output guard checks the raw local `outDir` string rather than resolved effective TypeScript options, so dot-segment, absolute, inherited `outDir`, and `declarationDir` paths inside workspace `src` can bypass validation before generated files exist; local `noEmit: false` also remains possible.
- These findings are in scope because they weaken the story's mechanical domain boundary and all-source typecheck guarantee.
- Per the one-targeted-re-review limit, US-006 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add failing literal/non-literal regressions for recursively composed bind method invocations; recursively model or fail closed on those direct require-derived chains; validate resolved effective `outDir`/`declarationDir` containment and `noEmit` using TypeScript's parsed options; rerun all checks and restart the staged review loop.

## 2026-07-16 15:53:37 MST - US-006
- What was implemented
  - Resumed the modular-workspace story and closed the recorded nested indirect-bind and effective TypeScript output-configuration gaps.
  - Recursively modeled `bind` functions transformed through repeated `call`/`apply` chains, preserving literal forbidden-package and non-literal loader rejection.
  - Validated TypeScript's parsed effective options so every workspace retains `noEmit` and cannot place `outDir` or `declarationDir` under its `src` tree through inherited, absolute, or dot-segment paths.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `disabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: nested `require.bind.call.call(...)` returned no violation)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped because both recommended agents previously advised US-006 and this resumed iteration implemented concrete, narrowly scoped prior-review findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer` planned for the complete staged workspace/configuration diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Separated recursive bind-function recognition from loader invocation because the same argument transformations apply at every nested `call`/`apply` layer and the smaller model closes the demonstrated chain without whole-program alias analysis.
  - Used TypeScript's parsed effective compiler options rather than raw local JSON because inheritance and path normalization determine the actual `noEmit`, `outDir`, and `declarationDir` behavior.
  - Did not run `bun install` because dependencies were already present and unchanged, while auto approval is disabled and package-manager installation requires confirmation.
  - Did not add nested `AGENTS.md` guidance because the root guide already owns reusable workspace and domain-boundary conventions.
- **Learnings for future iterations:**
  - A direct-loader guard must recursively reduce method transforms on the bind function itself, not only one immediate `bind.call` or `bind.apply` layer.
  - TypeScript output safety must inspect parsed effective options; raw relative strings miss inherited, absolute, and normalized dot-segment paths.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If review passes, implement US-007 quality tooling and Git hooks; otherwise resume the recorded blocking finding.
---

## 2026-07-16 16:15:13 MST - US-006
- What was implemented
  - Resumed the modular-workspace story and fixed the five deduplicated findings from the latest complete staged review.
  - Limited direct CommonJS loader suppression to syntax actually consumed by the modeled loader expression, including transparent wrappers and composed loader chains.
  - Required exact root workspace globs, resolved every triple-slash file reference as relative, rejected absolute domain manifest targets, and failed on recoverable parsed TypeScript configuration diagnostics.
- Files changed: `.gitignore`, `package.json`, `bun.lock`, `tsconfig.base.json`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `apps/*/package.json`, `apps/*/tsconfig.json`, `apps/*/src/index.ts`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts`, `README.md`, `AGENTS.md`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Establish the modular workspace`
- Auto approval: `disabled`
- Checks:
  - `bun scripts/check-workspace-boundaries.test.mjs` (expected fail before implementation: loader values in unrelated arguments were suppressed)
  - `bun scripts/check-workspace-boundaries.test.mjs` (pass after implementation)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run typecheck` (pass; all nine workspaces)
  - `bun pm ls --all` (pass; all nine workspaces and TypeScript 5.9.3 resolved)
  - `git diff --check && git diff --cached --check` (pass)
  - Lint/format/Vitest (not run; US-007 intentionally introduces those tools, and the executable boundary regression suite is the available affected test)
- Implementation agents:
  - Recommended: `build-engineer`, `typescript-pro`
  - Used: skipped because both recommended agents previously advised US-006 and this resumed iteration addressed concrete review findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: initial loader-suppression, workspace-glob, triple-slash path, parsed-config diagnostic, and manifest-target findings fixed; targeted re-review pending
- Decisions (why):
  - Tracked the exact loader-expression nodes consumed by the existing direct-loader model instead of restoring broad ancestor suppression, because unrelated arguments are evaluated and must remain visible to the guard.
  - Restricted root workspace globs to the approved pair instead of implementing arbitrary glob expansion because the workspace contract intentionally contains only `apps/*` and `packages/*`.
  - Used TypeScript's returned parsed diagnostics in addition to its unrecoverable callback because recoverable configuration errors must also fail the standalone boundary check.
  - Did not run `bun install` because dependencies were unchanged and auto approval is disabled.
- **Learnings for future iterations:**
  - Loader-value exemptions must follow only the modeled callee/receiver path and must not suppress loader values in unrelated argument subtrees.
  - Triple-slash `path` values are relative to their source file even when they do not begin with `.`.
  - TypeScript configuration parsing can return recoverable diagnostics through `parsedConfig.errors`; successful parsing alone does not mean the configuration is valid.
- Known issues / follow-ups:
  - Root lint, formatting, generic Vitest, and Git hooks remain intentionally unavailable until US-007.
  - Web and game-server runtime commands remain intentionally unavailable until their implementation stories.
- Next iteration start:
  - If targeted review passes, complete US-006 metadata and commit; otherwise stop on any remaining blocking finding.
---

Targeted re-review outcome for US-006 at 2026-07-16 16:17:45 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists.
- The original loader-suppression, workspace-glob, triple-slash path, parsed-config diagnostic, and manifest-target findings are resolved.
- Residual risks are limited to intentionally unavailable US-007 tooling, injected rather than filesystem-managed symlink fixtures, and out-of-scope whole-program loader alias analysis.
- All in-scope blocking findings are resolved; US-006 can be marked passing and committed.

## 2026-07-16 17:31:53 MST - US-007
- What was implemented
  - Added pinned ESLint flat-config, Prettier/Tailwind, Vitest, React Testing Library, Playwright, Husky, and lint-staged tooling with executable root scripts and a real root TypeScript check.
  - Preserved the required three-line Husky pre-commit hook exactly and added an executable-mode/content contract test.
  - Added isolated invalid fixtures that prove ESLint, Prettier's Tailwind sorting, TypeScript, Vitest, React Testing Library, and Playwright reject the expected violations while normal checks remain green.
  - Applied the initial Prettier baseline to tracked source/documentation and updated contributor guidance for the now-available checks.
- Files changed: `.gitignore`, `.husky/pre-commit`, `.prettierignore`, `AGENTS.md`, `README.md`, `bun.lock`, `eslint.config.mjs`, `lint-staged.config.mjs`, `package.json`, `playwright.config.ts`, `prettier.config.mjs`, `scripts/check-workspace-boundaries.mjs`, `scripts/check-workspace-boundaries.test.mjs`, `tasks/prd-private-realtime-bingo.md`, `tests/**`, `tsconfig.json`, `vitest.config.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-007 - Configure quality tooling and Git hooks`
- Auto approval: `enabled`
- Checks:
  - `bun run test:quality-tooling` (expected fail before implementation: script did not exist; pass after configuration with 7 checks)
  - `bun install --frozen-lockfile` (pass; locked install and Husky prepare completed with no changes)
  - `bun run test:workspace-boundaries` (pass through typecheck/test commands)
  - `bun run lint` (pass)
  - `bun run format:check` (pass after applying the initial formatting baseline)
  - `npx lint-staged` (pass against all staged story files, including ignored violation fixtures)
  - `npx tsc --noEmit` (pass; exact hook typecheck command)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; complete typecheck plus 2 Vitest files and 8 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `test-automator`
  - Used: `build-engineer`, `test-automator` for dependency/configuration shape, isolated fixture strategy, discovery boundaries, hook validation, and check recommendations
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and dependency/configuration-triggered `security-engineer`; targeted re-review by `qa-expert` and `security-engineer` planned after substantive fixes
  - Findings: fixed lint-staged ignored-fixture warnings, TSX Playwright collection by Vitest, and missing workspace-specific typechecks in the hook path; the bare `npx` download risk is requirement-bound because the story mandates the exact hook text and local pinned binaries are installed before hooks run
- Decisions (why):
  - Kept React and jsdom dependencies as root development-only test tooling because US-007 explicitly requires React Testing Library configuration while product React runtimes remain later-story work.
  - Used diagnostic-specific subprocess assertions for invalid fixtures so missing tools, collection failures, and unrelated configuration errors cannot satisfy the negative checks.
  - Kept Playwright separate from Vitest and used a runner-only smoke because no application runtime or browser journey exists yet; browser binaries were not installed outside the worktree.
  - Added a root `tsconfig.json` that checks repository source, tests, and typed configs because the mandated plain `npx tsc --noEmit` hook must not become a no-op.
  - Made `bun run test` execute the complete root/workspace typecheck before Vitest so the exact hook still enforces each workspace's narrower ambient type environment.
  - Preserved the exact required bare-`npx` hook commands despite the review's network-fallback concern because changing those bytes would violate the acceptance criterion; pinned local `lint-staged` and TypeScript binaries provide the normal resolution path.
- **Learnings for future iterations:**
  - Custom Vitest project `include` patterns must be rooted to repository-owned paths or dependency and `.opencode` tests can be collected despite normal defaults.
  - Intentional failure fixtures should assert stable diagnostic markers, not merely nonzero status, and must stay outside normal lint, format, typecheck, Vitest, and Playwright discovery.
  - A fixture TypeScript config that extends the root must explicitly override the root fixture exclusion while retaining inherited `noEmit`; otherwise it proves only a no-input configuration error or can emit generated JavaScript.
  - Playwright tests that do not request a browser fixture can validate runner wiring before product browser journeys and browser installation are available.
- Known issues / follow-ups:
  - Browser-backed Playwright journeys and their required browser binaries remain owned by later frontend/test-suite stories.
  - Product React/Next.js runtime dependencies and application scripts remain owned by later dependency-ordered stories.
- Next iteration start:
  - Implement US-008 runtime configuration defaults, overrides, validation, and startup tests without allowing domain code to read process environment directly.
---

Targeted re-review outcome for US-007 at 2026-07-16 17:38:11 MST:
- `qa-expert` and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists.
- The lint-staged fixture handling, Vitest/Playwright discovery separation, and complete workspace typecheck path are resolved.
- Residual risks are limited to intentionally deferred browser-backed journeys, developer-local hook bypassability, and the requirement-bound bare `npx` fallback behavior.
- All in-scope blocking findings are resolved; US-007 can be marked passing and committed.

## 2026-07-16 17:51:00 MST - US-008
- What was implemented
  - Added a shared immutable runtime configuration type, exact seven-value defaults, strict environment parser, bounded ranges, and cross-field timing validation in `packages/contracts`.
  - Wired both application entry points to validate `process.env` before future serving behavior and declared their contracts workspace dependency without adding placeholder runtimes.
  - Added parser and isolated subprocess startup tests covering defaults, overrides, malformed and unsafe values, inconsistent timing, deterministic aggregation, immutability, and secret-free errors; documented the accepted constraints.
- Files changed: `AGENTS.md`, `README.md`, `apps/game-server/package.json`, `apps/game-server/src/index.ts`, `apps/game-server/tsconfig.json`, `apps/web/package.json`, `apps/web/src/index.ts`, `apps/web/tsconfig.json`, `bun.lock`, `packages/contracts/src/index.ts`, `packages/contracts/src/runtime-config.ts`, `tests/runtime-config.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-008 - Define and validate runtime configuration`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/runtime-config.test.ts` (expected fail before implementation: 39 failures because the parser and startup validation did not exist; pass after review remediation with 40 tests)
  - `bun install --frozen-lockfile` (pass before and after manifest lock refresh)
  - `bun install` (pass; refreshed workspace dependency metadata in `bun.lock`)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on 2 new files; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 3 Vitest files and 48 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `security-engineer`
  - Used: `security-engineer` for strict parsing, range, timing, redaction, startup, and test guidance; skipped `backend-developer` because standard mode budgets one adviser and the module placement and startup wiring were straightforward after repository inspection
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and parsing/startup-triggered `security-engineer`; targeted re-review by `qa-expert` planned after test remediation
  - Findings: added valid inclusive-boundary coverage for the QA medium finding; deferred the PRD-status observation because Ralph requires that metadata update only after review passes
- Decisions (why):
  - Put the shared parser and type in `packages/contracts` while keeping environment access in app entry points so every process validates before future serving and domain code remains environment-independent.
  - Accepted only unsigned decimal integers and bounded capacities to the supported 25-player/100-lobby profile, with conservative timing ceilings, because permissive coercion or unbounded resource settings would make invalid configuration operationally unsafe.
  - Required disconnect grace to be shorter than reconnect, ticket lifetime not to exceed reconnect, and reconnect to be shorter than lobby retention because violating those relationships creates contradictory lifecycle behavior.
  - Aggregated issues in deterministic field order without supplied values so operators receive actionable variable/constraint errors without reflecting sensitive environment content.
- **Learnings for future iterations:**
  - Startup configuration tests should execute each entry point in a subprocess so environment mutation and module caching cannot hide validation behavior.
  - A frozen install validates the existing lock but does not refresh new workspace dependency metadata; update `bun.lock` with `bun install`, then verify it with a frozen install.
  - Node ambient types belong only in the runtime app TypeScript configs; the contracts and domain packages remain independent of Node process globals.
- Known issues / follow-ups:
  - Future web and game-server runtime stories must preserve top-level validation before opening listeners or serving requests.
- Next iteration start:
  - Implement US-009 versioned Zod contracts using the parsed runtime configuration types without moving server-authoritative rules into clients.
---

Review remediation for US-008 at 2026-07-16 17:56:36 MST:
- Initial review used `code-reviewer`, `qa-expert`, and `security-engineer` against the complete staged diff.
- The merged in-scope blocking findings contained one medium test gap: documented range limits had rejection coverage but no valid inclusive-boundary combinations.
- Added timing-compatible lower/upper boundary combinations covering capacity limits, timing ceilings, smallest mutually consistent lifecycle timings, and co-winner bounds.
- The PRD completion-state finding was discarded because Ralph explicitly requires `passes: true` only after the staged specialist review passes; `prd.json` is intentionally added during final metadata staging.
- Post-fix checks: focused runtime configuration suite (40 tests pass), `bun run test` (48 tests pass), `bun run lint` (pass), `bun run format:check` (pass), and staged/unstaged diff checks (pass).
- Targeted re-review: `qa-expert` returned an empty actionable finding list and confirmed the inclusive-boundary gap is resolved with no residual risk in the remediation scope.

## 2026-07-16 18:12:46 MST - US-009
- What was implemented
  - Added a versioned Zod v1 contract surface with strict branded primitives and schemas for lobby/round state, safe participant-session projections, cards, marks, calls, presence, timers, co-winner windows/results, authorized snapshots, mutation/realtime commands, sequenced events, acknowledgements, and safe errors.
  - Modeled manual versus automatic calling as an exact discriminated union and covered create/start/pause/resume/call-next/continue/end/mark/absence-override mutations plus heartbeat and full-snapshot resync without event-history or chat commands.
  - Added malformed-payload tests for unknown/private fields, invalid identifiers/timestamps/sequences/cards/configuration, missing command IDs, duplicate winners, unsupported commands, and internal error detail.
  - Added Zod as the contracts runtime dependency and prevented Vitest from collecting dependency source tests through Bun workspace links.
- Files changed: `AGENTS.md`, `bun.lock`, `packages/contracts/package.json`, `packages/contracts/src/index.ts`, `packages/contracts/src/v1/commands.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/index.ts`, `packages/contracts/src/v1/primitives.ts`, `tests/contracts-v1.test.ts`, `vitest.config.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-009 - Define shared contracts`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/contracts-v1.test.ts` (expected fail before implementation: 50 tests failed because the v1 schemas were absent; expected fail for the added create-round case; review-remediation regressions failed in 9 focused areas; pass after implementation with 63 tests)
  - `bun install --frozen-lockfile` (pass; 246 installs checked with no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (initial fail on one unused test binding; pass after correction)
  - `bun run format:check` (initial fail on new files; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (initial fail because custom Vitest globs collected Zod source tests through nested workspace links; pass after review remediation with 4 files and 111 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `typescript-pro`
  - Used: `api-designer`, `typescript-pro` for strict versioning, schema organization, authority/privacy boundaries, Zod branding, and malformed-payload coverage guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and parsing/dependency/privacy-triggered `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: initial created-round, timestamp ordering, round-end typing, call-position bound, snapshot consistency/privacy, private-event routing, collection bound, and safe-error findings fixed; targeted re-review pending
- Decisions (why):
  - Used strict discriminated wire objects with inferred types instead of handwritten interfaces so runtime validation and compile-time shapes cannot drift.
  - Kept state-transition rules, persistence records, actor identity, server timestamps, winner calculation, and future draw positions out of client commands because later domain/application stories own those authoritative behaviors.
  - Represented co-winner windows with authoritative timestamps rather than hard-coding 2000 ms into the wire schema because the validated runtime setting is configurable.
  - Required positive safe event sequences while leaving cross-message monotonicity to the persistence and client sequence processors that can observe multiple messages.
- **Learnings for future iterations:**
  - `z.strictObject` must be applied to every nested wire object; outer strictness does not prevent private or authoritative fields from being silently stripped inside nested projections.
  - Bun workspace links can make dependency source tests appear beneath repository `apps` and `packages` globs, so custom Vitest project exclusions must explicitly include nested `node_modules`.
  - Snapshot privacy is strongest when foreign cards, credentials, future draws, prior-round results, and active event history are absent from the schema rather than filtered after parsing.
- Known issues / follow-ups:
  - Cross-message sequence monotonicity, command authorization, state transitions, and snapshot/entity consistency remain intentional application/domain responsibilities in later stories.
- Next iteration start:
  - If review passes, implement US-010 canonical pattern documentation; otherwise resume the recorded blocking finding.
---

Review remediation for US-009 at 2026-07-16 18:22:26 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced eight medium findings across created-round representation, temporal/event semantics, privacy boundaries, resource limits, and safe error content.
- Added failing regressions before remediation for a waiting round, 75-call bounds, fractional/reversed co-winner timestamps, current-round/owner snapshot consistency, bounded collections, private event separation, ended-only round-end events, and secret-bearing error messages.
- Added a waiting round variant, millisecond-precision UTC timestamps with instant comparisons, explicit ended-round event typing, and the 75-position call limit.
- Bound participants/winners to 25, marks to 24, and calls to 75; added snapshot cross-field checks for lobby/session/self ownership, unique participants, current-round calls/cards, and own-card marks.
- Split participant-private mark/near-win events from lobby-wide active events and restricted errors to allowlisted stable codes/messages and bounded validation issues.
- Post-fix checks: focused contracts suite (63 tests pass), `bun run test:workspace-boundaries` (pass), `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (111 tests pass), `bun run test:e2e` (pass), and staged/unstaged diff checks (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because the fixes affected each original specialist area.

Targeted re-review outcome for US-009 at 2026-07-16 18:25:27 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` confirmed the original eight findings were materially addressed but identified three deduplicated medium blockers.
- `SnapshotSchema` still needs to require its round's lobby ID to match the snapshot lobby, require an active lobby's round ID to match the supplied round, and reject an active lobby with no round.
- Participant-private mark/near-win events currently use the lobby-wide event sequence, which creates apparent sequence gaps for clients that correctly do not receive those private events; the next iteration must use participant-scoped sequencing, omit sequencing where safe, or define a privacy-safe public event for every global sequence.
- `ErrorIssueSchema.path` remains a free-text disclosure channel; replace it with allowlisted public path segments or omit it from public errors and add a secret-bearing-path regression.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-009` remains `passes: false` and `prd.json` is unchanged.
- Next iteration start: add failing regressions for lobby/round snapshot consistency, private-event sequence isolation, and safe validation paths; implement the narrow fixes, rerun all checks, and restart the complete staged review loop.

## 2026-07-16 18:28:39 MST - US-009
- What was implemented
  - Resumed the shared-contract story and added regressions for snapshot lobby/round linkage, private-event sequence isolation, and secret-bearing validation paths.
  - Required every supplied round to belong to the snapshot lobby, required active lobbies to include their matching round, and retained existing own-card/call consistency checks.
  - Removed lobby-wide event sequences from participant-private mark and near-win messages so private delivery cannot create apparent gaps in the public active-lobby stream.
  - Restricted validation issue paths to an allowlist of public v1 command fields instead of accepting arbitrary free text.
- Files changed: `AGENTS.md`, `bun.lock`, `packages/contracts/package.json`, `packages/contracts/src/index.ts`, `packages/contracts/src/v1/commands.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/index.ts`, `packages/contracts/src/v1/primitives.ts`, `tests/contracts-v1.test.ts`, `vitest.config.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-009 - Define shared contracts`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/contracts-v1.test.ts` (expected fail before remediation: 5 failures for the three recorded review blockers; pass after implementation with 66 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on the amended contract test; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 4 Vitest files and 114 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-009 and the remaining work directly addressed concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and `security-engineer` planned for the complete restaged contract diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Omitted sequences from participant-private events rather than allocating a second sequence domain because these messages are ephemeral authorized projections and do not participate in the lobby-wide continuity contract.
  - Used an explicit public error-path allowlist because free-form strings can disclose internal field names or values, while structured path expansion can remain versioned with future public contract fields.
  - Enforced active-lobby linkage in snapshot refinement rather than changing the individual lobby or round schemas because each state remains valid independently and consistency is a cross-object snapshot invariant.
- **Learnings for future iterations:**
  - Lobby-wide monotonic sequences must appear only on messages delivered to every authorized stream consumer; participant-private projections need an isolated ordering model or no sequence.
  - Safe error schemas must constrain field paths as well as codes and messages, because arbitrary path text is another disclosure channel.
- Known issues / follow-ups:
  - Cross-message sequence application, command authorization, transitions, and persistence remain owned by later stories.
- Next iteration start:
  - If review passes, implement US-010 canonical pattern documentation; otherwise resume the recorded blocking finding.
---

Review remediation for US-009 at 2026-07-16 18:36:04 MST:
- The complete staged review used `code-reviewer`, `qa-expert`, and `security-engineer` and produced three deduplicated medium blockers: private-only acknowledgements still required a lobby-wide sequence, snapshot identity projections/references could contradict the participant roster, and error codes/messages could be paired inconsistently.
- Added failing regressions first for mismatched participant presence, self/roster divergence, foreign host/timer/winner references, a private-only acknowledgement, and mismatched top-level and validation-issue error pairs; the focused suite failed in all 7 expected areas.
- Made acknowledgement event sequences nullable for private-only committed results, while sequenced lobby events and normal acknowledgements still require positive branded sequences.
- Bound presence to its participant summary; required the self projection to match its roster entry; and constrained host, disconnect-timer, and winner participant references to the authorized snapshot roster.
- Coupled every stable public error code to its single safe message and every validation issue code to its supported safe-message set.
- Post-fix checks: focused contracts suite (72 tests pass), `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (120 tests pass), `bun run test:e2e` (pass), and staged/unstaged diff checks (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because the substantive fixes address findings from all three specialists.

Targeted re-review outcome for US-009 at 2026-07-16 18:38:47 MST:
- `code-reviewer` returned no actionable findings; `qa-expert` and `security-engineer` identified two deduplicated medium blockers in the revised complete staged diff.
- `CommandAckSchema` currently permits `eventSequence: null` for every command. The next iteration must make acknowledgement scope explicit so participant-private results require no lobby sequence while lobby-event acknowledgements require a positive branded sequence, with malformed coverage for a public acknowledgement using null.
- Snapshot roster references need stronger semantic consistency: the designated lobby host must be the only participant with role `host`; a disconnect-grace timer must target matching grace presence generation/deadline; and winners must be round-eligible playing participants. Add failing malformed-payload regressions before tightening these refinements.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-009` remains `passes: false` and `prd.json` remains unchanged.
- Next iteration start: add the acknowledgement-scope and roster-semantic regressions, implement the narrow schema refinements, rerun all US-009 checks, and restart the complete staged review loop.

## 2026-07-16 18:42:00 MST - US-009
- What was implemented
  - Resumed the shared-contract story and added regressions for explicit acknowledgement sequencing scope, unique host designation, disconnect-grace timer consistency, and winner round eligibility.
  - Split acknowledgements into strict `active-lobby` and `participant-private` variants so public committed results require a positive lobby sequence while private-only results require `null`.
  - Tightened snapshot refinement so the designated host is the only host, disconnect timers exactly match the target participant's grace generation/deadline, and winners are playing participants in the current round.
  - Completed initial-review remediation by rejecting expired private snapshots, incompatible timer/round combinations, card/eligibility contradictions, incoherent call/mark histories, and stale co-winner call references.
- Files changed: `AGENTS.md`, `bun.lock`, `packages/contracts/package.json`, `packages/contracts/src/index.ts`, `packages/contracts/src/v1/commands.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/index.ts`, `packages/contracts/src/v1/primitives.ts`, `tests/contracts-v1.test.ts`, `vitest.config.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-009 - Define shared contracts`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/contracts-v1.test.ts` (expected fail before implementation: 5 failures in the recorded acknowledgement and roster semantics; expected fail during review remediation: 10 failures across 4 deduplicated blockers; pass after implementation with 85 tests)
  - `bun install --frozen-lockfile` (pass; 246 installs checked with no changes)
  - `bun run lint` (pass)
  - `bun run format:check` (initial remediation fail on the amended contract test; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass after remediation; 4 Vitest files and 133 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `typescript-pro`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-009 and the remaining work directly addressed concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and parsing/privacy-triggered `security-engineer`; targeted re-review by the same specialists
  - Findings: initial timer/state, card eligibility, call/mark coherence, and expired-snapshot privacy blockers fixed; targeted re-review returned no actionable findings
- Decisions (why):
  - Added an explicit acknowledgement `scope` discriminant instead of inferring delivery semantics from nullable data because callers and consumers need an unambiguous contract for sequence continuity.
  - Compared disconnect timers to the participant's complete grace projection because roster membership alone cannot prove the timer is current or targets a participant actually in grace.
  - Used `roundEligibility: "playing"` as the winner eligibility contract because waiting participants are present in snapshots but cannot win the active round.
  - Enforced snapshot collection coherence at the wire boundary because a complete authoritative snapshot must not represent duplicate calls, fabricated daubs, stale winner triggers, or incompatible countdown state.
  - Rejected expired lobbies from authorized snapshots rather than defining a minimal expiry payload because restoration of expired lobby data is explicitly out of scope.
- **Learnings for future iterations:**
  - A nullable sequence alone cannot express whether an acknowledgement is public or private; delivery scope should be a wire-level discriminant.
  - Snapshot references must be checked semantically, not only for ID membership: role uniqueness, timer generation/deadline, and round eligibility are cross-object invariants.
- Known issues / follow-ups:
  - Cross-message sequence application, authorization, state transitions, and persistence remain owned by later stories.
- Next iteration start:
  - Implement US-010 canonical pattern documentation from all four supplied PDFs.
---

Review remediation for US-009 at 2026-07-16 18:51:03 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four deduplicated medium blockers: timer/round contradictions, card/eligibility contradictions, incoherent call/mark histories with stale co-winner references, and expired snapshots retaining private state.
- Added failing malformed-payload regressions before implementation; the focused suite failed in all 10 expected areas.
- Required automatic timers to target active automatic rounds and co-winner timers to match the current round window; required co-winner windows/results to reference the latest contiguous unique call history.
- Required playing participants to have their current-round card and waiting participants to have no card/marks; required marks to be unique, on-card, and called.
- Rejected authorized snapshots for expired lobbies and isolated the participant collection-bound fixture so its valid 25-person baseline now passes before the 26th participant fails.
- Post-fix checks: focused contracts suite (85 tests pass), `bun run lint` (pass), `bun run format:check` (pass after formatting), `bun run typecheck` (pass), `bun run test` (133 tests pass), `bun run test:e2e` (pass), and diff checks (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because the substantive fixes address findings from all three specialists.

Targeted re-review outcome for US-009 at 2026-07-16 18:52:00 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists with no residual risks.
- The timer/round, eligibility/card, call/mark/co-winner, expired-snapshot privacy, and participant-bound fixture findings are resolved.
- All in-scope blocking findings are resolved; US-009 can be marked passing and committed.

## 2026-07-16 19:09:50 MST - US-010
- What was implemented
  - Added the human-reviewable canonical pattern catalog with notation, center semantics, exact/flexible/alias classification, no-transform rules, source references, aliases, duplicates, and audit totals.
  - Transcribed all 82 diagrams from the four supplied PDFs: 25 shapes, 25 letters, 20 numbers, and 12 Christmas patterns.
  - Preserved the approved Q, W, and 10-19 masks exactly, mapped both Two Lines diagrams to one flexible rule, and documented Full House only as the source alias for Blackout.
  - Added parser-based completeness and mask-format tests covering every source label/reference, classification totals, 5x5 syntax, approved masks, alias behavior, duplicate-mask identities, and future runtime parity guidance.
- Files changed: `docs/bingo-pattern-catalog.md`, `tests/pattern-catalog-doc.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-010 - Document the canonical pattern catalog`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/pattern-catalog-doc.test.ts` (expected fail before documentation: 11 failures because the catalog was absent; pass after implementation with 12 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 5 Vitest files and 145 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: `data-researcher` for an independent all-PDF inventory, mask transcription, duplicate analysis, and test recommendations because this was complex source-data review within the standard-mode budget
- Specialist review:
  - Agents used: `code-reviewer` and `qa-expert` for the complete staged test-sensitive diff
  - Findings: no blocking findings; both specialists raised the same low bookkeeping observation about the post-review `prd.json` update, which the required Ralph sequence resolves before commit
- Decisions (why):
  - Used one inventory table per PDF with reading-order `p1/dNN` references because all PDFs are single-page grids and the convention makes completeness mechanically auditable.
  - Recorded source-display masks even for flexible examples and the Full House alias so every source diagram is reviewable without incorrectly turning those diagrams into separate exact runtime rules.
  - Kept runtime IDs and implementation out of this story because US-013 owns `packages/patterns/src/catalog.ts`; the documentation states that runtime data must later become canonical and generate this representation.
  - Treated the catalog format as parsed documentation behavior and wrote the completeness test first because the acceptance criteria explicitly require documentation completeness and mask-format tests.
  - Did not update `AGENTS.md` because its existing pattern guidance already prohibits second mask sources and implicit source-mask transformations.
- **Learnings for future iterations:**
  - The four source PDFs contain 82 diagrams total: 79 exact masks, two Two Lines flexible examples, and one Full House alias for Blackout.
  - Shape Outside Edge/Letter O, Shape X/Letter X, Shape Cross/Christmas Cross, and Shape Checkerboard/Christmas Snow have duplicate masks but require separate category-specific identities.
  - The letters PDF ends at Y, and the number PDF heading says “Letters + numbers” despite containing only 0-19.
- Known issues / follow-ups:
  - US-013 must make runtime canonical data the source for generated documentation parity; later catalog stories add stable IDs, previews, golden fixtures, and source review records.
- Next iteration start:
  - Implement US-011 valid card generation with injectable cryptographic randomness and unit/property-based invariant tests.
---

Review outcome for US-010 at 2026-07-16 19:09:50 MST:
- `code-reviewer` and `qa-expert` reviewed the complete staged catalog, test, and progress diff against the four source PDFs.
- Both specialists returned only the same low-severity observation that `progress.txt` listed `prd.json` before the required post-review metadata update; the finding is resolved by the mandated final staging sequence and requires no substantive change or re-review.
- No in-scope critical, high, or medium findings remain. Residual risk is limited to future runtime/documentation parity, which US-013 explicitly owns.

## 2026-07-16 19:21:46 MST - US-011
- What was implemented
  - Added framework-independent 75-ball card generation with B/I/N/G/O column ranges, unique column values, a free center, and duplicate rejection across every card generated for a round.
  - Added injected cryptographic random-byte input with rejection-sampled bounded selection so deterministic tests do not require domain access to runtime crypto APIs and production selection avoids modulo bias.
  - Added free-cell satisfaction behavior plus unit and fast-check property tests covering deterministic generation, forced duplicate candidates, card invariants, round uniqueness, bounded random-source failure, and the configured 25-participant bound.
- Files changed: `AGENTS.md`, `package.json`, `bun.lock`, `packages/domain/src/bingo-card.ts`, `packages/domain/src/index.ts`, `tests/bingo-card.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-011 - Generate valid Bingo cards`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/bingo-card.test.ts` (expected fail before implementation: 5 failures because the domain card API did not exist; expected fail during review remediation for unbounded rejection sampling; pass after remediation with 7 tests)
  - `bun install --frozen-lockfile` (pass; 248 installs checked with no changes)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on 2 new files; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass after review remediation; 6 Vitest files and 152 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the card rules and existing domain boundaries were clear and standard mode did not require an implementation adviser
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and randomness-triggered `security-engineer`; targeted re-review by the same specialists
  - Findings: initial forced-collision coverage and unbounded rejection-sampling findings fixed; targeted re-review returned no actionable findings
- Decisions (why):
  - Generated all cards for a round through one API so uniqueness is enforced at the domain boundary that can observe the complete card set rather than delegated to callers.
  - Required injected random bytes instead of reading global or Node crypto inside the domain package, preserving deterministic tests and framework-independent runtime boundaries.
  - Used Fisher-Yates shuffles with rejection-sampled byte selection because direct modulo reduction would bias bounded choices when 256 is not divisible by the requested range.
  - Added fast-check as an exact root development dependency because the acceptance criteria explicitly require property-based invariant tests.
- **Learnings for future iterations:**
  - A 75-ball card is stored row-major to match the v1 card contract; `index % 5` identifies B/I/N/G/O range and index 12 is the free center.
  - Round-level generation must own duplicate detection; independently generated cards cannot guarantee uniqueness within the round.
  - The same injected byte-source and rejection-sampling convention should be reused for the draw-order shuffle in US-012.
- Known issues / follow-ups:
  - Runtime adapters that supply secure bytes land in later application stories; the domain intentionally requires the source as an argument.
- Next iteration start:
  - Implement US-012 draw-order generation using the established injected byte-source convention without exposing future positions.
---

Review remediation for US-011 at 2026-07-16 19:25:50 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced two deduplicated medium blockers: generated property inputs did not force the duplicate-card path, and rejection sampling could loop forever for a pathological injected source.
- Added a repeating deterministic source that proves duplicate candidates never escape and the generator fails instead of returning a non-unique round.
- Added a failing regression for repeated rejected bytes, then bounded unbiased index sampling to 256 attempts with an explicit failure.
- Post-fix checks: focused card suite (7 tests pass), `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (152 tests pass), `bun run test:e2e` (pass), and staged/unstaged diff checks (pass).
- Targeted re-review: `code-reviewer`, `qa-expert`, and `security-engineer` returned empty actionable finding lists with no residual risks; all in-scope blockers are resolved.

## 2026-07-16 19:34:38 MST - US-012
- What was implemented
  - Added a server-internal 75-ball draw-order generator that shuffles every number exactly once using injected cryptographic random bytes and bounded rejection sampling.
  - Added a one-step draw transition that returns only the newly committed ball, 1-based position, and committed count, with a stable `DRAW_ORDER_EXHAUSTED` result after position 75.
  - Extracted the existing unbiased Fisher-Yates implementation into a shared domain utility and retained Bingo-card behavior through regression coverage.
- Files changed: `packages/domain/src/bingo-card.ts`, `packages/domain/src/draw-order.ts`, `packages/domain/src/index.ts`, `packages/domain/src/random.ts`, `tests/draw-order.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-012 - Generate the draw order`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/draw-order.test.ts` (expected fail before implementation: 8 failures because the draw-order API did not exist)
  - `bunx vitest run tests/draw-order.test.ts tests/bingo-card.test.ts` (pass after implementation with 15 tests; pass after review remediation with 16 tests)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on the new test; pass after formatting)
  - `bun run typecheck` (initial fail because ES2023 `toSorted` was outside the ES2022 target; pass after using copied-array sorting)
  - `bun run test` (pass before review with 160 tests; pass after remediation with 7 Vitest files and 161 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `security-engineer`
  - Used: `security-engineer` for randomness, privacy-boundary, API, and test guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and randomness-triggered `security-engineer`; targeted re-review by `qa-expert` planned after the test remediation
  - Findings: added explicit first-boundary rejection coverage for the initial 75-way sample; security returned no actionable findings, and the low PRD bookkeeping note is resolved by the required post-review staging sequence
- Decisions (why):
  - Generated the complete permutation once and advanced it by an authoritative committed count because later persistence stories will transact the cursor and committed call without consuming new randomness per call.
  - Returned no draw order or remaining-ball collection from the transition because future positions are server-private and must not enter client contracts, acknowledgements, or logs.
  - Shared one bounded rejection-sampling shuffle between cards and draw order because duplicating cryptographic selection logic would create avoidable drift and bias risk.
  - Kept database commits out of this story because durable models and serializable idempotent transactions belong to US-021 and US-022; this domain transition supplies exactly one next position for those later adapters.
- **Learnings for future iterations:**
  - Persist the generated permutation as private server state and expose only committed call records; never serialize the remaining order through shared client contracts or structured logs.
  - Later transactional callers should pass the persisted committed count to `commitNextDrawPosition` and atomically store its single returned position with the call and event.
- Known issues / follow-ups:
  - Runtime CSPRNG adapters, private draw-order persistence, and transactional call commits land in later dependency-ordered stories.
- Next iteration start:
  - If review passes, implement US-013 canonical pattern semantics; otherwise resume the recorded blocking finding.
---

Review remediation for US-012 at 2026-07-16 19:38:43 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced one blocking medium finding: the rejection test did not pin byte 225 as the first rejected value for a 75-way sample, so a biased boundary implementation could evade the suite.
- Added deterministic coverage that supplies 225 followed by 224, proves the first byte is rejected through the extra random-byte request, and verifies the accepted boundary leaves the first shuffle position correct.
- The security review returned no actionable findings. The code review's low note that `prd.json` was not yet staged follows Ralph's required post-review metadata sequence and needs no pre-review change.
- Post-fix checks: focused draw/card suite (16 tests pass), `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (161 tests pass), `bun run test:e2e` (pass), and staged/unstaged diff checks (pass).
- Targeted re-review: `qa-expert` confirmed the boundary finding is resolved and returned no actionable findings; residual runtime CSPRNG and persistence work remains explicitly deferred to later stories.

## 2026-07-16 19:46:45 MST - US-013
- What was implemented
  - Added a strict, versioned runtime pattern schema and canonical core entries for One Line, Two Lines, and Blackout with stable IDs, categories, source references, and the Full House source alias.
  - Generated the 12 row/column/diagonal masks and all 66 distinct two-line unions, while preserving one untransformed mask for exact patterns and one all-cell mask for Blackout.
  - Added matching that tolerates extra daubs, always satisfies the free center, and accepts no implicit rotation, reflection, or translation.
  - Added an exact generated documentation section and parity test so runtime core metadata and documentation cannot diverge.
- Files changed: `README.md`, `bun.lock`, `docs/bingo-pattern-catalog.md`, `packages/patterns/package.json`, `packages/patterns/src/catalog.ts`, `packages/patterns/src/documentation.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/matcher.ts`, `tests/pattern-catalog-doc.test.ts`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-013 - Implement canonical pattern semantics`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts` (expected fail before implementation: 10 failures because the runtime APIs and generated section did not exist; expected fail during review remediation: 2 failures for nested mutability and mask-content-insensitive documentation; pass after remediation with 25 tests)
  - `bun install --frozen-lockfile` (pass; 248 installs checked with no changes)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on new files; pass after formatting and generated-block stabilization)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass after review remediation; 8 Vitest files and 174 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the core pattern rules and dependency-ordered scope were clear and standard mode did not require an implementation adviser
- Specialist review:
  - Agents used: `code-reviewer` and `qa-expert`; targeted re-review by both specialists
  - Findings: initial nested immutability, mask-content parity, complete flexible-arrangement coverage, and exact-transform coverage findings fixed; targeted re-review returned no actionable findings
- Decisions (why):
  - Limited the runtime catalog to One Line, Two Lines, and Blackout because US-014 and US-016 through US-018 explicitly own the 79 source-specific exact entries; the generic exact matcher is ready for those additions.
  - Represented flexible rules as all accepted required-cell masks because the same canonical data can drive matching, later progress calculations, and previews without separate rule-specific transformations.
  - Kept Full House only as Blackout source metadata rather than a selectable entry because the documentation and PRD define it solely as an alias.
  - Added Zod directly to `packages/patterns` because catalog definitions are runtime-parsed at their canonical boundary and the workspace does not rely on undeclared transitive dependencies.
- **Learnings for future iterations:**
  - A 5x5 Bingo board has exactly 12 valid lines: five rows, five columns, and two corner-to-corner diagonals; choosing any two distinct lines yields 66 canonical Two Lines masks.
  - Generated Markdown that is compared byte-for-byte must be isolated from Prettier, or formatting can create parity failures without changing canonical data.
  - Later exact catalog entries should use `PatternDefinitionSchema` and one source-orientation mask; matcher behavior already tolerates additional daubs and satisfies the center.
- Known issues / follow-ups:
  - Shape, letter, number, and Christmas exact entries remain intentionally deferred to their dependency-ordered catalog stories.
- Next iteration start:
  - If review passes, implement US-014 shape pattern encoding and cell-by-cell source review records; otherwise resume the recorded blocking finding.
---

Review remediation for US-013 at 2026-07-16 19:54:07 MST:
- The initial `code-reviewer` and `qa-expert` pass produced four deduplicated medium blockers: nested catalog values remained mutable, documentation parity serialized only mask counts, tests did not independently prove every valid flexible arrangement, and exact no-transform coverage checked only one reflection.
- Added failing regressions for nested mutation and mask-content-insensitive documentation, then recursively froze parsed catalog values and exposed deeply readonly definitions.
- Added a deterministic 64-bit mask-content digest to generated documentation, independently asserted all 12 line masks and 66 line-pair unions, exercised every flexible arrangement with the center omitted, and added reflected, rotated, and translated exact-mask negatives.
- Post-fix checks: focused pattern/catalog suite (25 tests pass), `bun install --frozen-lockfile` (pass), `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (174 tests pass), and `bun run test:e2e` (pass).
- Targeted re-review: `code-reviewer` and `qa-expert` returned empty actionable finding lists; residual work is limited to the source-specific exact entries intentionally assigned to later stories.

## 2026-07-16 20:03:39 MST - US-014
- What was implemented
  - Added all 22 exact shape entries with category-specific stable IDs, source references, versioned exact mode, and untransformed canonical masks.
  - Reused the single existing Two Lines flexible rule for both source examples and the single Blackout runtime entry for the Full House source alias, yielding 24 source-derived shape selections without duplicate entries.
  - Added 25 cell-by-cell source review records and parity coverage that maps each PDF diagram to its runtime ID/disposition and compares exact records to canonical runtime masks.
- Files changed: `packages/patterns/src/catalog.ts`, `docs/bingo-pattern-catalog.md`, `tests/patterns.test.ts`, `tests/pattern-catalog-doc.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-014 - Encode and review shape patterns`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts` (expected fail before implementation: 3 failures for missing exact shapes, selection count, and source review records; pass after implementation with 28 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 8 Vitest files and 177 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the canonical source inventory, runtime schema, and exact-entry conventions made this a straightforward standard catalog change
- Specialist review:
  - Agents used: `code-reviewer` and `qa-expert` for the complete staged test-sensitive diff
  - Findings: none; both specialists returned empty actionable finding lists
- Decisions (why):
  - Used `shape-<name>` IDs for exact entries while retaining `standard-two-lines` and `standard-blackout` because flexible rules and aliases must remain single cross-category runtime concepts.
  - Kept source masks only in canonical runtime data and the existing source inventory; review records contain mapping, disposition, and cell counts rather than creating another mask source.
  - Added shape rows to the existing generated documentation block so metadata and mask digests continue to fail parity checks when runtime data changes.
  - Did not update `AGENTS.md` because its existing canonical-mask, no-transform, and no-second-source guidance already covers this implementation.
- **Learnings for future iterations:**
  - The shapes PDF has 25 diagrams that map to 24 selectable patterns: 22 exact shapes, one Two Lines flexible rule represented by two examples, and one Blackout entry represented by the Full House alias.
  - Source review records can prove cell review without duplicating masks by linking each diagram to canonical runtime data and parity-checking exact mappings against the source inventory.
- Known issues / follow-ups:
  - Shape thumbnails and golden fixtures are intentionally deferred to US-015.
- Next iteration start:
  - Implement US-015 generated shape previews and golden fixtures, then verify the rendered output in a browser.
---

Review outcome for US-014 at 2026-07-16 20:09:15 MST:
- `code-reviewer` and `qa-expert` reviewed the complete staged catalog, documentation, tests, and progress diff and returned empty actionable finding lists.
- Residual risk is limited to automated PDF-image comparison, generated previews, and golden fixtures explicitly assigned to US-015.
- No in-scope critical, high, or medium findings remain; US-014 can be marked passing and committed.

## 2026-07-16 23:34:03 MST - US-015
- What was implemented
  - Added a catalog-derived shape preview model covering all 24 runtime shape selections and all 25 source diagrams, including both canonical Two Lines examples.
  - Added a responsive standalone HTML gallery showing each pattern's name, stable ID, mode, source, classification, raw mask, and accessible visual thumbnail.
  - Added a reviewed JSON golden fixture and DOM tests that detect missing, extra, stale, malformed, or incorrectly rendered shape previews.
- Files changed: `packages/patterns/src/catalog.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/previews.ts`, `docs/shape-pattern-previews.html`, `tests/fixtures/patterns/shape-previews.v1.json`, `tests/pattern-shape-previews.test.tsx`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-015 - Preview and golden-test shapes`
- Auto approval: `disabled`
- Checks:
  - `bunx vitest run tests/pattern-shape-previews.test.tsx --project dom` (expected fail before implementation: 4 failures because the preview API did not exist; pass after implementation with 4 tests)
  - `bunx vitest run tests/pattern-shape-previews.test.tsx tests/patterns.test.ts tests/pattern-catalog-doc.test.ts` (pass; 3 files and 32 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass after review remediation; 9 Vitest files and 182 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
  - Browser verification with `dev-browser` at desktop and 390px touch viewport (pass; 24 entries, 25 thumbnails, no horizontal overflow, no console warnings/errors)
  - Lighthouse mobile snapshot (pass; accessibility 100 and best practices 100)
- Implementation agents:
  - Recommended: `frontend-developer`, `test-automator`
  - Used: `frontend-developer`, `test-automator` for framework-independent rendering, accessibility, source-example modeling, golden-fixture strategy, and focused test guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher`; targeted re-review by specialists relevant to the fixed findings planned
  - Findings: fixed the blocking mode-label test weakness plus specific source-invariant/test and mobile mask-overflow findings before targeted re-review
- Decisions (why):
  - Used a standalone semantic HTML gallery instead of adding React or Next.js runtime behavior because those application runtimes belong to later dependency-ordered stories.
  - Stored the two reviewed Two Lines source examples with the canonical runtime rule and validated each mask against its 66 accepted arrangements so renderer code does not become a second mask source.
  - Golden-tested the normalized preview model rather than browser pixels because metadata, order, classifications, sources, and masks remain deterministic across environments while screenshots do not.
  - Used visible ASCII mask notation plus accessible image labels so required cells are not communicated by color alone.
  - Did not update `AGENTS.md` because its existing canonical-data and generated-preview guidance already covers the reusable implementation rule.
- **Learnings for future iterations:**
  - The 24 shape selections produce 25 thumbnails because the single Two Lines runtime rule has two source-diagram examples.
  - A standalone generated review page enables browser verification before the Next.js runtime story without introducing placeholder application behavior.
  - Prettier can preserve a byte-for-byte generated HTML root when `<!-- prettier-ignore -->` immediately precedes the `<html>` node.
- Known issues / follow-ups:
  - Letter, number, and Christmas preview models/goldens remain intentionally assigned to US-016 through US-018.
- Next iteration start:
  - If review passes, implement US-016 letter encoding, source-linked previews, and golden fixtures; otherwise resume the recorded blocking finding.
---

Review remediation for US-015 at 2026-07-16 23:42:27 MST:
- The initial `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher` pass produced one blocking medium finding: exact-mode cards could lose their rendered mode label while the test still passed on the thumbnail's `exact` classification text.
- Added a dedicated generated `data-mode` contract and a failing assertion that distinguishes the card's mode metadata from classification content.
- Also fixed three specific low-cost findings directly tied to the story: standalone source schemas now reject examples without a source file, focused negative tests cover mode/reference/mask example invariants, and intrinsic mask sizing prevents glyph overflow inside the thumbnail border.
- Focused remediation tests failed in the two expected areas before implementation and pass afterward with 18 tests.
- Post-fix checks: `bun run lint` (pass), `bun run format:check` (pass), `bun run typecheck` (pass), `bun run test` (182 tests pass), staged/unstaged diff checks (pass), and 390px browser overflow/console verification (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `ui-designer` review because their findings received substantive fixes; `ux-researcher` is not repeated because its initial findings list was empty.

Targeted re-review outcome for US-015 at 2026-07-16 23:48:00 MST:
- `code-reviewer` and `ui-designer` returned empty actionable finding lists and confirmed the standalone source invariant and mobile mask-overflow fixes.
- `qa-expert` identified one remaining medium blocker: the new `data-mode` assertion proves machine metadata but still does not independently prove the visible mode eyebrow because exact thumbnail classifications duplicate the same text.
- `qa-expert` also identified one low test-isolation issue: the non-Two-Lines example case can fail because its second example mask is outside the reduced exact mask set rather than because examples are prohibited for that mode.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-015` remains `passes: false` and `prd.json` remains unchanged.
- Next iteration start: add a failing assertion against the dedicated visible mode element and isolate the non-Two-Lines schema fixture so only the mode rule can reject it, rerun all checks, and restart the complete staged review loop.

## 2026-07-16 23:50:21 MST - US-015
- What was implemented
  - Resumed the shape-preview story by adding a dedicated visible mode-label contract and exact-text DOM coverage for every gallery card.
  - Isolated the non-Two-Lines source-example fixture so its only schema issue is the unsupported mode, eliminating the prior false-positive path.
  - Regenerated the committed standalone gallery with the visible mode marker while preserving responsive and generator-parity behavior.
- Files changed: `packages/patterns/src/catalog.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/previews.ts`, `docs/shape-pattern-previews.html`, `tests/fixtures/patterns/shape-previews.v1.json`, `tests/pattern-shape-previews.test.tsx`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-015 - Preview and golden-test shapes`
- Auto approval: `disabled`
- Checks:
  - `bunx vitest run tests/pattern-shape-previews.test.tsx tests/patterns.test.ts` (expected fail before implementation: visible mode marker was absent; pass after implementation with 18 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 9 Vitest files and 182 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check && git diff --cached --check` (pass)
  - Browser verification with `dev-browser` at 390px touch and 1440px desktop (pass; 24 visible mode labels, 24 entries, 25 thumbnails, no horizontal overflow, no console warnings/errors)
- Implementation agents:
  - Recommended: `frontend-developer`, `test-automator`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-015 and the remaining work directly addressed concrete prior specialist findings
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher` planned for the complete restaged test-sensitive frontend diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Added a dedicated visible-element marker instead of relying on aggregate card text or machine-only metadata because exact thumbnail classification text can duplicate the mode value.
  - Reduced the exact-pattern fixture to one reference, one matching example, and one accepted mask so no unrelated source-reference or mask issue can satisfy the rejection assertion.
  - Reused the existing standalone gallery and canonical preview model rather than introducing application runtime behavior owned by later stories.
- **Learnings for future iterations:**
  - Visible metadata tests should select the dedicated rendered element and compare its complete text; aggregate text and data attributes cannot prove that users see the label.
  - Negative schema fixtures should assert the exact issue set and satisfy every invariant except the one under test.
- Known issues / follow-ups:
  - Letter, number, and Christmas preview models/goldens remain intentionally assigned to US-016 through US-018.
- Next iteration start:
  - If review passes, implement US-016 letter encoding, source-linked previews, and golden fixtures; otherwise resume the recorded blocking finding.
---

Review outcome for US-015 at 2026-07-16 23:50:21 MST:
- `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher` reviewed the complete restaged diff and returned empty actionable finding lists.
- The visible mode-label coverage and isolated non-Two-Lines schema fixture resolve the prior targeted-review findings.
- Residual risk is limited to manual rather than image-based source-PDF comparison and browser verification outside Chromium; neither is required by this story.
- No in-scope critical, high, or medium findings remain. US-015 can be marked passing and committed.

## 2026-07-17 00:02:41 MST - US-016
- What was implemented
  - Encoded canonical exact letter patterns A through Y with stable `letter-*` IDs, source references, and the documented masks, including confirmed Q and W masks.
  - Preserved Letter O and Letter X as independent runtime selections despite masks matching Shape Outside Edge and Shape X, and documented/rendered that the source contains no Z.
  - Added source-linked letter preview modeling, a responsive standalone HTML gallery, a reviewed JSON golden, catalog/documentation parity, and focused DOM/catalog coverage.
- Files changed: `packages/patterns/src/catalog.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/previews.ts`, `docs/bingo-pattern-catalog.md`, `docs/letter-pattern-previews.html`, `tests/fixtures/patterns/letter-previews.v1.json`, `tests/pattern-catalog-doc.test.ts`, `tests/pattern-letter-previews.test.tsx`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-016 - Encode, preview, and test letters`
- Auto approval: `disabled`
- Checks:
  - `bunx vitest run tests/patterns.test.ts tests/pattern-letter-previews.test.tsx` (expected fail before implementation: six failures for missing letter entries and preview API)
  - `bunx vitest run tests/pattern-catalog-doc.test.ts` (expected fail during review remediation: no letter source-review records existed)
  - `bunx vitest run tests/pattern-letter-previews.test.tsx tests/pattern-shape-previews.test.tsx tests/patterns.test.ts tests/pattern-catalog-doc.test.ts` (pass after remediation; 4 files and 40 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass after remediation; 10 Vitest files and 189 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
  - Browser verification with `dev-browser` at 390px touch and 1440px desktop (pass; 25 entries, 25 thumbnails, no Z entry, no horizontal overflow, no console warnings/errors)
  - Lighthouse mobile snapshot (pass; accessibility 100 and best practices 100)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the prior shape implementation established a clear category pattern and standard mode budgets at most one optional adviser
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher`; targeted re-review by `code-reviewer` and `qa-expert` planned after substantive documentation/test fixes
  - Findings: fixed missing letter source-review parity and the stale catalog introduction before targeted re-review
- Decisions (why):
  - Reused one source-filtered preview model and shared semantic renderer while retaining category-specific public functions because number and Christmas stories need the same presentation without coupling their committed outputs.
  - Kept exact letter masks in the canonical runtime catalog and generated documentation/previews from that source so visual artifacts cannot become a second mask authority.
  - Used stable `letter-*` IDs rather than deduplicating identical masks because category identity is part of the product contract.
  - Kept the standalone gallery framework-independent because application runtime/UI foundation stories remain dependency ordered for later work.
- **Learnings for future iterations:**
  - Number and Christmas preview stories can add category-specific wrappers over the shared source-preview renderer without changing the existing shape or letter outputs.
  - Category identity must be preserved even when masks match across sources; masks are gameplay geometry, not catalog identity.
  - Generated core documentation must be refreshed whenever runtime catalog entries change or the parity test will fail.
- Known issues / follow-ups:
  - Number and Christmas catalog entries, goldens, and preview pages remain intentionally assigned to US-017 and US-018.
  - Local browser screenshot and Lighthouse report artifacts are untracked and excluded from the candidate story because they are verification outputs rather than maintained product artifacts.
- Next iteration start:
  - If review passes, implement US-017 number encoding, source-linked previews, and golden fixtures; otherwise resume any recorded blocking finding.
---

Review remediation for US-016 at 2026-07-17 00:10:21 MST:
- The initial `code-reviewer`, `qa-expert`, `ui-designer`, and `ux-researcher` pass produced one blocking medium finding: letter runtime masks were not directly parity-tested against completed source review records for all 25 PDF diagrams.
- Added a failing documentation test first, then added A-Y cell-review records that map every source reference/name to its category-specific runtime ID and assert source mask, source reference, category, and runtime mask parity.
- Fixed the QA review's specific low stale-documentation finding by updating the catalog introduction to state that only number and Christmas runtime entries remain.
- Post-fix checks: focused 40 tests, workspace boundaries, lint, formatting, typecheck, all 189 Vitest tests, Playwright smoke, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer` and `qa-expert` review because their findings received substantive documentation/test fixes; UI/UX specialists are not repeated because their initial findings lists were empty and rendered output did not change.

Targeted re-review outcome for US-016 at 2026-07-17 00:10:21 MST:
- `code-reviewer` and `qa-expert` reviewed the revised complete staged diff and returned empty actionable finding lists.
- Both specialists confirmed that the A-Y review records and direct source-inventory-to-runtime assertions resolve the original parity gap without introducing brittle or tautological coverage.
- Residual risk is limited to manual rather than automated image-level comparison against the source PDF and browser verification outside Chromium; neither is required by this story.
- No in-scope critical, high, or medium findings remain. US-016 can be marked passing and committed.

## 2026-07-17 00:19:19 MST - US-017
- What was implemented
  - Encoded canonical exact number patterns 0 through 19 with stable `number-*` IDs, source references, and the source-inventory masks, including the approved 10 through 19 masks without pending sign-off flags.
  - Added source-linked number preview modeling, a responsive standalone HTML gallery, a committed JSON golden, generated catalog documentation, and completed 25-cell source-to-runtime review records for all 20 diagrams.
  - Added focused catalog, documentation parity, golden, and DOM coverage while preserving number identity independently from unrelated category entries.
- Files changed: `packages/patterns/src/catalog.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/previews.ts`, `docs/bingo-pattern-catalog.md`, `docs/number-pattern-previews.html`, `tests/fixtures/patterns/number-previews.v1.json`, `tests/pattern-catalog-doc.test.ts`, `tests/pattern-number-previews.test.tsx`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-017 - Encode, preview, and test numbers`
- Auto approval: `disabled`
- Checks:
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts tests/pattern-number-previews.test.tsx` (expected fail before implementation: six failures for missing number entries, review records, and preview API)
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts tests/pattern-number-previews.test.tsx tests/pattern-letter-previews.test.tsx tests/pattern-shape-previews.test.tsx` (pass; 5 files and 46 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 11 Vitest files and 195 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
  - Browser verification with `dev-browser` at 390px touch and 1440px desktop (pass; 20 entries, 20 thumbnails, source order 0-19, no horizontal overflow, three desktop columns, semantic labels, and no console warnings/errors)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the established category-preview pattern made this a straightforward standard implementation and no domain adviser was needed
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, and `ux-researcher`; targeted re-review by `documentation-engineer`
  - Findings: fixed stale future-tense catalog language; discarded the expected pre-review `prd.json` status finding because Ralph requires that metadata update only after review passes
- Decisions (why):
  - Reused the shared source-filtered preview model and semantic renderer with number-specific public functions because it preserves category isolation without duplicating rendering behavior.
  - Kept every mask in the canonical runtime catalog and generated documentation, goldens, and the standalone gallery from that data so maintained visual artifacts cannot become another mask authority.
  - Used `number-0` through `number-19` because category-specific stable IDs preserve selection identity and match the catalog's established shape and letter conventions.
- **Learnings for future iterations:**
  - The source PDF heading says “Letters + numbers,” but its complete diagram set is exactly numbers 0 through 19 in reading order.
  - Number category artifacts use the same source-filtered preview and generated-parity workflow as shape and letter artifacts; Christmas can add its category wrapper without changing shared output behavior.
- Known issues / follow-ups:
  - Christmas catalog entries, goldens, and preview pages remain intentionally assigned to US-018.
  - Existing local letter-preview screenshots and Lighthouse reports remain untracked and excluded because they are unrelated verification outputs.
- Next iteration start:
  - If review passes, implement US-018 Christmas encoding, duplicate-identity preservation, source-linked previews, and golden fixtures; otherwise resume any recorded blocking finding.
---

Review remediation for US-017 at 2026-07-17 00:19:19 MST:
- The initial `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, and `ux-researcher` pass produced one valid blocking medium finding: the source-inventory and duplicate-mask explanations still described implemented shape, letter, and number mappings as future work.
- Updated those explanations to distinguish current runtime IDs from the remaining Christmas mappings.
- Discarded the separate pre-review `prd.json` finding because Ralph explicitly requires the status file to remain `passes: false` and unstaged until the staged implementation review passes.
- Post-fix checks: focused 38 documentation/catalog tests, formatting, typecheck, all 195 Vitest tests, and staged/unstaged whitespace checks pass.
- Targeted re-review: `documentation-engineer` returned an empty findings list and confirmed the documentation-accuracy root cause is fully resolved.

Targeted re-review outcome for US-017 at 2026-07-17 00:19:19 MST:
- `documentation-engineer` reviewed the revised complete staged diff and returned no actionable findings.
- The corrected inventory and duplicate-mask explanations now accurately distinguish implemented shape, letter, and number IDs from the remaining Christmas work.
- Residual risk is limited to manual rather than automated image-level PDF comparison and browser verification outside Chromium; neither is required by this story.
- No in-scope critical, high, or medium findings remain. US-017 can be marked passing and committed.

## 2026-07-17 00:37:07 MST - US-018
- What was implemented
  - Encoded all 12 Christmas source patterns as exact runtime entries with category-specific stable IDs, source references, and canonical source masks.
  - Preserved Christmas Cross and Christmas Snow as independent selections despite matching Shape Cross and Shape Checkerboard masks.
  - Added source-linked Christmas preview modeling, a responsive standalone HTML gallery, a committed JSON golden, generated catalog documentation, and completed 25-cell source-to-runtime review records for every diagram.
- Files changed: `packages/patterns/src/catalog.ts`, `packages/patterns/src/index.ts`, `packages/patterns/src/previews.ts`, `docs/bingo-pattern-catalog.md`, `docs/christmas-pattern-previews.html`, `tests/fixtures/patterns/christmas-previews.v1.json`, `tests/pattern-catalog-doc.test.ts`, `tests/pattern-christmas-previews.test.tsx`, `tests/patterns.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-018 - Encode, preview, and test Christmas patterns`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts tests/pattern-christmas-previews.test.tsx` (expected fail before implementation: six failures for missing Christmas entries, review records, preview API, golden, and gallery; pass after implementation with 40 tests)
  - `bunx vitest run tests/patterns.test.ts tests/pattern-catalog-doc.test.ts tests/pattern-christmas-previews.test.tsx tests/pattern-number-previews.test.tsx tests/pattern-letter-previews.test.tsx tests/pattern-shape-previews.test.tsx` (pass; 6 files and 52 tests)
  - `bun install --frozen-lockfile` (pass; 248 installs checked with no changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (initial fail on new edits; pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 12 Vitest files and 201 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
  - Browser verification with `dev-browser` at 390px touch and 1440px desktop (pass; 12 ordered entries and thumbnails, complete semantic labels, no horizontal overflow, three desktop columns, and no console warnings/errors)
  - Lighthouse desktop snapshot (pass; accessibility 100 and best practices 100)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the established category catalog/preview workflow made this a straightforward standard implementation
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, and `ux-researcher`
  - Findings: none; all five specialists returned empty actionable finding lists
- Decisions (why):
  - Reused the shared source-filtered preview model and semantic renderer with Christmas-specific public functions because category artifacts should remain isolated without duplicating rendering behavior.
  - Used `christmas-tree` rather than `christmas-christmas-tree` while retaining the `christmas-*` category prefix because the source display name already contains the category word and stable IDs should remain concise.
  - Kept Christmas Cross and Snow masks unchanged and assigned distinct IDs because source/category identity cannot be deduplicated by matching geometry.
  - Did not update `AGENTS.md` or Codebase Patterns because the existing canonical-data, duplicate-identity, source-review, and category-preview guidance already covers the reusable implementation rules.
- **Learnings for future iterations:**
  - The complete Christmas PDF maps one-to-one to 12 exact runtime selections in source order with no aliases or flexible examples.
  - Christmas uses the same generated documentation, review-record parity, golden-model, and standalone-browser workflow as the prior source categories.
- Known issues / follow-ups:
  - Browser verification report artifacts remain untracked and excluded from the candidate story because they are local review outputs rather than maintained product artifacts.
  - Automated pixel-level comparison against the source PDF and non-Chromium browser verification were not required.
- Next iteration start:
  - If review passes, implement US-019 complete catalog auditing; otherwise resume any recorded blocking finding.
---

Review outcome for US-018 at 2026-07-17 00:37:07 MST:
- `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, and `ux-researcher` reviewed the complete staged catalog, documentation, generated artifacts, tests, and progress diff and returned no actionable findings.
- UI and UX specialists independently used `dev-browser` to verify the generated Christmas gallery at mobile and desktop widths.
- Residual risk is limited to manual rather than pixel-automated PDF transcription, Chromium-only browser verification, and no real screen-reader pronunciation test for punctuation-heavy masks; these do not violate the story requirements.
- No in-scope critical, high, or medium findings remain. US-018 can be marked passing and committed.

## 2026-07-17 00:49:23 MST - US-019
- What was implemented
  - Added one fail-closed catalog audit that strictly parses and joins all 82 documented PDF diagrams and review dispositions to runtime source references, stable IDs, aliases, exact/flexible masks, approved masks, distinct category identities, and all committed preview goldens.
  - Added mutation coverage proving the audit reports missing source mappings, invalid or collapsed IDs, alias mismatches, source/runtime parity changes, approved-mask changes, and missing golden entries.
  - Added reusable repository guidance requiring the complete joined audit for future catalog changes.
- Files changed: `AGENTS.md`, `packages/patterns/src/audit.ts`, `packages/patterns/src/index.ts`, `tests/pattern-catalog-audit.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-019 - Audit the complete pattern catalog`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/pattern-catalog-audit.test.ts` (expected fail before implementation: 7 failures because the audit API did not exist; pass after implementation with 7 tests)
  - Review-remediation regressions (expected fail: missing strict documentation parser and unknown dispositions were accepted; pass after implementation with 9 audit tests)
  - Focused complete pattern suite (pass; 7 files and 61 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only because it scanned pre-existing untracked US-018 Lighthouse/DOM artifacts; no intended or tracked file failed)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 13 Vitest files and 210 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the catalog, source-review, and golden-preview conventions were established and the standard-scope audit design was clear
- Specialist review:
  - Agents used: `code-reviewer` and `qa-expert`; targeted re-review by `qa-expert`
  - Findings: fixed fail-open documentation ingestion, incidental constraint mutations, unknown disposition handling, and order-sensitive golden comparison; targeted re-review returned no actionable findings
- Decisions (why):
  - Implemented a pure audit over supplied catalog/source/golden data so mutation tests can independently prove each required failure mode without filesystem mocking or changing canonical runtime data.
  - Parsed the existing human-reviewed source inventory and review records through a pure audit parser rather than creating another maintained mask source.
  - Compared the combined four category goldens with previews derived from the runtime catalog so missing, extra, stale, or reordered entries fail one complete gate.
  - Left unrelated untracked browser-verification artifacts unchanged and used the tracked-tree formatting check because modifying those files would violate story scope.
- **Learnings for future iterations:**
  - Category-local catalog and preview tests do not replace a complete joined audit; source mappings, aliases, runtime parity, identity constraints, and goldens must fail together when any link is removed or changed.
  - Audit requirements such as approved masks and deliberately distinct IDs belong in independent test input so the audit can detect canonical catalog corruption instead of deriving every expectation from the value under test.
- Known issues / follow-ups:
  - Pre-existing untracked US-018 Lighthouse, DOM, and screenshot artifacts still cause root `bun run format:check` to scan three non-product report files; they remain intentionally untouched and unstaged.
- Next iteration start:
  - Implement US-020 domain state-machine behavior with failing transition tests first.
---

Review remediation for US-019 at 2026-07-17 00:57:20 MST:
- The initial `code-reviewer` and `qa-expert` pass produced two blocking medium QA findings: documentation ingestion could silently skip malformed/unknown/unreviewed rows, and approved-mask/distinct-ID mutations could pass through incidental parity or duplicate-ID failures.
- Added failing regressions first, then moved documentation inventory/review parsing into a strict pure parser that validates every table row, disposition, 25-cell review, duplicate, and bidirectional source/review key mapping.
- Made runtime audit input reject unknown dispositions, isolated approved-mask and distinct-ID requirement mutations by exact issue message, and fixed the specific low order-sensitive golden comparison through recursive key canonicalization while preserving array order.
- Discarded the low pre-review `prd.json` bookkeeping observation because Ralph requires that file to remain unstaged until review passes and then adds it to the final intended file set.
- Post-fix checks: focused 61 pattern tests, lint, complete tracked-tree formatting, typecheck, all 210 Vitest tests, Playwright smoke, and staged/unstaged whitespace checks pass.
- Targeted re-review: `qa-expert` returned an empty findings list with no residual risks; `code-reviewer` was not repeated because its only finding was expected post-review bookkeeping.

Targeted re-review outcome for US-019 at 2026-07-17 00:57:20 MST:
- `qa-expert` reviewed the revised complete staged diff and confirmed the strict parser, isolated constraint mutations, unknown-disposition rejection, and structural golden comparison resolve all initial QA findings.
- No in-scope critical, high, or medium findings remain. US-019 can be marked passing and committed.

## 2026-07-17 01:07:54 MST - US-020
- What was implemented
  - Added a pure domain round state machine covering waiting, active, paused, co-winner-window, result, and terminal ended behavior with forward-only transition timestamps.
  - Enforced the only allowed continuation path from an initially selected One Line through Two Lines to terminal Blackout; initially selected Two Lines, Blackout, and exact patterns remain terminal after result.
  - Added lobby inactivity expiry behavior for waiting, completed, abandoned, and unprotected active lobbies while protecting active lobbies with calls or connections and keeping expired lobbies terminal.
- Files changed: `AGENTS.md`, `packages/domain/src/index.ts`, `packages/domain/src/state-machine.ts`, `tests/domain-state-machine.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-020 - Define the domain state machine`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/domain-state-machine.test.ts` (expected fail before implementation: 18 failures because the state-machine API did not exist; pass after implementation and review remediation with 22 tests)
  - Forward-only timestamp regression (expected fail before implementation because ending before a pause was accepted; pass after implementation)
  - `bun run test:workspace-boundaries` via typecheck (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only because it scanned pre-existing untracked US-018 Lighthouse/DOM artifacts; no intended or tracked file failed)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `bun run test` (pass; 14 Vitest files and 232 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - Browser verification (not applicable; this story changes framework-independent domain behavior only)
- Implementation agents:
  - Recommended: none
  - Used: skipped because the dependency-free domain and stable-error conventions were established and the standard-scope transition rules were clear
- Specialist review:
  - Agents used: `code-reviewer` and `qa-expert`; targeted re-review by both specialists planned after substantive fixes
  - Findings: fixed stale stage fields, unstarted-round ending, unvalidated continuation targets, and incomplete transition/error matrix coverage before targeted re-review
- Decisions (why):
  - Represented progression using initial and current catalog modes so an initially selected Two Lines remains terminal while Two Lines reached from One Line may continue to Blackout.
  - Required continuation commands to identify the requested next mode so the domain rejects skipped or incompatible targets instead of leaving progression validation to adapters.
  - Kept the state machine independent of contracts and persistence because this story defines pure rules; later stories own database models and boundary adapters.
  - Used explicit millisecond timestamps and durations with injected current time so transition and inactivity tests remain deterministic without wall-clock dependencies.
  - Allowed an idle active lobby with no calls or connections to expire because the product rule protects active lobbies only while either protection is present.
- **Learnings for future iterations:**
  - Preserve `initialPatternMode` separately from `patternMode`; the current mode alone cannot distinguish an initially terminal Two Lines round from the continuation path.
  - Expected invalid state transitions return stable result errors, while malformed internal timing/count inputs throw `RangeError` consistently with existing domain APIs.
- Known issues / follow-ups:
  - Pre-existing untracked US-018 Lighthouse, DOM, and screenshot artifacts still cause root `bun run format:check` to scan three non-product report files; they remain intentionally untouched and unstaged.
- Next iteration start:
  - If review passes, implement US-021 durable PostgreSQL state after applying the scoped migration approval; otherwise resume any recorded blocking finding.
---

Review remediation for US-020 at 2026-07-17 01:15:51 MST:
- The initial `code-reviewer` and `qa-expert` pass produced four deduplicated medium blockers: stale `activeAt` fields in paused/co-winner variants, waiting-to-ended states incompatible with the strict boundary model, continuation commands unable to validate the requested target, and incomplete invalid-transition/malformed-input coverage.
- Added failing regressions first for exact stage shapes, unstarted ending, and skipped continuation targets; the focused suite failed in all three implementation areas before remediation.
- Constructed stage variants from their declared common fields, rejected ending before start, required and validated the requested continuation mode, and added a complete stage-command rejection matrix plus malformed timestamp coverage.
- Post-fix checks: focused 22 state-machine tests, lint, complete tracked-tree formatting, workspace boundaries, typecheck, all 232 Vitest tests, Playwright smoke, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer` and `qa-expert` review of the revised complete staged diff because both original specialist areas were affected.

Targeted re-review outcome for US-020 at 2026-07-17 01:17:43 MST:
- `code-reviewer` and `qa-expert` returned empty finding lists after reviewing the revised complete staged diff.
- The stage-shape, boundary compatibility, continuation-target, transition-matrix, and malformed-input findings are resolved.
- Residual risk is limited to deferred persistence/wire-adapter integration and no measured coverage percentage; neither is required by this pure-domain story, and all project-required checks pass.
- No in-scope critical, high, or medium findings remain. US-020 can be marked passing and committed.

## 2026-07-17 01:40:16 MST - US-021
- What was implemented
  - Added the Prisma 7/PostgreSQL durable model for lobbies, participants, hash-only sessions, presence generations, one current round, cards, marks, private draw positions, calls, co-winners, active-lobby events, command results, and inactivity timestamps.
  - Added an append-only migration with PostgreSQL-only partial indexes, checks, composite foreign keys, current-round uniqueness, valid-card validation, called-mark integrity, scoped event/command integrity, and lobby-owned cascade deletion.
  - Added a Prisma-backed aggregate repository that exposes no Prisma types and reconstructs complete authoritative state after disconnect/restart, plus PostgreSQL 16 integration coverage for restart persistence, scoped uniqueness, token storage, cascade metadata, and no prior-round history model.
- Files changed: `.gitignore`, `.prettierignore`, `AGENTS.md`, `README.md`, `bun.lock`, `eslint.config.mjs`, `package.json`, `packages/database/package.json`, `packages/database/prisma.config.ts`, `packages/database/prisma/schema.prisma`, `packages/database/prisma/migrations/migration_lock.toml`, `packages/database/prisma/migrations/20260717083124_initial_durable_state/migration.sql`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/tsconfig.json`, `progress.txt`, `prd.json`
- Commit message: `feat: US-021 - Model durable PostgreSQL state`
- Auto approval: `enabled`
- Checks:
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation: 4 failures because the repository and schema were absent; pass after implementation with 4 tests)
  - `DATABASE_URL=... bun run db:migrate:deploy` (pass; initial migration applied to an empty isolated PostgreSQL 16 database)
  - `DATABASE_URL=... prisma validate --config packages/database/prisma.config.ts` (pass)
  - `DATABASE_URL=... prisma migrate status --config packages/database/prisma.config.ts` (pass; schema up to date)
  - `bun install --frozen-lockfile` (pass; generated Prisma client refreshed with no lock changes)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report artifacts)
  - `git diff --cached --name-only -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete staged candidate)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 15 Vitest files and 238 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `postgres-pro`, `database-optimizer`
  - Used: `postgres-pro`, `database-optimizer` for relational shape, custom PostgreSQL constraints, minimal indexing, repository boundaries, restart query shape, and integration-test guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and dependency/data-integrity-triggered `security-engineer`; targeted re-review by the same specialists
  - Findings: initial cross-lobby card ownership, nullable-check, call-fixture, and cascade-assertion findings fixed; targeted re-review returned no actionable findings
- Decisions (why):
  - Kept exactly one round per lobby with cascaded round-owned state because persistence must support restart without retaining prior-round result history.
  - Stored the full draw permutation only in the server-internal repository aggregate and exposed no Prisma client/model types, so future adapters can construct privacy-safe snapshots without making future calls representable in wire contracts.
  - Used custom migration SQL for partial indexes, checks, and cross-table composite integrity because Prisma 7 does not model those PostgreSQL features; the Prisma schema remains the portable relation model.
  - Deferred Serializable retry, sequence allocation, and duplicate-command execution behavior to US-022 while modeling the uniqueness and foreign-key prerequisites now.
  - Left unrelated untracked browser-verification artifacts unchanged and used an isolated PostgreSQL 16 Compose project on loopback for migration and integration validation.
- **Learnings for future iterations:**
  - Prisma 7 requires an explicit PostgreSQL driver adapter at runtime and generates a client to the package-local ignored directory during root install preparation.
  - Future Prisma schema changes must inspect generated SQL for accidental loss of custom partial indexes, checks, or composite foreign keys that Prisma cannot represent.
  - Database integration tests are opt-in through an explicit migrated `TEST_DATABASE_URL`; generic root tests remain usable without starting PostgreSQL.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans pre-existing untracked US-018 Lighthouse/DOM artifacts; those unrelated files remain intentionally untouched and unstaged.
  - US-022 must add bounded Serializable retries, atomic event-sequence allocation, persist-before-broadcast command commits, and idempotent replay behavior over this schema.
- Next iteration start:
  - Implement US-022 serializable idempotent transactions over the committed durable schema.
---

Review remediation for US-021 at 2026-07-17 01:52:41 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four deduplicated blocking findings: cards were not constrained to participants in the round's lobby, nullable PostgreSQL checks failed open on `UNKNOWN`, the call-uniqueness fixture could fail through an unrelated draw foreign key, and cascade coverage checked only table-level metadata.
- Added failing regressions first; the focused suite failed in all four expected areas before remediation.
- Added lobby-scoped card storage and composite participant/round ownership constraints, propagated lobby identity through the repository, and added cross-lobby rejection coverage.
- Made card arrays non-null, forced nullable session/presence/round checks to evaluate `IS TRUE`, and covered missing rejoin, grace, automatic-interval, and card data.
- Corrected the duplicate-call fixture to retain a valid draw tuple, proved the same call position/ball remains reusable across rounds, and strengthened cascade assertions to require the exact owned table-to-parent foreign-key edges.
- An executing `DELETE` cascade test was not added because the active scoped approval explicitly excludes destructive operations and repository policy prohibits executing DELETE without separate confirmation; exact foreign-key edge and delete-rule inspection provides non-destructive migration verification.
- Post-fix checks: migration applied from empty to a fresh isolated PostgreSQL 16 database, focused 6 integration tests pass, `bun run lint` (pass), `bun run typecheck` (pass), full 238-test suite with `TEST_DATABASE_URL` (pass), Playwright smoke (pass), and migration deploy status (pass).
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because the fixes affect all three original specialist areas.

Targeted re-review outcome for US-021 at 2026-07-17 01:55:19 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists.
- The cross-lobby ownership, nullable-check, call-fixture, and cascade-edge findings are resolved.
- Residual risk is limited to structural rather than executing DELETE cascade verification because destructive-operation approval was not granted; the committed foreign-key graph and delete rules are covered non-destructively.
- No in-scope critical, high, or medium findings remain. US-021 can be marked passing and committed.

## 2026-07-17 02:12:00 MST - US-022
- What was implemented
  - Added a Prisma-confined command transaction repository that serializes mutations per lobby, checks committed command IDs before effects, and atomically persists state, active-lobby events, command results, and monotonic event sequences.
  - Added bounded observable retries for Prisma/PostgreSQL serialization conflicts, including adapter-wrapped SQLSTATEs, with a typed exhaustion error and privacy-safe retry events.
  - Added unit and PostgreSQL integration coverage for retries, exhaustion, public/private commit scope, sequential and concurrent idempotent replay, contiguous concurrent ordering, and rollback before commit.
- Files changed: `AGENTS.md`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/transaction-retry.test.ts`, `packages/database/src/transaction-retry.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-022 - Add serializable idempotent transactions`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run packages/database/src/transaction-retry.test.ts` (expected fail before implementation because the retry module was absent; pass after implementation with 4 tests)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation because command transactions were absent; pass after implementation and review remediation with 14 tests)
  - `DATABASE_URL=... bun run db:migrate:deploy` (pass; existing initial migration applied to the approved local PostgreSQL 16 database, with no new migration required)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report artifacts)
  - Tracked tree plus intended new-file Prettier check (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 16 Vitest files and 250 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `postgres-pro`, `backend-developer`
  - Used: `postgres-pro`, `backend-developer` for the transaction protocol, package-owned API, retry classification, observability seam, concurrency risks, and integration-test strategy
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and data-integrity-triggered `security-engineer`; targeted re-review by `code-reviewer` and `qa-expert` planned after substantive fixes
  - Findings: concurrent private no-op replay, unchecked replay typing, deterministic retry assertion, and late-failure rollback findings fixed before targeted re-review
- Decisions (why):
  - Reused the existing command-result primary key, event foreign key, and lobby sequence column without a migration because they already enforce the required durable relationships.
  - Used a no-op lobby-row write before the idempotency lookup so all mutations for one lobby have one serializable ordering point, including participant-private commands that otherwise change no lobby state.
  - Scoped the callback repository to the target lobby and returned a broadcastable event only for a fresh active-lobby commit so callers cannot publish before commit, rebroadcast replays, or create sequence gaps for private results.
  - Required a caller-provided result decoder and rejected command-type/round mismatches on replay so persisted JSON is never cast to a newly inferred handler result type.
  - Used deterministic capped backoff rather than jitter because the single-instance MVP has bounded lobby contention and deterministic injected sleep makes retry behavior directly testable.
- **Learnings for future iterations:**
  - Prisma 7 surfaces raw PostgreSQL serialization failures as `P2010` errors with SQLSTATE `40001` nested in adapter metadata, while other write conflicts may use top-level `P2034`; retry classification must handle both without broadly retrying integrity failures.
  - Serializable callbacks can execute more than once after rolled-back conflicts, so IDs, timestamps, randomness, and external effects must be prepared outside the callback or persisted through its transaction-scoped repositories.
  - Replayed and participant-private commits deliberately return `committedEvent: null`; only a fresh active-lobby commit is safe to broadcast.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while the complete intended candidate passes Prettier.
  - Future command stories must add transaction-scoped state methods rather than expose Prisma or perform external work inside the retryable callback.
- Next iteration start:
  - If review passes, implement US-023 secure lobby-code generation and active-lobby capacity enforcement; otherwise resume any recorded blocking finding.
---

Review remediation for US-022 at 2026-07-17 02:19:51 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four deduplicated blocking findings: private no-op duplicate races could invoke mutation twice, replayed JSON was cast to an unchecked caller-inferred type, PostgreSQL concurrency tests did not require a retry, and rollback coverage stopped before event/result persistence.
- Added failing regressions first for concurrent private no-op replay and mismatched command-type reuse; both failed in the expected implementation paths before remediation.
- Replaced the read lock with a no-op lobby-row write fence so every lobby command conflicts and retries consistently, even when the command has no state or event mutation.
- Added explicit persisted-result decoding and command scope checks, asserted real PostgreSQL retry observations, and proved a command-result foreign-key failure rolls back prior activity, sequence, and event writes.
- Post-fix checks: focused 4 retry tests and 14 database integration tests pass, `bun run lint` passes, the complete intended candidate passes Prettier, `bun run typecheck` passes, the full PostgreSQL-enabled suite passes with 250 tests, Playwright smoke passes, and `git diff --check` passes.
- Targeted re-review: pending `code-reviewer` and `qa-expert` review because their substantive findings changed implementation and tests; `security-engineer` is not repeated because it returned no findings and the fixes did not expand the reviewed security surface.

Targeted re-review outcome for US-022 at 2026-07-17 02:23:00 MST:
- `code-reviewer` returned no actionable findings and confirmed the private no-op write fence plus explicit result-decoding/scope-check remediations.
- `qa-expert` found one remaining medium test-reliability blocker: the integration tests require observed retries after unsynchronized `Promise.all` starts, so PostgreSQL may validly schedule all commands serially and make the assertions flaky.
- The finding is in scope because deterministic retry coverage is an explicit US-022 acceptance criterion; the next iteration must coordinate transaction overlap before asserting a real retry rather than relying on scheduler timing.
- Per the one-targeted-re-review limit, US-022 remains `passes: false`; no commit was created and `prd.json` was not updated.
- Next iteration start: add a deterministic transaction-test synchronization seam or database coordination barrier that forces two Serializable attempts to overlap, prove the test fails without retry handling, rerun all US-022 checks, and restart the staged review loop.

## 2026-07-17 02:27:55 MST - US-022
- What was implemented
  - Resumed the serializable transaction story by replacing scheduler-dependent retry assertions with a PostgreSQL lock-wait barrier that proves the duplicate command's second transaction overlaps the first at the per-lobby fence.
  - Retained deterministic retry verification on the coordinated duplicate-command path and kept the independent sequence-ordering test focused on contiguous committed sequences rather than requiring incidental contention.
  - Preserved the previously reviewed transaction implementation, result decoding, command-scope validation, rollback behavior, and private/public broadcast semantics.
- Files changed: `AGENTS.md`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/transaction-retry.test.ts`, `packages/database/src/transaction-retry.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-022 - Add serializable idempotent transactions`
- Auto approval: `enabled`
- Checks:
  - `docker compose up -d --wait postgres` (pass; PostgreSQL 16 healthy)
  - `DATABASE_URL=... bun run db:migrate:deploy` (pass; no pending migrations)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (pass; 14 tests with coordinated lock-induced retry)
  - `bunx vitest run packages/database/src/transaction-retry.test.ts` (expected fail in 2 deadlock-classification cases during review remediation; pass after implementation with 5 tests)
  - `bun run lint` (pass)
  - `bun run format:check` (expected fail only on three pre-existing untracked US-018 report files)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked candidate)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 16 Vitest files and 251 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
- Implementation agents:
  - Recommended: `postgres-pro`, `backend-developer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-022 and the remaining work directly addressed one concrete prior QA finding
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and data-integrity specialist `postgres-pro`; targeted re-review by `postgres-pro` planned after substantive retry-telemetry fixes
  - Findings: fixed inaccurate deadlock telemetry before targeted re-review; `code-reviewer` and `qa-expert` returned no actionable findings
- Decisions (why):
  - Observed `pg_stat_activity` for the second command's lock wait before releasing the first transaction because this coordinates real database overlap without adding a test-only synchronization seam to the production repository API.
  - Removed retry assertions from the four-command sequence test because contiguous sequence allocation is its contract and PostgreSQL may validly serialize unsynchronized starts without a retry.
  - Kept the retry assertion on the coordinated duplicate path because it now fails if conflict classification or retry execution is removed while also proving the mutation callback does not repeat.
  - Did not add more `AGENTS.md` guidance because its existing Serializable callback and external-effect rules remain complete; the test-specific coordination pattern belongs in consolidated progress guidance.
- **Learnings for future iterations:**
  - `Promise.all` starts do not guarantee overlapping PostgreSQL transactions; wait until the competing command is blocked on the known row fence before releasing the winning transaction.
  - Retry behavior and sequence ordering should be asserted in separately coordinated tests so valid database scheduling cannot make an unrelated ordering test flaky.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while the complete tracked candidate passes Prettier.
- Next iteration start:
  - If review passes, implement US-023 secure lobby-code generation and active-lobby capacity enforcement; otherwise resume any recorded blocking finding.
---

Review remediation for US-022 at 2026-07-17 02:34:29 MST:
- The restarted complete staged review used `code-reviewer`, `qa-expert`, and `postgres-pro`; code and QA returned empty finding lists, while the PostgreSQL specialist identified one medium telemetry issue.
- Split SQLSTATE `40P01` deadlocks from `40001` serialization conflicts in observable retry events while preserving the same bounded retry behavior for both.
- Added failing regressions first for top-level and adapter-nested deadlock codes; both failed with the prior `serialization-conflict` reason and pass with the new `deadlock` reason.
- Post-fix checks: focused retry tests pass with 5 tests, `bun run lint` passes, tracked-tree Prettier passes, `bun run typecheck` passes, and the full PostgreSQL-enabled suite passes with 251 tests.
- Targeted re-review: pending `postgres-pro` review because the substantive fix addresses its sole finding; `code-reviewer` and `qa-expert` are not repeated because their initial finding lists were empty.

Targeted re-review outcome for US-022 at 2026-07-17 02:35:57 MST:
- `postgres-pro` confirmed standalone and nested `40P01` classification but found one remaining medium precedence gap: an outer generic `P2034` returns `serialization-conflict` before nested metadata can identify a more specific `40P01` deadlock.
- The finding remains in scope because bounded retry telemetry is required to be observable and the current traversal can still mislabel wrapped PostgreSQL deadlocks.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-022` remains `passes: false` and `prd.json` remains unchanged.
- Next iteration start: add a failing mixed-wrapper regression such as `{ code: "P2034", meta: { originalCode: "40P01" } }`, scan all reachable metadata for `40P01` before falling back to generic `P2034`/`40001`, rerun all US-022 checks, and restart the complete staged review loop.

## 2026-07-17 02:38:57 MST - US-022
- What was implemented
  - Resumed the serializable transaction story and corrected mixed-wrapper retry telemetry so a nested PostgreSQL `40P01` deadlock takes precedence over an outer generic Prisma `P2034` conflict.
  - Added a focused regression proving wrapped metadata is fully scanned before generic serialization fallback, while preserving bounded retries, idempotent replay, atomic persistence, and monotonic sequence behavior.
  - Preserved the previously reviewed PostgreSQL lock-wait coordination that deterministically proves real Serializable retry behavior.
- Files changed: `AGENTS.md`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/transaction-retry.test.ts`, `packages/database/src/transaction-retry.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-022 - Add serializable idempotent transactions`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run packages/database/src/transaction-retry.test.ts` (expected fail before implementation: mixed `P2034`/nested `40P01` wrapper reported `serialization-conflict`; pass after implementation with 6 tests)
  - `docker compose ps` (pass; PostgreSQL 16 healthy on loopback)
  - `DATABASE_URL=... bun run db:migrate:deploy` (pass; no pending migrations)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (pass; 14 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (expected fail only on three pre-existing untracked US-018 report files)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked candidate)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass; 16 Vitest files and 252 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `postgres-pro`, `backend-developer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-022 and the remaining change directly addressed one concrete prior PostgreSQL specialist finding
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and data-integrity specialist `postgres-pro`; targeted re-review by `qa-expert` planned after its substantive test fix
  - Findings: initial broadcast-candidate/persisted-event equality gap fixed before targeted re-review; `code-reviewer` and `postgres-pro` returned no actionable findings
- Decisions (why):
  - Scanned every reachable supported wrapper before returning the generic serialization classification because specific PostgreSQL SQLSTATE telemetry is more informative than Prisma's outer retryable category.
  - Kept immediate return for `40P01` but accumulated generic conflict detection because no supported retry reason is more specific than deadlock and the traversal remains bounded by object identity.
  - Added no further nested `AGENTS.md` guidance because the root Serializable callback rules and consolidated retry-classification pattern cover the reusable behavior.
- **Learnings for future iterations:**
  - Outer Prisma codes can describe a broad retry category while nested adapter metadata carries the precise PostgreSQL SQLSTATE; classification precedence must inspect the complete supported wrapper graph.
  - A visited-object set keeps recursive/wrapped error metadata traversal cycle-safe without logging error contents.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while the complete tracked candidate passes Prettier.
- Next iteration start:
  - If review passes, implement US-023 secure lobby-code generation and active-lobby capacity enforcement; otherwise resume any recorded blocking finding.
---

Review remediation for US-022 at 2026-07-17 02:45:26 MST:
- The restarted complete staged review used `code-reviewer`, `qa-expert`, and `postgres-pro`; code and PostgreSQL returned empty finding lists, while QA identified one medium atomicity-test gap.
- Strengthened the active-lobby command integration test to compare the complete newly persisted durable event with the exact event returned for post-commit broadcast, covering sequence, round, type, schema version, payload, and timestamp.
- The assertion passed against the existing implementation, confirming the finding was a regression-coverage gap rather than a production mapping defect.
- Post-fix checks: focused 14-test database integration suite passes, `bun run lint` passes, tracked-tree Prettier passes, `bun run typecheck` passes, the full PostgreSQL-enabled suite passes with 252 tests, and diff whitespace checks pass.
- Targeted re-review: pending `qa-expert` review because the substantive fix addresses its sole finding; `code-reviewer` and `postgres-pro` are not repeated because their initial finding lists were empty.

Targeted re-review outcome for US-022 at 2026-07-17 02:47:10 MST:
- `qa-expert` returned an empty actionable finding list and confirmed the sequence-ordered final restored event reliably represents the newly persisted event.
- The durable-event/broadcast-candidate equality gap is resolved, and no new test-reliability issue was introduced.
- Residual risk is limited to future Prisma/adapter error-wrapper shapes that may require additional SQLSTATE traversal coverage; all currently supported wrappers and required project checks pass.
- No in-scope critical, high, or medium findings remain. US-022 can be marked passing and committed.

## 2026-07-17 03:02:11 MST - US-023
- What was implemented
  - Added secure six-character lobby-code generation from the exact unambiguous uppercase Base32 alphabet using injected cryptographic random bytes and unbiased bounded selection.
  - Added case-insensitive entry normalization while leaving canonical format validation strict, so normalization does not turn codes into authentication credentials or accept invalid characters.
  - Replaced unrestricted durable lobby creation with an atomic `createActive` repository path that serializes admission, enforces the configured waiting/active limit, retries active-code collisions, and permits completed-code reuse.
  - Added active-only locator lookup that returns only a lobby ID and no participant authority or private state.
- Files changed: `AGENTS.md`, `packages/domain/src/random.ts`, `packages/domain/src/lobby-code.ts`, `packages/domain/src/index.ts`, `tests/lobby-code.test.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/index.ts`, `packages/database/src/database.integration.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-023 - Generate secure lobby codes`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/lobby-code.test.ts` (expected fail before implementation: 4 failures because the lobby-code API did not exist; pass after implementation with 4 tests)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation: 4 failures because atomic active-lobby creation and locator lookup did not exist; pass after implementation and review remediation with 20 tests)
  - `bun run test:workspace-boundaries` (pass)
  - `bun run lint` (pass)
  - `bun run format:check` (expected fail only on three pre-existing untracked US-018 report files)
  - Tracked tree plus intended new-file Prettier check (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 17 Vitest files and 262 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: `backend-developer` after inspection showed the story crossed domain randomness and concurrent PostgreSQL admission; guidance informed the policy-aware API and test risks
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and secure-randomness/data-integrity specialist `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: Unicode case-folding and late active-code unique-race findings fixed before targeted re-review
- Decisions (why):
  - Kept code generation and case normalization in the framework-independent domain while injecting `nextCode` into the database repository so Prisma remains confined and runtime crypto wiring stays with later adapters.
  - Generated each candidate before entering the retryable Serializable transaction so transaction retries reuse one candidate rather than consuming entropy or changing callback behavior.
  - Serialized all lobby admissions with one PostgreSQL transaction advisory lock because no parent row exists to lock before the first lobby and capacity must remain correct under concurrent creation.
  - Returned only an active lobby ID from code lookup because the code is a locator; later session credentials must independently authorize every private operation.
  - Added no migration because the committed partial unique index and code-format check already enforce active-code persistence constraints.
- **Learnings for future iterations:**
  - The active-code partial unique index deliberately releases codes when a lobby becomes completed or abandoned; repository lookup and capacity counting must use the same waiting/active status set.
  - Random values and other nondeterministic inputs must be prepared outside retryable transaction callbacks, while admission checks and complete aggregate insertion stay inside the serialized transaction.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while every tracked and intended new file passes Prettier.
  - HTTP create/join authorization and rate limiting remain intentionally assigned to US-027; a lobby code grants no authority.
- Next iteration start:
  - If review passes, implement US-024 username normalization, stable validation, uniqueness, and participant-limit enforcement; otherwise resume any recorded blocking finding.
---

Review remediation for US-023 at 2026-07-17 03:10:46 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced two deduplicated blocking medium findings: Unicode uppercasing could transform invalid input into a valid ASCII code, and a late active-code unique violation could escape the outer candidate retry loop.
- Added a failing Unicode regression first, then restricted normalization to ASCII lowercase characters so invalid non-ASCII input remains invalid for strict contract parsing.
- A coordinated same-candidate creator regression passed against existing Serializable retry behavior; a stricter late-commit race then failed with Prisma `P2002`, proving the unique-index path could bypass candidate retry.
- Added narrow Prisma collision classification for only the Lobby `code` field, allowing that rolled-back transaction to advance to the next candidate while unrelated aggregate uniqueness errors continue to propagate.
- Post-fix checks: focused domain and database regressions pass, `bun run lint` passes, tracked/intended Prettier passes, `bun run typecheck` passes, the full PostgreSQL-enabled suite passes with 262 tests, Playwright smoke passes, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` review because the fixes address findings from all three specialist areas.

Targeted re-review outcome for US-023 at 2026-07-17 03:10:46 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists.
- The ASCII-only normalization and narrow late active-code collision retry resolve both initial findings without masking unrelated integrity failures.
- Residual risk is limited to runtime cryptographic wiring plus HTTP authorization/rate limiting intentionally assigned to later stories, and the bounded 128-candidate exhaustion path is not separately integration-tested.
- No in-scope critical, high, or medium findings remain. US-023 can be marked passing and committed.

## 2026-07-17 03:19:22 MST - US-024
- What was implemented
  - Added domain username normalization that preserves display casing, trims and collapses Unicode whitespace, applies NFC, derives the lowercase uniqueness key, and returns stable errors for empty, control/format, or overlength names.
  - Added lobby-scoped participant reservation that serializes on the active lobby, rejects normalized-name collisions, enforces the supplied player limit, and returns stable full/not-found/taken results.
  - Added Unicode, whitespace, control-character, collision, configurable-limit, rejected-insert, and concurrent-capacity tests.
- Files changed: `packages/domain/src/username.ts`, `packages/domain/src/index.ts`, `tests/username.test.ts`, `packages/database/package.json`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/index.ts`, `packages/database/src/database.integration.test.ts`, `bun.lock`, `progress.txt`, `prd.json`
- Commit message: `feat: US-024 - Normalize and reserve usernames`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/username.test.ts` (expected fail before implementation because the normalization API was absent; review regressions then failed for canonical, format-control, and length handling; pass after remediation with 7 tests)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation because participant reservation was absent; review regressions then failed against caller-controlled keys and limits above 25; pass after remediation with 26 tests)
  - `bun install --frozen-lockfile` (pass; package-local domain dependency is reproducible)
  - `bun run lint` (pass)
  - `bun run format:check` (expected fail only on three pre-existing untracked US-018 browser report artifacts)
  - Intended-file Prettier check (pass)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 18 Vitest files and 275 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: skipped because this was a standard, focused domain/repository change with clear existing transaction and test patterns
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and untrusted-input/data-integrity specialist `security-engineer`; targeted re-review by `qa-expert` and `security-engineer` planned after substantive fixes
  - Findings: caller-controlled uniqueness, Unicode canonical/format/length validation, product-limit validation, persisted/scoped forms, and deterministic concurrency coverage fixed before targeted re-review
- Decisions (why):
  - Rejected control characters before whitespace normalization so tabs, newlines, and every other Unicode `Cc` value cannot be silently converted into acceptable spacing.
  - Made the repository normalize raw names through the domain API because accepting an independent uniqueness key allowed callers to bypass canonical display and collision rules.
  - Reused the lobby-row write fence and Serializable retry policy because name uniqueness and participant capacity must remain correct under concurrent joins.
  - Passed the configured player limit into each reservation while enforcing 25 as the repository's absolute ceiling, so validated overrides work without allowing a miswired caller to exceed the product maximum.
- **Learnings for future iterations:**
  - JavaScript Unicode lowercasing preserves required non-ASCII behavior, including context-sensitive mappings such as Greek final sigma; NFC prevents canonically equivalent spellings from reserving separate names.
  - Control-character validation must precede whitespace collapse because several prohibited controls are also matched as whitespace.
  - The participant unique index and repository lookup are both scoped by lobby, while the serialized count prevents concurrent reservations from exceeding configured capacity.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while every intended file passes Prettier.
  - Session issuance, departed-slot behavior, and active-round waiting eligibility remain assigned to US-025, US-026, and US-036.
- Next iteration start:
  - If review passes, implement US-025 same-device lobby session issuance; otherwise resume any recorded blocking finding.
---

Review remediation for US-024 at 2026-07-17 03:28:27 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four blocking root causes: the repository trusted a caller-supplied uniqueness key; canonical/invisible/overlength Unicode forms lacked stable validation; values above the product maximum of 25 were accepted; and persistence/scope/concurrent race coverage was incomplete or scheduler-dependent.
- Added failing domain regressions for NFC, Unicode format controls, overlength display values, and lowercased-key expansion, then implemented canonicalization and stable length/control errors.
- Removed the caller-supplied normalized key from the reservation API and made the database repository invoke the domain normalizer before persistence; added the explicit 1-25 repository guard and package-local domain dependency.
- Strengthened integration coverage to verify exact persisted forms, same-name reuse across lobbies, deterministic overlapping capacity and normalized-name races, stable collision results, and observed Serializable retries.
- Changed database-test connection cleanup to run after each test after the expanded suite exposed PostgreSQL connection exhaustion.
- Post-fix checks: focused 7 domain and 26 database tests pass, frozen install passes, lint passes, intended Prettier passes, typecheck passes, the full PostgreSQL-enabled suite passes with 275 tests, and Playwright smoke passes.
- Targeted re-review: pending `qa-expert` and `security-engineer` because the substantive fixes address their findings; `code-reviewer` is not repeated because its only low bookkeeping observation is resolved by the required post-review PRD staging sequence.

Targeted re-review outcome for US-024 at 2026-07-17 03:28:27 MST:
- `qa-expert` and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists with no residual risks.
- Deterministic concurrency, exact persistence, lobby scoping, trusted canonicalization, Unicode validation, schema-length alignment, and the absolute product limit are covered.
- The initial code review's low `prd.json` bookkeeping observation is resolved by staging the required post-review passing-status update now; it was not an implementation defect.
- No in-scope critical, high, or medium findings remain. US-024 can be marked passing and committed.

## 2026-07-17 03:44:39 MST - US-025
- What was implemented
  - Added a framework-neutral web session service that generates 256-bit unpadded base64url credentials, hashes the exact token with SHA-256, persists only the digest, and returns a Secure HttpOnly SameSite=Strict cookie scoped to the canonical active-lobby API path.
  - Added missing/malformed-cookie handling that requires a new participant and recognized-session resolution that derives the prior participant only from a lobby-scoped persisted hash lookup.
  - Added Prisma-confined session issuance and narrow identity lookup for non-departed participants in waiting/active lobbies, with exact hash-collision handling and no schema migration because the existing hash-length, uniqueness, ownership, and cascade constraints already satisfy persistence requirements.
- Files changed: `AGENTS.md`, `apps/web/src/participant-session.ts`, `apps/web/src/participant-session.test.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/index.ts`, `packages/database/src/database.integration.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-025 - Issue same-device lobby sessions`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run apps/web/src/participant-session.test.ts` (expected fail before implementation because the session service module was absent; pass after implementation with 6 tests)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation because session repository methods were absent; pass after implementation with 29 tests)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report artifacts after intended files were formatted)
  - Intended-file Prettier check (pass after formatting)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass; 19 Vitest files and 284 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `security-engineer`
  - Used: `security-engineer` for credential entropy/encoding/hash, cookie scope, persistence lookup, collision behavior, migration assessment, and security-test guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive specialist `security-engineer`
  - Findings: none; all three specialists returned empty actionable finding lists
- Decisions (why):
  - Kept credential/cookie behavior in a framework-neutral `apps/web` service because Next.js endpoints land in US-027, while Prisma persistence remains confined to `packages/database`.
  - Used 32 random bytes, unpadded base64url, and SHA-256 because a uniformly random 256-bit bearer credential does not need a password KDF; the plaintext exists only in the returned cookie descriptor.
  - Used a `__Secure-` cookie with no Domain, no persistent expiry, SameSite=Strict, and a per-lobby API path because `__Host-` would require the broader root path and the product has not defined persistent cookie retention.
  - Added no migration because the committed schema already enforces a 32-byte unique digest, lobby/participant ownership, lifecycle consistency, and lobby-owned cascade deletion.
- **Learnings for future iterations:**
  - Hash the encoded credential rather than raw entropy so issuance and cookie resolution use one canonical representation, and reject malformed/noncanonical tokens before querying persistence.
  - Session authentication should return only the scoped identity projection needed by adapters; loading `findById` would unnecessarily expose all session hashes and private lobby state to an authentication path.
  - Generate credentials outside retryable persistence callbacks, classify only the exact token-hash unique violation as a regenerable collision, and let unrelated integrity failures propagate.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while every intended story file passes Prettier.
  - US-026 owns disconnect timestamps, the two-minute rejoin window, departed-slot invalidation, and active-round new-join eligibility.
- Next iteration start:
  - If review passes, implement US-026 two-minute same-device rejoin behavior; otherwise resume any recorded blocking finding.
---

Initial review outcome for US-025 at 2026-07-17 03:50:12 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the complete staged candidate and returned empty actionable finding lists.
- Residual risk is limited to intentionally deferred disconnect/rejoin/departure behavior in US-026 and HTTP cookie serialization, secure entropy wiring, CSRF/origin controls, rate limits, and private caching in US-027.
- Destructive cascade execution was not run because repository policy prohibits DELETE without separate confirmation; the existing exact foreign-key edge and delete-rule integration coverage confirms session ownership by the lobby.
- No in-scope critical, high, or medium findings remain. US-025 can be marked passing and committed.

## 2026-07-17 04:04:58 MST - US-026
- What was implemented
  - Added server-clock disconnect handling that stores one configured rejoin deadline, cookie-only status resolution that offers `Rejoin as <username>` during the window, and cookie-hash rejoin that restores the prior active session.
  - Added Serializable lobby-scoped persistence transitions that treat the exact deadline as expired, atomically mark the participant and prior sessions departed, reject cross-lobby/inactive/deleted-lobby credentials, and preserve an eligible participant slot on timely rejoin.
  - Removed caller control over new-participant round eligibility and derive `waiting` in persistence whenever a current round has started and has not ended.
- Files changed: `AGENTS.md`, `apps/web/src/participant-session.ts`, `apps/web/src/participant-session.test.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-026 - Implement two-minute same-device rejoin`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run apps/web/src/participant-session.test.ts` (expected fail before implementation with 4 missing-lifecycle failures; pass after implementation with 9 tests)
  - `TEST_DATABASE_URL=... bunx vitest run packages/database/src/database.integration.test.ts` (expected fail before implementation with 10 missing-transition/eligibility failures; review regressions then failed for no-cookie expiry, sibling invalidation, capacity reuse, and credential reissuance; pass after remediation with 36 tests)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report files)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=... bun run test` (pass after review remediation; 19 Vitest files and 294 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none
  - Used: `security-engineer` because the story changes credential-based identity recovery and durable authorization state; guidance informed atomic transitions, the exact deadline boundary, sibling-session invalidation, persistence-derived eligibility, and security tests
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive specialist `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: fixed cookie-dependent expiry, stale sibling ejection, departed-capacity counting, pre-lock authorization time, and weak active-stage eligibility coverage before targeted re-review
- Decisions (why):
  - Reused the existing session lifecycle columns and constraints without a migration because they already represent active, disconnected, deadline, and departed states exactly.
  - Used the active-lobby transaction fence for status, rejoin, and expiry because a lookup followed by a separate update could race at the two-minute boundary or reclaim an invalidated slot.
  - Defined the window as open only while `now < rejoinUntil`; the exact configured deadline atomically departs the participant and invalidates all nondeparted credentials for that slot.
  - Derived new-join eligibility from the persisted round stage because browser or HTTP callers must not be able to claim current-round participation.
  - Kept presence-generation changes out of scope because connection aggregation and absence generations belong to US-032 and US-033.
- **Learnings for future iterations:**
  - Durable deadlines plus opportunistic atomic expiry avoid relying on process-local timers and remain correct across process restarts.
  - Rejoin status may expose only the prior username and narrow session identity after a lobby-scoped hash lookup; neither username nor participant ID is proof of reclaim authority.
  - Idempotent disconnect must retain the original deadline rather than extending the window on repeated disconnect handling.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while the complete tracked tree passes Prettier.
  - Presence generation departure and realtime disconnect wiring remain intentionally assigned to later presence/Socket.IO stories.
- Next iteration start:
  - If review passes, implement US-027 versioned lobby entry HTTP APIs; otherwise resume any recorded blocking finding.
---

Review remediation for US-026 at 2026-07-17 04:18:42 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four deduplicated blocking root causes: expiry required the old cookie, stale sibling deadlines could eject a successfully rejoined participant, departed rows still consumed capacity, and authorization reused time sampled before the transaction fence/retry.
- Added failing regressions first for a no-cookie expiry sweep, sibling-session invalidation, replacement capacity with waiting eligibility, and replacement credential issuance after expiry; the focused suites failed in the expected old paths.
- Added an injectable repository lifecycle clock sampled after the lobby fence on every retry, a shared due-session expiry transition callable without credentials and reused by status, rejoin, disconnect, credential issuance, and participant reservation.
- Expiry now invalidates each due credential at its durable deadline and departs the participant only when no active or still-eligible sibling remains; successful rejoin activates one session and invalidates every sibling so stale credentials cannot later eject the slot.
- Participant capacity now counts only nondeparted rows, and active-round eligibility coverage uses an actual `active` stage while a no-round branch proves `playing` remains available before a round.
- Post-fix checks: focused 9 web and 36 PostgreSQL tests pass, lint passes, tracked-tree Prettier passes, typecheck passes, the full PostgreSQL-enabled suite passes with 294 tests, Playwright smoke passes, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because fixes affected every original specialist area.

Targeted re-review outcome for US-026 at 2026-07-17 04:23:04 MST:
- `qa-expert` and `security-engineer` returned empty actionable finding lists and confirmed the no-cookie repository sweep, lock-time clock sampling, sibling invalidation, capacity reuse, and eligibility remediations.
- `code-reviewer` identified one remaining medium acceptance blocker: `resolveSameDeviceSession` returns for a missing or malformed cookie before invoking `expireParticipantRejoinWindows`, so a cleared-cookie status check does not itself depart the overdue prior slot.
- The finding is in scope because cleared-cookie behavior and departure after 120 seconds are explicit US-026 acceptance criteria; the web status service must sweep the lobby before its cookie-validation short circuit and cover that ordering with a fake-timer regression.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-026` remains `passes: false` and `prd.json` remains unchanged.
- Next iteration start: add a failing cleared-cookie deadline test proving expiry runs before cookie validation, invoke the lobby-scoped expiry at the start of `resolveSameDeviceSession`, rerun all US-026 checks, and restart the complete staged review loop.

## 2026-07-17 04:26:44 MST - US-026
- What was implemented
  - Resumed the blocked story with a fake-timer regression proving that same-device status resolution sweeps overdue rejoin windows even after the participant clears the cookie.
  - Invoked lobby-scoped expiry before cookie validation so missing and malformed credentials still allow the prior participant slot to depart at its durable deadline.
  - Preserved the reusable credential-independent expiry rule in root agent guidance and the consolidated Codebase Patterns section.
- Files changed: `AGENTS.md`, `apps/web/src/participant-session.ts`, `apps/web/src/participant-session.test.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-026 - Implement two-minute same-device rejoin`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run apps/web/src/participant-session.test.ts` (expected fail before implementation because no expiry sweep occurred; pass after implementation with 10 tests)
  - `TEST_DATABASE_URL=<inherited> bunx vitest run packages/database/src/database.integration.test.ts` (fail before correcting the test environment because the inherited URL omitted a string password; no tests reached application behavior)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts` (pass with 36 tests)
  - `bun run lint` (pass)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report files)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass with 19 Vitest files and 295 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
- Implementation agents:
  - Recommended: none
  - Used: skipped because this resumed iteration addressed one concrete prior security-aware review finding using the existing store boundary and test pattern
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive specialist `security-engineer` planned for the complete staged diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Kept expiry ahead of even malformed-cookie rejection because participant departure is a lobby lifecycle transition, not an authentication side effect.
  - Reused `expireParticipantRejoinWindows` rather than duplicating deadline logic in the web service because persistence already serializes expiry and samples authoritative time after the lobby fence.
  - Left the unrelated untracked preview reports untouched and validated formatting across every tracked file instead of modifying files outside US-026.
- **Learnings for future iterations:**
  - Status endpoints that opportunistically maintain server lifecycle state must run credential-independent maintenance before early authentication returns.
  - An inherited database URL can be present but unusable; derive local test credentials from the healthy approved Compose service without printing them.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 browser report files; they remain untouched and unstaged, while the complete tracked tree passes Prettier.
  - Presence generation departure and realtime disconnect wiring remain intentionally assigned to later presence/Socket.IO stories.
- Next iteration start:
  - If review passes, implement US-027 versioned lobby entry HTTP APIs; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-026 at 2026-07-17 04:36:05 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass returned no security finding and four deduplicated review findings: direct lock-time deadline enforcement, staggered sibling expiry, repeated-disconnect idempotency, and durable departed-roster growth versus the future snapshot limit.
- Added PostgreSQL regressions for rejoin beginning before the deadline but acquiring the lobby fence at the deadline, direct reservation/capacity reuse at the deadline, repeated disconnect retaining the original deadline, and one sibling expiring while another remains eligible.
- The 39-test PostgreSQL suite passed on the first run after adding the tests, confirming the existing implementation already satisfied all three in-scope QA findings; no production code changed during this remediation.
- Deferred bounded visible-roster projection to US-027 because US-026 must durably retain departed participants and no authorized snapshot adapter exists yet; deleting departed rows here could cascade current-round cards or winner state. US-027 must project at most 25 participants while preserving the required departed-state UX without exposing unbounded durable history.
- Post-review checks: `bun run lint` (pass), tracked-tree Prettier (pass after formatting the added tests), `bun run typecheck` (pass), approved-local-PostgreSQL `bun run test` (pass with 19 files and 298 tests), `bun run test:e2e` (pass), and staged/unstaged whitespace checks (pass).
- Targeted re-review: pending `qa-expert` review of the revised complete staged diff because only test coverage changed for the in-scope findings.

Targeted re-review outcome for resumed US-026 at 2026-07-17 04:38:22 MST:
- `qa-expert` reviewed the revised complete staged diff and returned an empty actionable finding list with no residual test risks.
- The targeted review confirmed direct lock-time deadline enforcement, exact-deadline capacity reuse, repeated-disconnect idempotency, and staggered sibling eligibility are now covered.
- No unresolved in-scope critical, high, or medium findings remain. The bounded authorized-roster projection remains explicitly assigned to US-027.
- US-026 can be marked passing and committed after the final staged/unstaged consistency check.

## 2026-07-17 05:08:07 MST - US-027
- What was implemented
  - Added strict v1 HTTP contracts and explicit Next.js 16 App Router endpoints for the public pattern catalog, lobby creation/join, same-device status/rejoin, and actor-authorized snapshots.
  - Added secure mutation guards, independent in-memory create/join/rejoin limiter namespaces, stable versioned errors, private `no-store` responses, and cookie-only credential delivery.
  - Added atomic PostgreSQL host/session and player/session admission plus an actor-scoped snapshot query that allowlists own-card/current-call state and excludes hashes, foreign cards, future draws, active events, and command results.
  - Activated `bun run dev:web`, documented required `DATABASE_URL` behavior, and configured Next's Webpack resolver for the workspace's Node ESM TypeScript source convention.
- Files changed: `.gitignore`, `AGENTS.md`, `README.md`, `package.json`, `bun.lock`, `eslint.config.mjs`, `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next-env.d.ts`, `apps/web/next.config.ts`, `apps/web/src/app/api/v1/**/route.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/index.ts`, `packages/database/package.json`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-027 - Implement lobby entry HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/lobby-entry-contracts-v1.test.ts` (expected fail before implementation because the HTTP contract schemas were absent; pass with 7 tests)
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts` (expected fail before implementation because the HTTP service was absent; review regression then failed while the handler over-read a streamed body; pass with 17 tests)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts` (expected fail with 4 missing repository behaviors; review regressions then failed for command replay and entry metadata; pass with 50 tests)
  - `bun install --frozen-lockfile` (pass; Next.js and workspace dependencies are reproducible)
  - `bun run lint` (pass after excluding generated `.next` output)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run format:check` (fail only on three pre-existing untracked US-018 browser report files)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass after review remediation; 21 Vitest files and 333 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all six `/api/v1` routes compiled)
  - Live `GET /api/v1/patterns` against `bun run dev:web` (pass; versioned canonical catalog returned)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `security-engineer`
  - Used: `api-designer`, `security-engineer` for route contracts, safe status/error semantics, atomic admission, rate-limit boundaries, origin/body controls, cookie handling, and snapshot privacy guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: fixed replay-unsafe entry commands, unbounded streamed-body buffering, invalid no-card eligibility, late limiter ordering, unbounded roster fetching, and focused error/rate/concurrency/snapshot coverage before targeted re-review
- Decisions (why):
  - Kept transport route files thin over a framework-neutral Request/Response service so contract, rate-limit, authorization, and privacy behavior is deterministic under Vitest.
  - Added atomic entry-specific repository methods rather than composing reservation and session issuance because a credential collision or persistence failure must not strand a username or participant slot.
  - Used an allowlisted actor-scoped snapshot query instead of `findById` because the durable aggregate contains every private card, complete draw order, session hashes, events, and command results.
  - Selected Webpack with a narrow `.js` extension alias because Next 16 Turbopack does not support extension aliases and rewriting established Node ESM specifiers across all workspaces would exceed this story.
  - Used process-local fixed-window limiters without Redis because the MVP explicitly cannot depend on optional Redis; create, join, and rejoin quotas remain independent.
- **Learnings for future iterations:**
  - Next 16 dynamic route parameters are asynchronous, but these route adapters can forward the complete request URL to the shared dispatcher without trusting or duplicating route parameters.
  - Production builds and live development both need Webpack while workspace packages export raw TypeScript with `.js` ESM specifiers.
  - Snapshot privacy is strongest when forbidden aggregate fields are absent from the repository query itself rather than removed during HTTP serialization.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans three pre-existing untracked US-018 report files; they remain untouched and unstaged, while the complete tracked tree passes Prettier.
  - Shared/distributed rate limiting can replace the process-local implementation only when multi-instance infrastructure is introduced; the MVP does not depend on Redis.
- Next iteration start:
  - If review passes, implement US-028 single-use realtime tickets; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-027 at 2026-07-17 11:09:41 MST:
- The restarted `code-reviewer`, `qa-expert`, and `security-engineer` pass produced three deduplicated blocking findings: bare `/api/v1/lobbies/{code}` paths bypassed the shared dispatcher; the 26-participant contract exception was not tied to an active current round; and co-winner-window snapshots attempted to construct a settled result before settlement.
- Added failing focused regressions before implementation for the missing route adapter, a roundless 26-participant snapshot, and a PostgreSQL co-winner-window snapshot with confirmed winners but no settlement timestamp.
- Added the bare lobby-code Next route adapter for every supported method, restricted 26-participant projections to active lobbies with a current round, and constructed winner results only for result/ended stages.
- Added reusable root guidance and Codebase Patterns for stage-specific authoritative snapshot projection.
- Post-fix checks: focused HTTP, contract, and PostgreSQL regressions pass; the PostgreSQL-enabled root suite passes with typecheck, 21 files, and 349 tests; lint, tracked-tree Prettier, Playwright smoke, Next production build, and whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because fixes addressed findings from all three specialist areas.

Targeted re-review outcome for resumed US-027 at 2026-07-17 11:15:00 MST:
- `qa-expert` and `security-engineer` returned empty actionable finding lists with no residual risks in their remediation areas.
- `code-reviewer` identified one remaining medium blocker: repository `visibleParticipantLimit` can still select 26 participants for a waiting-status lobby with 25 persisted cards, while the tightened contract permits 26 only for an active lobby, causing snapshot parsing to return 500 for that durable state.
- The next iteration must add a PostgreSQL regression for a waiting lobby with 25 current-round cards plus a waiting requester, require durable lobby status `ACTIVE` before selecting the 26-entry projection, rerun all checks, and restart the complete staged review loop.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-027` remains `passes: false`, and `prd.json` remains unchanged.

Review remediation for US-027 at 2026-07-17 05:32:40 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced five accepted blocking root causes: create/join/rejoin command IDs were not replay-safe; streamed bodies were buffered before enforcing 4 KiB; no-card admissions could be `playing` while a round record existed; join/rejoin limits ran after database lookup; and the snapshot query fetched an unbounded durable roster before slicing.
- Added failing contract, HTTP, and PostgreSQL regressions for replay metadata and duplicate intent, early stream cancellation, then implemented persisted private command results with mismatch detection and fresh credential issuance for replayed admission without duplicating lobby/participant state.
- Rejoin now validates and commits its command scope before activation, with repeated commands returning `idempotentReplay: true`; create commands serialize through the global admission fence and join commands through the lobby fence.
- Stream parsing now reads and cancels incrementally at 4,096 bytes, mutation limiter buckets run before body/database work, any current round keeps a no-card entrant waiting, and the actor-scoped roster IDs are limited to 25 in SQL with self/host/winner/current-participant priority.
- Added stable error mapping, exact fixed-window reset, all three limiter namespaces, atomic normalized-name/capacity concurrency, waiting/ended eligibility, no-card snapshot, bounded roster, and rejoin replay coverage.
- Discarded the source-alias finding because the PRD explicitly preserves `Full House` as canonical source alias metadata for the runtime Blackout entry; the public catalog exposes source-linked canonical previews by design.
- Deferred active-lobby join event sequencing to the realtime presence stories because no participant-join active-event contract or broadcaster exists yet; entry command results are participant-private and carry no lobby sequence under the established sequence rules.
- Retained conservative process/lobby-scoped limiter keys because the standard Fetch `Request` exposed by self-hosted Next does not provide trusted peer-address metadata, spoofable forwarding headers are intentionally ignored, Redis cannot become an MVP dependency, and independent early create/join/rejoin limits satisfy this story. Trusted ingress or shared limiting remains an infrastructure follow-up.
- Post-fix checks: focused 24 contract/HTTP tests and 50 PostgreSQL tests pass, lint passes, tracked-tree Prettier passes, typecheck passes, the full PostgreSQL-enabled suite passes with 333 tests, Playwright smoke passes, Next production build passes, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because fixes addressed findings from all three specialist areas.

Targeted re-review outcome for US-027 at 2026-07-17 05:39:13 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff after the remediation pass.
- The merged high-severity blocker is that create/join command replay treats a public command ID plus repeated public intent as authority to mint a fresh active session for the original participant. A command ID is not a credential, so replay must require proof of the original scoped session or return immutable metadata without issuing a new credential.
- Remaining medium blockers are that rejoin command persistence and activation span two transactions; the 25-person snapshot roster can omit a required co-winner when self/host/winners exceed the bound; and the new capacity-concurrency test starts from an already-full lobby rather than proving one final-slot winner and one rejection.
- `qa-expert` also retained a high rate-limit denial/memory-growth finding for global/per-lobby keys and unbounded expired buckets. The lack of trusted peer metadata explains requester scoping but does not bound attacker-controlled canonical-code buckets; the next iteration must at minimum bound storage and establish a deployable trusted requester-key boundary or document a concrete safe alternative.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-027` remains `passes: false`, and `prd.json` remains unchanged.
- Next iteration start: redesign entry replay so credentials cannot be minted from command IDs, combine rejoin replay/activation atomically, make required snapshot references fit the bounded roster, correct the capacity race fixture, bound and safely key limiter storage, rerun all checks, and restart the complete staged review loop.

## 2026-07-17 05:56:29 MST - US-027
- What was implemented
  - Resumed the blocked lobby-entry story and changed create/join command replays to return the original immutable session metadata without persisting or issuing a new credential.
  - Replaced the split rejoin command/activation path with one Serializable lobby-scoped repository transaction that expires due sessions, authenticates the hash-only credential, validates idempotency, activates the session, and invalidates siblings atomically.
  - Bounded each in-memory mutation/read requester map, authenticated the trusted-proxy marker before hashing forwarded addresses, and used one bounded unidentified fallback when no valid trusted identity is available.
  - Consolidated cookie authentication, lock-time expiry, and authorized snapshot projection into one transaction; unrelated departed history is excluded and the 26th entry is allowed only for a waiting actor beside the 25-player current round.
  - Corrected the capacity concurrency fixture to race two joins for one remaining slot and documented the trusted ingress and replay invariants.
- Files changed: `.gitignore`, `AGENTS.md`, `README.md`, `package.json`, `bun.lock`, `eslint.config.mjs`, `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next-env.d.ts`, `apps/web/next.config.ts`, `apps/web/src/app/api/v1/**/route.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `apps/web/src/participant-session.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/index.ts`, `packages/database/package.json`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `tests/contracts-v1.test.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-027 - Implement lobby entry HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts` (expected fail with 5 replay, requester-isolation, bucket-bound, and atomic-rejoin regressions; review remediations then failed in 3 trusted-proxy/read-limit/snapshot-transaction areas; pass after implementation with 25 tests)
  - `bunx vitest run tests/contracts-v1.test.ts` (expected fail for the required 26-person bounded snapshot projection; pass after implementation with 85 tests)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts` (expected fail with 4 replay, snapshot-reference, and atomic-rejoin regressions after correcting a local Ruby command-substitution compatibility error; review remediations then failed in 3 immutable-replay/lock-time timestamp areas; pass after implementation with 54 tests)
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts apps/web/src/participant-session.test.ts tests/contracts-v1.test.ts tests/lobby-entry-contracts-v1.test.ts` (pass after review remediation with 126 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run format:check` (not rerun because it still scans three pre-existing untracked US-018 report files; the complete tracked tree passes)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass after review remediation with 21 Vitest files and 344 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all six `/api/v1` routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `security-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-027 and the work directly addressed concrete findings from the prior complete specialist review
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: fixed trusted-proxy spoofing, unrelated departed-roster exposure, mutable replay reconstruction, pre-fence timestamps, unbounded fenced reads, and split snapshot authentication before targeted re-review
- Decisions (why):
  - Returned original entry/session metadata with no `Set-Cookie` on replay because a public command ID can support idempotent result retrieval but cannot prove participant identity or authorize credential issuance.
  - Stored the complete allowlisted original entry projection inside the private command result rather than adding schema columns because mutable participant/lobby rows cannot reconstruct an immutable replay and no migration is needed.
  - Used one transaction for valid-cookie rejoin instead of composing status, command, and activation calls because expiry or command conflicts must roll back without partially activating or recording the session.
  - Allowed 26 snapshot participants only for the maximum 25-player round plus the requesting waiting replacement because all referenced winners, the host, and self must remain representable while durable departed history stays bounded out of the projection.
  - Required a secret proxy marker before accepting the rightmost forwarded address and hashed it before storage; direct, invalid, or unauthenticated headers share a bounded fallback rather than creating attacker-controlled keys.
- **Learnings for future iterations:**
  - Entry-command idempotency records must retain immutable response metadata, but credential issuance always requires separate scoped proof of identity.
  - Rejoin command persistence, expiry, activation, and sibling invalidation are one lifecycle transition and must share one lobby fence and sampled lock-time clock.
  - Snapshot collection bounds must account for required historical references in addition to current admission capacity; one waiting replacement can coexist with a full prior-round winner roster.
  - Process-local limiter maps need both trusted requester normalization and a hard active-bucket bound; expiry cleanup alone does not prevent active-key memory growth.
- Known issues / follow-ups:
  - The unrelated untracked US-018 browser reports remain untouched and unstaged; root `bun run format:check` continues to scan three of them while every tracked file passes Prettier.
  - The current limiter is intentionally process-local for the single-instance MVP. Multi-instance shared limiting remains future infrastructure work and must not introduce a default Redis dependency.
- Next iteration start:
  - If review passes, implement US-028 single-use realtime tickets; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-027 at 2026-07-17 06:18:10 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced six deduplicated blocking findings: forwarded addresses lacked an enforced trust marker; the 26-entry query exposed unrelated departed history; replay responses reconstructed mutable fields; snapshot and rejoin timestamps were sampled before the fence; status/snapshot reads could contend on lobby fences without limits; and snapshot authentication plus projection used two transactions.
- Added failing regressions before remediation: 3 HTTP failures for direct-header spoofing, unbounded status reads, and preliminary snapshot authentication; 1 contract failure for unconditional 26-entry rosters; and 3 PostgreSQL failures for mutable replay state plus pre-fence snapshot/rejoin timestamps.
- Added a minimum-32-character `TRUSTED_PROXY_SECRET` runtime setting and constant-time secret-marker verification before forwarded addresses can produce requester keys; unauthenticated headers now map to the bounded unidentified bucket.
- Added independent status/snapshot limiter scopes before lobby lookup and changed snapshot repository input to the scoped cookie hash so authentication, expiry, timestamping, and projection share one Serializable transaction.
- Persisted the complete strict allowlisted lobby-entry response inside each private command result and decode that immutable projection on create/join/rejoin replay.
- Sampled rejoin command and snapshot timestamps from the repository clock after acquiring the lobby fence on every retry.
- Restricted the roster query to self, host, current active participants, current-round card owners, and winners; the contract permits 26 only for one waiting self plus 25 playing current-round participants.
- Post-fix checks: focused 126 HTTP/session/contract tests pass, 54 PostgreSQL tests pass, lint passes, tracked-tree Prettier passes, typecheck passes, the PostgreSQL-enabled root suite passes with 344 tests, Playwright smoke passes, Next production build passes, and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because substantive fixes addressed all three specialist areas.

Targeted re-review outcome for resumed US-027 at 2026-07-17 06:24:04 MST:
- `security-engineer` returned an empty actionable finding list and confirmed the authenticated proxy marker, bounded read/mutation limits, single-transaction snapshot authentication, lock-time production timestamps, and replay credential protections.
- `code-reviewer` and `qa-expert` identified one deduplicated high blocker: after a departed current-round player is replaced, the SQL projection returns all 25 current-round card owners plus the waiting replacement to every actor. The 26-entry contract correctly permits that projection only when self is waiting, so a playing host/player snapshot can fail parsing and return 500.
- `qa-expert` identified one medium test blocker: the snapshot/rejoin timestamp tests inject a later clock value but never hold the lobby fence while the operation waits, so they do not prove the clock is sampled after lock acquisition on a retry or delayed transaction.
- Both findings are in scope because authorized snapshot availability and lock-time lifecycle sampling are explicit repository invariants introduced by this story.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-027` remains `passes: false`, and `prd.json` remains unchanged.
- Next iteration start: add a dual-actor full-roster regression, make roster selection include the waiting replacement only for a waiting requester while preserving all required host/winner/current-round references, add coordinated fence-blocking snapshot and rejoin timestamp tests, rerun all checks, and restart the complete staged review loop.

## 2026-07-17 06:33:16 MST - US-027
- What was implemented
  - Resumed the blocked lobby-entry story with a dual-actor full-roster regression covering both the waiting replacement and a current-round playing actor.
  - Limited authorized snapshot projection to 25 participants for playing requesters while retaining the 26th waiting replacement only when that participant requests the snapshot.
  - Replaced nominal timestamp assertions with coordinated PostgreSQL fence-blocking tests that prove snapshots and atomic rejoin expiry sample lifecycle time after lock acquisition.
- Files changed: `.gitignore`, `AGENTS.md`, `README.md`, `package.json`, `bun.lock`, `eslint.config.mjs`, `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next-env.d.ts`, `apps/web/next.config.ts`, `apps/web/src/app/api/v1/**/route.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `apps/web/src/participant-session.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/index.ts`, `packages/database/package.json`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `tests/contracts-v1.test.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-027 - Implement lobby entry HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts -t 'returns actor-valid rosters'` (expected fail: playing actor received an invalid 26-participant snapshot)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts -t 'returns actor-valid rosters|samples the snapshot timestamp|samples atomic rejoin expiry'` (pass with 3 tests)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts` (pass with 55 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run format:check` (fail only on three pre-existing untracked browser-report files)
  - `bun run typecheck` (pass; root and all nine workspaces)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass with 21 Vitest files and 345 tests)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all six `/api/v1` routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `security-engineer`
  - Used: `api-designer`, `security-engineer` for resumed contract-completeness, replay, cookie, authorization, proxy trust, rate-limit, and actor-scoped snapshot guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer` planned for the complete staged diff
  - Findings: pending staged-diff review
- Decisions (why):
  - Parameterized the SQL projection limit from the authenticated actor's durable round eligibility because the existing reference-priority order already retains host, winners, and current-round card owners before a waiting replacement.
  - Coordinated tests with a real PostgreSQL row lock because changing an injected clock without proving lock contention cannot verify lock-time sampling.
  - Did not add more `AGENTS.md` guidance because the root guide already states the requester-specific 26-entry roster exception and lock-time lifecycle rule.
- **Learnings for future iterations:**
  - Snapshot roster limits are actor-dependent: a waiting replacement may need 26 entries, but playing actors must receive only the 25 current-round references.
  - Tests for post-fence timestamps must hold the lobby row lock, observe the repository operation waiting, advance the injected clock, and release the lock before asserting persisted or projected time.
- Known issues / follow-ups:
  - The unrelated untracked browser reports remain untouched and unstaged; root `bun run format:check` reports three of them while every tracked file passes Prettier.
  - The current limiter remains intentionally process-local for the single-instance MVP; multi-instance shared limiting is future infrastructure work.
- Next iteration start:
  - If review passes, implement US-028 single-use realtime tickets; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-027 at 2026-07-17 06:47:55 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced three deduplicated blocking findings: waiting snapshots could still exceed the contract after a partial round refilled; `DATABASE_URL` and `TRUSTED_PROXY_SECRET` bypassed shared runtime parsing; and framework-generated unknown-route/unsupported-method responses bypassed stable versioned `no-store` errors.
- Added failing PostgreSQL and runtime-configuration regressions, then limited 26-entry snapshots to waiting requesters only when the current round actually has 25 card-owning participants.
- Extended `parseRuntimeConfig` with optional web database/proxy settings and secret-safe validation, and changed the web runtime to consume only the immutable parsed result.
- Forwarded all supported Next route-handler methods through the shared dispatcher and added an explicit private catch-all route; live Next development requests confirmed unknown resources and unsupported methods return versioned `NOT_FOUND` JSON with `Cache-Control: no-store`.
- Added reusable root guidance for preserving stable private errors across the framework routing boundary.
- Post-fix checks: focused runtime configuration and 4 PostgreSQL regression tests pass; the PostgreSQL-enabled root suite passes with 21 files and 347 tests; lint, tracked-tree Prettier, typecheck, Playwright smoke, Next production build, and whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because substantive fixes affected each original specialist area.

## 2026-07-17 10:55:48 MST - US-027
- What was implemented
  - Resumed the staged lobby-entry HTTP API candidate after the prior remediation added bounded actor-valid snapshots, shared runtime parsing, and stable framework-route errors.
  - Revalidated the complete implementation without changing its behavior before restarting the required staged review loop.
- Files changed: `.gitignore`, `AGENTS.md`, `README.md`, `package.json`, `bun.lock`, `eslint.config.mjs`, `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next-env.d.ts`, `apps/web/next.config.ts`, `apps/web/src/app/api/v1/**/route.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `apps/web/src/participant-session.ts`, `packages/contracts/src/runtime-config.ts`, `packages/contracts/src/v1/events.ts`, `packages/contracts/src/v1/game-state.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/index.ts`, `packages/database/package.json`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `tests/contracts-v1.test.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `tests/runtime-config.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-027 - Implement lobby entry HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass; typecheck plus 21 Vitest files and 347 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run test:e2e` (pass; 1 Playwright runner smoke test)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all versioned API routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `api-designer`, `security-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-027 and the staged candidate directly incorporates the prior specialist remediation
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer` planned for the restarted complete staged review
  - Findings: pending staged-diff review
- Decisions (why):
  - Restarted review against the complete staged candidate because the prior iteration ended after substantive remediation without recording its targeted review outcome.
  - Kept unrelated untracked browser artifacts untouched because they predate and do not overlap the selected story.
- **Learnings for future iterations:**
  - A resumed story with a fully staged candidate should rerun current checks and review the complete staged diff rather than relying only on a prior iteration's pending review state.
- Known issues / follow-ups:
  - Root `bun run format:check` still scans unrelated untracked browser artifacts; the complete tracked tree passes Prettier.
- Next iteration start:
  - If review passes, implement US-028 single-use realtime tickets; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-027 at 2026-07-17 11:36:18 MST:
- The restarted `code-reviewer`, `qa-expert`, and `security-engineer` pass produced three deduplicated medium blockers: the 26-participant projection ignored durable lobby status, unsupported methods queried the lobby without a limiter, and route-source assertions did not exercise the real Next routing boundary.
- Added failing HTTP and PostgreSQL regressions before implementation. Unsupported private methods now return the stable `no-store` error before lobby lookup, and the 26-entry exception now requires the fenced lobby status to be `ACTIVE` in addition to one waiting requester and a full 25-card current round.
- Added a database-backed Playwright request test that starts Next through `webServer` and verifies bare lobby paths, unknown nested resources, and unsupported methods return versioned `NOT_FOUND` JSON with `Cache-Control: no-store`.
- Updated root guidance and Codebase Patterns with the reusable live framework-boundary testing requirement.
- Post-fix checks: focused HTTP and PostgreSQL regressions pass; `TEST_DATABASE_URL=<approved local Compose database> bun run test` passes with typecheck, 21 files, and 350 tests; `bun run lint` passes; tracked-tree Prettier passes; the database-backed Playwright suite passes with 2 tests; the Next production build passes; frozen install and whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because substantive fixes addressed all three specialist areas.

Targeted re-review outcome for resumed US-027 at 2026-07-17 11:36:18 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the revised complete staged diff and returned empty actionable finding lists.
- The reviewers confirmed the active-lobby 26-entry guard, pre-lookup unsupported-method rejection, and database-backed live Next route coverage fully resolve the initial findings without introducing a new critical, high, or medium issue.
- No unresolved in-scope blocking findings remain. US-027 can be marked passing and committed after the final staged/unstaged consistency check.

## 2026-07-17 12:06:32 MST - US-028
- What was implemented
  - Added strict v1 realtime-ticket request/response contracts and `POST /api/v1/lobbies/{code}/realtime-ticket` with origin protection, an independent bounded rate limit, scoped cookie authentication, collision retries, private caching, and secret-safe errors.
  - Added an append-only PostgreSQL migration and repository operations that issue tickets from an active lobby/session under the lobby fence, persist only SHA-256 hashes and authoritative scope, atomically consume one ticket exactly once, purge expired abandoned rows, and invalidate tickets when sessions disconnect.
  - Added a transport-neutral game-server authentication boundary that validates canonical ticket syntax, hashes immediately, and returns only the persisted lobby, participant, and participant-session identity for the later Socket.IO adapter.
  - Added contract, HTTP, game-server, and PostgreSQL tests for canonical credentials, hash-only persistence, authorization, rate isolation, collision retry, exact expiry, concurrent single-use, reconnect renewal, scope derivation, and redaction.
- Files changed: `AGENTS.md`, `apps/game-server/src/realtime-ticket.ts`, `apps/game-server/src/realtime-ticket.test.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/primitives.ts`, `packages/database/prisma/schema.prisma`, `packages/database/prisma/migrations/20260717120000_add_realtime_tickets/migration.sql`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-028 - Issue single-use realtime tickets`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run tests/lobby-entry-contracts-v1.test.ts` (expected fail before implementation because ticket contracts were absent; pass after implementation)
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts` (expected fail before implementation with four missing endpoint/rate-limit behaviors; pass after implementation)
  - `bunx vitest run apps/game-server/src/realtime-ticket.test.ts` (expected fail before implementation because the authentication boundary was absent; pass after implementation)
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts -t 'realtime ticket|atomically consumes|expires tickets'` (expected fail before implementation with three missing repository behaviors; pass after migration and implementation)
  - `DATABASE_URL=<approved local Compose database> bun run db:migrate:deploy` (pass; append-only realtime-ticket migration applied locally)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass after review remediation; typecheck, 22 Vitest files, and 366 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `bun run format:check` (fails only on three pre-existing untracked US-018 browser-report files; intended tracked files pass)
  - `TEST_DATABASE_URL=<approved local Compose database> DATABASE_URL=<approved local Compose database> bun run test:e2e` (pass; 2 Playwright tests)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all versioned API routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `security-engineer`, `websocket-engineer`
  - Used: `security-engineer`, `websocket-engineer` for credential threat modeling, hash-only persistence, atomic consume semantics, future Socket.IO boundary design, authorization/error behavior, and test coverage
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer`; targeted re-review by the same specialists planned after substantive fixes
  - Findings: fixed stale expired-ticket retention, nondeterministic concurrent-consume coverage, missing valid cross-lobby denial coverage, incomplete reconnect renewal coverage, and pre-disconnect ticket revival before targeted re-review
- Decisions (why):
  - Implemented a transport-neutral game-server consume boundary but did not add Socket.IO or a long-lived runtime because US-030 owns that dependency and server lifecycle.
  - Made issuance non-idempotent and omitted command IDs because every successful request must mint a fresh credential, while persisting or replaying plaintext would violate the hash-only requirement.
  - Bound each ticket to lobby, participant, and participant session with composite foreign keys so the future adapter derives identity only from committed persistence and outstanding tickets cascade with scoped lifecycle deletion.
  - Used one conditional PostgreSQL `DELETE ... RETURNING` for consumption because a check-then-delete sequence could admit concurrent replay across the separate web and game-server processes.
- **Learnings for future iterations:**
  - Socket.IO authentication in US-030 should call `consumeRealtimeTicketCredential`, use only its returned identity, pass the ticket through handshake auth rather than a URL, and require HTTP renewal for every reconnect.
  - Ticket issuance and consumption sample the injected repository lifecycle clock inside retryable transactions; exact expiry is `now >= expiresAt`.
  - Canonical unpadded base64url for 32 bytes is 43 characters and constrains the final character; length and alphabet checks alone accept noncanonical encodings.
- Known issues / follow-ups:
  - The unrelated untracked US-018 browser reports remain untouched and unstaged; root `bun run format:check` reports three of them while every tracked file passes Prettier.
  - The actual Socket.IO connection lifecycle, handshake integration, and reconnect fetch flow remain intentionally assigned to US-030.
- Next iteration start:
  - If review passes, implement US-029 round-control HTTP APIs; otherwise resume any recorded blocking finding.
---

Review remediation for US-028 at 2026-07-17 12:19:00 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced five deduplicated blocking findings: expired/abandoned ticket retention, unsynchronized consume concurrency coverage, missing valid cross-lobby scope denial, incomplete renewal-flow coverage, and pre-disconnect ticket revival after same-session rejoin.
- Added failing PostgreSQL regressions for expired-row deletion and disconnect/rejoin revival, then changed consumption to atomically delete every presented ticket before returning identity only for a valid active scope.
- Purged expired abandoned ticket rows during issuance and invalidated outstanding session tickets in the same transaction that disconnects or expires the participant session.
- Coordinated two consumers behind a real ticket-row lock before release, used a valid second-lobby session for scope denial, and added HTTP/game-server renewal tests proving distinct credentials, replay rejection, and fresh reconnect acceptance.
- Post-fix checks: focused HTTP, game-server, and PostgreSQL tests pass; the PostgreSQL-enabled root suite passes with typecheck, 22 files, and 366 tests; lint, tracked-tree Prettier, Playwright, Next production build, and whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because the substantive fixes affected all three original specialist areas.

Targeted re-review outcome for US-028 at 2026-07-17 12:20:00 MST:
- `code-reviewer` and `security-engineer` returned empty actionable finding lists and confirmed the expired-ticket cleanup, burn-on-presentation behavior, scope enforcement, and disconnect/rejoin invalidation fixes.
- `qa-expert` identified one remaining medium blocker: if the deterministic concurrent-consume wait or commit fails, the checked-out PostgreSQL client can retain its transaction and ticket-row lock because release/rollback are not protected by `try/finally`, which can hang pending consumers and teardown instead of producing an isolated test failure.
- The next iteration must wrap the row-lock setup/release in failure-safe rollback and client release, ensure pending consumers settle on synchronization failure, rerun all checks, and restart the complete staged review loop.
- Per the one-targeted-re-review limit, no further implementation changes or commit are permitted in this iteration. `US-028` remains `passes: false`, and `prd.json` remains unchanged.

## 2026-07-17 12:28:37 MST - US-028
- What was implemented
  - Resumed the single-use realtime-ticket story and made its deterministic concurrent-consume PostgreSQL harness failure-safe.
  - Wrapped the checked-out row-lock transaction in `try/finally`, rolled back an open transaction before release, and awaited both blocked consumers during cleanup so a synchronization or commit failure cannot hang the suite.
  - Added the reusable failure-safe lock-coordination convention to root guidance and Codebase Patterns.
- Files changed: `AGENTS.md`, `apps/game-server/src/realtime-ticket.ts`, `apps/game-server/src/realtime-ticket.test.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-runtime.ts`, `packages/contracts/src/v1/http.ts`, `packages/contracts/src/v1/primitives.ts`, `packages/database/prisma/schema.prisma`, `packages/database/prisma/migrations/20260717120000_add_realtime_tickets/migration.sql`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `tests/lobby-entry-contracts-v1.test.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-028 - Issue single-use realtime tickets`
- Auto approval: `enabled`
- Checks:
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts -t "atomically consumes one ticket once"` (pass; 1 focused PostgreSQL test)
  - `bunx vitest run apps/game-server/src/realtime-ticket.test.ts apps/web/src/lobby-entry-http.test.ts tests/lobby-entry-contracts-v1.test.ts` (pass; 3 files and 45 tests)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (pass; typecheck, 22 Vitest files, and 366 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `TEST_DATABASE_URL=<approved local Compose database> DATABASE_URL=<approved local Compose database> bun run test:e2e` (pass; 2 Playwright tests)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all versioned API routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `security-engineer`, `websocket-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-028 and the remaining work directly addressed the concrete prior QA finding
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer`
  - Findings: none; all specialists returned empty actionable finding lists
- Decisions (why):
  - Kept the remediation inside the integration test because the production atomic-consume behavior already passed correctness and security review; the outstanding defect was failure isolation in the lock-coordination harness.
  - Awaited the individual consumer promises with `Promise.allSettled` during cleanup because `Promise.all` may reject before every blocked operation settles.
  - Did not add a synthetic test of the test harness because the change is a behavior-preserving test refactor; the real focused PostgreSQL concurrency test is the meaningful validation.
- **Learnings for future iterations:**
  - Checked-out PostgreSQL clients used as deterministic lock barriers need rollback and release in `finally`, even when the normal path commits successfully.
  - Cleanup must await every operation blocked behind a test-owned lock so assertion failures remain isolated and pool teardown cannot hang.
- Known issues / follow-ups:
  - The unrelated untracked browser-report artifacts remain untouched and unstaged; tracked files pass Prettier.
  - Socket.IO handshake wiring and reconnect lifecycle integration remain intentionally assigned to US-030.
- Next iteration start:
  - If review passes, implement US-029 round-control HTTP APIs; otherwise resume any recorded blocking finding.
---

Review outcome for resumed US-028 at 2026-07-17 12:28:37 MST:
- `code-reviewer`, `qa-expert`, and `security-engineer` reviewed the complete restaged high-risk diff and returned empty actionable finding lists.
- The reviewers confirmed the failure-safe PostgreSQL lock cleanup resolves the prior QA blocker without changing the already-reviewed issuance, expiry, burn-on-presentation, scope, renewal, or redaction behavior.
- Residual risk is limited to actual Socket.IO handshake and browser reconnect integration intentionally assigned to US-030. No in-scope critical, high, or medium findings remain; US-028 can be marked passing and committed.

## 2026-07-17 12:59:39 MST - US-029
- What was implemented
  - Added strict `POST` endpoints for configure, create/start/pause/resume/call-next/continue/end round, and own-card mark commands through the shared versioned private dispatcher.
  - Added scoped-cookie authentication, endpoint-specific command parsing, host versus own-card authorization, stable private errors, and committed active-lobby or participant-private acknowledgements.
  - Added a PostgreSQL round-command executor that resolves identity and command scope under the lobby fence, applies canonical domain transitions and authoritative card/draw operations, persists state/events/results atomically, rejects changed-intent command-ID reuse, and replays original committed metadata without repeating effects.
  - Added HTTP, pure command-rule, and PostgreSQL integration coverage for route contracts, call configuration, cookie scope, authorization, own-card marking, privacy, sequences, and idempotent replay.
- Files changed: `AGENTS.md`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-runtime.ts`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/participant-session-expiry.ts`, `packages/database/src/round-command-executor.test.ts`, `packages/database/src/round-command-executor.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-029 - Implement round-control HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts` (expected fail before implementation with 23 missing route/contract behaviors; pass after implementation)
  - `bunx vitest run apps/web/src/lobby-entry-http.test.ts packages/database/src/round-command-executor.test.ts` (pass; 2 files and 57 tests)
  - `bun run typecheck` (pass; workspace boundaries and all nine workspaces)
  - `bun run lint` (pass)
  - `bun run test` (pass after review remediation; 22 files and 333 tests; 70 PostgreSQL tests skipped because `TEST_DATABASE_URL` is unset)
  - `bunx prettier --check <intended story files>` (pass)
  - `bun run format:check` (fails only on three pre-existing untracked browser-report files; all intended files pass)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: none in story notes
  - Used: `api-designer` for endpoint/application-port guidance and `backend-developer` for the atomic PostgreSQL executor; both were within the standard-mode complex-story budget
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer`; targeted re-review planned for the remediated areas
  - Findings: fixed missing pre-fence command throttling, round-replacement replay loss, and insufficient command-path/idempotency/authorization coverage before targeted re-review
- Decisions (why):
  - Reused the existing v1 mutation command and acknowledgement contracts instead of adding HTTP-only shapes so fallback HTTP and later realtime transports share one intent model.
  - Kept canonical pattern lookup injected from the web runtime so Prisma remains confined to `packages/database` without introducing a database-to-pattern-catalog dependency.
  - Performed session, role, round, and own-card resolution inside the command transaction because separate HTTP authorization would create a stale-identity race before persistence.
  - Persisted canonical command intent in the result because command type and round ID alone cannot detect a reused configure, continue, or mark command ID with changed payload.
- **Learnings for future iterations:**
  - Active-lobby acknowledgements must use the persisted command-result sequence on replay because a replay intentionally produces no fresh broadcast event.
  - Participant-private mark results persist with `eventSequence: null`; only lobby-visible commands allocate the monotonic active-lobby sequence.
  - The catch-all Next route already exports every supported HTTP method, so new private resources belong in the shared dispatcher to preserve stable `no-store` errors rather than in isolated route modules.
- Known issues / follow-ups:
  - PostgreSQL integration cases were added but could not run because no approved `TEST_DATABASE_URL` is present; the generic suite skipped the existing 70 database tests.
  - The unrelated untracked browser-report artifacts remain untouched and unstaged; root `bun run format:check` reports three of them while intended story files pass Prettier.
  - Realtime Socket.IO command transport and post-commit broadcasting remain assigned to US-030.
- Next iteration start:
  - If review passes, implement US-030 authenticated Socket.IO; otherwise resume any recorded blocking finding.
---

Review remediation for US-029 at 2026-07-17 13:10:00 MST:
- The initial `code-reviewer`, `qa-expert`, and `security-engineer` pass produced four deduplicated blockers: no pre-fence command rate limit, round replacement deleting replay protection, incomplete PostgreSQL command-path coverage, and insufficient changed-intent/concurrency/scope authorization coverage.
- Added a failing HTTP regression before implementing a bounded command limiter that rejects excess requests before body, lobby lookup, or transaction-fence work.
- Detached prior command-result round scopes before deleting an ended round so replay intent and original acknowledgement metadata survive while old cards, calls, marks, draw order, winners, and round events still cascade.
- Added PostgreSQL cases covering create/configure/start/pause/resume/call-next/continue/end/mark behavior, generated card/draw state, automatic deadlines, state preservation, changed-intent rejection, cross-lobby and disconnected credentials, concurrent duplicates, round-replacement replay, sequence allocation, and rollback/no-mutation outcomes.
- Preserved monotonic lobby activity for participant-private marks with `GREATEST`, matching existing command activity behavior.
- Post-fix checks: focused HTTP and executor tests pass; `bun run typecheck`, `bun run lint`, and `bun run test` pass with 333 tests and 70 PostgreSQL tests skipped without `TEST_DATABASE_URL`; intended-file Prettier and staged/unstaged whitespace checks pass.
- Targeted re-review: pending `code-reviewer`, `qa-expert`, and `security-engineer` because substantive fixes affected every original specialist area.

Targeted re-review outcome for US-029 at 2026-07-17 13:12:00 MST:
- `qa-expert` and `security-engineer` returned empty actionable finding lists; their residual risk is the unexecuted PostgreSQL suite while `TEST_DATABASE_URL` is absent.
- `code-reviewer` found one remaining high blocker: detaching `command_results.round_id` does not preserve lobby-visible replay tombstones because those results still reference round-scoped `active_lobby_events`, and deleting the round cascades the event then the command result through `command_results_event_fkey`.
- The blocker is in scope because a delayed old start/configure/call command can otherwise execute against the replacement round instead of replaying its original acknowledgement or rejecting changed intent.
- Per the one-targeted-re-review limit, US-029 remains `passes: false`; `prd.json` was not updated and no commit was created.
- Next iteration start: detach both command-result and referenced active-event round scopes before deleting an ended round, add a regression that verifies the event/result rows and original acknowledgement survive replacement, rerun all checks, and restart the complete staged review loop.

## 2026-07-17 13:17:02 MST - US-029
- What was implemented
  - Resumed the round-control HTTP API story and fixed the remaining round-replacement replay cascade identified by the prior targeted review.
  - Detached prior-round active-lobby event scopes before deleting the ended round, preserving the event rows required by lobby-visible command replay tombstones while old gameplay rows still cascade.
  - Strengthened the PostgreSQL regression to verify the original event, command results, sequence, timestamp, scope, and acknowledgement survive replacement without re-executing the old command.
  - Corrected an independently exposed changed-intent test fixture so its call-free round no longer retained an invalid called-ball mark.
- Files changed: `AGENTS.md`, `apps/web/src/lobby-entry-http.test.ts`, `apps/web/src/lobby-entry-http.ts`, `apps/web/src/lobby-entry-runtime.ts`, `packages/database/src/database.integration.test.ts`, `packages/database/src/index.ts`, `packages/database/src/lobby-state-repository.ts`, `packages/database/src/participant-session-expiry.ts`, `packages/database/src/round-command-executor.test.ts`, `packages/database/src/round-command-executor.ts`, `progress.txt`, `prd.json`
- Commit message: `feat: US-029 - Implement round-control HTTP APIs`
- Auto approval: `enabled`
- Checks:
  - `TEST_DATABASE_URL=<approved local Compose database> bunx vitest run packages/database/src/database.integration.test.ts -t "preserves command replay intent when an ended round is replaced"` (expected fail before implementation because the old command executed again with sequence 3; pass after implementation)
  - `TEST_DATABASE_URL=<approved local Compose database> bun run test` (initial fail on an invalid pre-existing changed-intent fixture; pass after fixture correction with typecheck, 23 files, and 403 tests)
  - `bun run lint` (pass)
  - `git ls-files -z | xargs -0 bunx prettier --check --ignore-unknown` (pass; complete tracked tree)
  - `TEST_DATABASE_URL=<approved local Compose database> DATABASE_URL=<approved local Compose database> bun run test:e2e` (pass; 2 Playwright tests)
  - `DATABASE_URL=<approved local Compose database> bun run --cwd apps/web build` (pass; all versioned API routes compiled)
  - `bun install --frozen-lockfile` (pass; 371 installs checked with no changes)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: none in story notes
  - Used: skipped additional agents because `api-designer` and `backend-developer` already advised US-029, and this resumed iteration directly addressed one concrete prior review finding
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, and security-sensitive `security-engineer` planned for the complete restaged high-risk diff
  - Findings: fixed the blocking due-session roster-selection finding; targeted `code-reviewer` re-review returned no actionable findings
- Decisions (why):
  - Detached the active-event scope before command-result scope because both rows must survive round deletion for the existing composite event reference and original acknowledgement sequence to remain valid.
  - Preserved prior active-lobby event rows rather than changing the append-only migration history or weakening the command-result event foreign key because the targeted review required replay metadata preservation within the existing durable model.
  - Ran the previously skipped PostgreSQL suite against the healthy local Compose database because scoped auto approval is enabled and the defect depends on real cascade behavior.
- **Learnings for future iterations:**
  - A lobby-visible replay tombstone depends on both `command_results` and its referenced `active_lobby_events` row; detaching only the command result does not prevent the event cascade from deleting it.
  - Database integration fixtures must preserve called-ball coherence when removing calls: cards in a call-free round cannot retain marks.
- Known issues / follow-ups:
  - The unrelated untracked browser-report artifacts remain untouched and unstaged; tracked files pass Prettier.
  - Realtime Socket.IO command transport and post-commit broadcasting remain assigned to US-030.
- Next iteration start:
  - If review passes, implement US-030 authenticated Socket.IO; otherwise resume any recorded blocking finding.
---

Review remediation for resumed US-029 at 2026-07-17 13:27:00 MST:
- The complete staged `code-reviewer`, `qa-expert`, and `security-engineer` pass produced one blocking medium finding: create-round roster selection did not first expire disconnected sessions whose rejoin deadline had passed. QA and security returned empty finding lists.
- Added a failing PostgreSQL regression proving the overdue participant remained active and received a replacement-round card before remediation.
- Extracted the existing lobby-fenced session-expiry transition into a shared database-internal helper and invoked it with the post-fence command timestamp before selecting or updating the new-round roster.
- The regression now proves the due session and participant depart at the durable deadline and only the active host receives a card.
- Post-fix checks: focused expiry regression passes; `TEST_DATABASE_URL=<approved local Compose database> bun run test` passes typecheck, 23 files, and 404 tests; lint, intended-file Prettier, and whitespace checks pass.
- Targeted re-review: pending `code-reviewer` review because its finding received the substantive production and integration-test fix; QA and security are not repeated because their initial finding lists were empty.

Targeted re-review outcome for resumed US-029 at 2026-07-17 13:29:00 MST:
- `code-reviewer` reviewed the revised complete staged diff and returned an empty actionable finding list with no residual risks.
- The shared due-session expiry transition now runs inside the round-command lobby fence before roster selection, resolving the only blocking finding without changing the reviewed authorization, replay, or sequencing behavior.
- No in-scope critical, high, or medium findings remain. US-029 can be marked passing and committed after the final staged/unstaged consistency check.
